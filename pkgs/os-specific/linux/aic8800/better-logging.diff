diff --git a/aic8800_fdrv/aic_br_ext.c b/aic8800_fdrv/aic_br_ext.c
index dd038e3..a6e18e8 100644
--- a/aic8800_fdrv/aic_br_ext.c
+++ b/aic8800_fdrv/aic_br_ext.c
@@ -92,7 +92,7 @@ static __inline__ int __nat25_add_pppoe_tag(struct sk_buff *skb, struct pppoe_ta
 
 	data_len = tag->tag_len + TAG_HDR_LEN;
 	if (skb_tailroom(skb) < data_len) {
-		printk("skb_tailroom() failed in add SID tag!\n");
+		AICWFDBG(LOGERROR, "skb_tailroom() failed in add SID tag!\n");
 		return -1;
 	}
 
@@ -232,7 +232,7 @@ static int update_nd_link_layer_addr(unsigned char *data, int len, unsigned char
 		if (len >= 8) {
 			mac = scan_tlv(&data[8], len - 8, 1, 1);
 			if (mac) {
-				printk("Router Solicitation, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
+				AICWFDBG(LOGTRACE, "Router Solicitation, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
 					mac[0], mac[1], mac[2], mac[3], mac[4], mac[5],
 					replace_mac[0], replace_mac[1], replace_mac[2], replace_mac[3], replace_mac[4], replace_mac[5]);
 				memcpy(mac, replace_mac, 6);
@@ -243,7 +243,7 @@ static int update_nd_link_layer_addr(unsigned char *data, int len, unsigned char
 		if (len >= 16) {
 			mac = scan_tlv(&data[16], len - 16, 1, 1);
 			if (mac) {
-				printk("Router Advertisement, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
+				AICWFDBG(LOGTRACE, "Router Advertisement, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
 					mac[0], mac[1], mac[2], mac[3], mac[4], mac[5],
 					replace_mac[0], replace_mac[1], replace_mac[2], replace_mac[3], replace_mac[4], replace_mac[5]);
 				memcpy(mac, replace_mac, 6);
@@ -254,7 +254,7 @@ static int update_nd_link_layer_addr(unsigned char *data, int len, unsigned char
 		if (len >= 24) {
 			mac = scan_tlv(&data[24], len - 24, 1, 1);
 			if (mac) {
-				printk("Neighbor Solicitation, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
+				AICWFDBG(LOGTRACE, "Neighbor Solicitation, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
 					mac[0], mac[1], mac[2], mac[3], mac[4], mac[5],
 					replace_mac[0], replace_mac[1], replace_mac[2], replace_mac[3], replace_mac[4], replace_mac[5]);
 				memcpy(mac, replace_mac, 6);
@@ -265,7 +265,7 @@ static int update_nd_link_layer_addr(unsigned char *data, int len, unsigned char
 		if (len >= 24) {
 			mac = scan_tlv(&data[24], len - 24, 2, 1);
 			if (mac) {
-				printk("Neighbor Advertisement, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
+				AICWFDBG(LOGTRACE, "Neighbor Advertisement, replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
 					mac[0], mac[1], mac[2], mac[3], mac[4], mac[5],
 					replace_mac[0], replace_mac[1], replace_mac[2], replace_mac[3], replace_mac[4], replace_mac[5]);
 				memcpy(mac, replace_mac, 6);
@@ -276,7 +276,7 @@ static int update_nd_link_layer_addr(unsigned char *data, int len, unsigned char
 		if (len >= 40) {
 			mac = scan_tlv(&data[40], len - 40, 2, 1);
 			if (mac) {
-				printk("Redirect,  replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
+				AICWFDBG(LOGTRACE, "Redirect,  replace MAC From: %02x:%02x:%02x:%02x:%02x:%02x, To: %02x:%02x:%02x:%02x:%02x:%02x\n",
 					mac[0], mac[1], mac[2], mac[3], mac[4], mac[5],
 					replace_mac[0], replace_mac[1], replace_mac[2], replace_mac[3], replace_mac[4], replace_mac[5]);
 				memcpy(mac, replace_mac, 6);
@@ -408,7 +408,7 @@ static int __nat25_db_network_lookup_and_replace(struct rwnx_vif *vif,
 				atomic_inc(&db->use_count);
 
 #ifdef CL_IPV6_PASS
-				printk("NAT25: Lookup M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x"
+				AICWFDBG(LOGTRACE, "NAT25: Lookup M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x"
 					 "%02x%02x%02x%02x%02x%02x\n",
 					 db->macAddr[0],
 					 db->macAddr[1],
@@ -434,7 +434,7 @@ static int __nat25_db_network_lookup_and_replace(struct rwnx_vif *vif,
 					 db->networkAddr[15],
 					 db->networkAddr[16]);
 #else
-				printk("NAT25: Lookup M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x\n",
+				AICWFDBG(LOGTRACE, "NAT25: Lookup M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x\n",
 					 db->macAddr[0],
 					 db->macAddr[1],
 					 db->macAddr[2],
@@ -522,7 +522,7 @@ static void __nat25_db_print(struct rwnx_vif *vif)
 
 		while (db != NULL) {
 #ifdef CL_IPV6_PASS
-			printk("NAT25: DB(%d) H(%02d) C(%d) M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x"
+			AICWFDBG(LOGTRACE, "NAT25: DB(%d) H(%02d) C(%d) M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x"
 				     "%02x%02x%02x%02x%02x%02x\n",
 				     j,
 				     i,
@@ -551,7 +551,7 @@ static void __nat25_db_print(struct rwnx_vif *vif)
 				     db->networkAddr[15],
 				     db->networkAddr[16]);
 #else
-			printk("NAT25: DB(%d) H(%02d) C(%d) M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x\n",
+			AICWFDBG(LOGTRACE, "NAT25: DB(%d) H(%02d) C(%d) M:%02x%02x%02x%02x%02x%02x N:%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x%02x\n",
 				     j,
 				     i,
 				     atomic_read(&db->use_count),
@@ -753,7 +753,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		struct iphdr *iph = (struct iphdr *)(skb->data + ETH_HLEN);
 
 		if (((unsigned char *)(iph) + (iph->ihl << 2)) >= (skb->data + ETH_HLEN + skb->len)) {
-			printk("NAT25: malformed IP packet !\n");
+			AICWFDBG(LOGTRACE, "NAT25: malformed IP packet !\n");
 			return -1;
 		}
 
@@ -766,7 +766,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			/* in class A, B, C, host address is all zero or all one is illegal */
 			if (iph->saddr == 0)
 				return 0;
-			printk("NAT25: Insert IP, SA=%08x, DA=%08x\n", iph->saddr, iph->daddr);
+			AICWFDBG(LOGTRACE, "NAT25: Insert IP, SA=%08x, DA=%08x\n", iph->saddr, iph->daddr);
 			__nat25_generate_ipv4_network_addr(networkAddr, &iph->saddr);
 			/* record source IP address and , source mac address into db */
 			__nat25_db_network_insert(vif, skb->data + ETH_ALEN, networkAddr);
@@ -776,7 +776,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		return 0;
 
 		case NAT25_LOOKUP: {
-			printk("NAT25: Lookup IP, SA=%08x, DA=%08x\n", iph->saddr, iph->daddr);
+			AICWFDBG(LOGTRACE, "NAT25: Lookup IP, SA=%08x, DA=%08x\n", iph->saddr, iph->daddr);
 #ifdef SUPPORT_TX_MCAST2UNI
 			if (vif->pshare->rf_ft_var.mc2u_disable ||
 			    ((((OPMODE & (WIFI_STATION_STATE | WIFI_ASOC_STATE))
@@ -790,14 +790,14 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 				if (!__nat25_db_network_lookup_and_replace(vif, skb, networkAddr)) {
 					if (*((unsigned char *)&iph->daddr + 3) == 0xff) {
 						/* L2 is unicast but L3 is broadcast, make L2 bacome broadcast */
-						printk("NAT25: Set DA as boardcast\n");
+						AICWFDBG(LOGTRACE, "NAT25: Set DA as boardcast\n");
 						memset(skb->data, 0xff, ETH_ALEN);
 					} else {
 						/* forward unknow IP packet to upper TCP/IP */
-						printk("NAT25: Replace DA with BR's MAC\n");
+						AICWFDBG(LOGTRACE, "NAT25: Replace DA with BR's MAC\n");
 						if ((*(u32 *)vif->br_mac) == 0 && (*(u16 *)(vif->br_mac + 4)) == 0) {
 							void netdev_br_init(struct net_device *netdev);
-							printk("Re-init netdev_br_init() due to br_mac==0!\n");
+							AICWFDBG(LOGTRACE, "Re-init netdev_br_init() due to br_mac==0!\n");
 							netdev_br_init(vif->ndev);
 						}
 						memcpy(skb->data, vif->br_mac, ETH_ALEN);
@@ -821,7 +821,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		unsigned int *sender, *target;
 
 		if (arp->ar_pro != __constant_htons(ETH_P_IP)) {
-			printk("NAT25: arp protocol unknown (%4x)!\n", htons(arp->ar_pro));
+			AICWFDBG(LOGERROR, "NAT25: arp protocol unknown (%4x)!\n", htons(arp->ar_pro));
 			return -1;
 		}
 
@@ -830,7 +830,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			return 0;	/* skb_copy for all ARP frame */
 
 		case NAT25_INSERT: {
-			printk("NAT25: Insert ARP, MAC=%02x%02x%02x%02x%02x%02x\n", arp_ptr[0],
+			AICWFDBG(LOGTRACE, "NAT25: Insert ARP, MAC=%02x%02x%02x%02x%02x%02x\n", arp_ptr[0],
 				arp_ptr[1], arp_ptr[2], arp_ptr[3], arp_ptr[4], arp_ptr[5]);
 
 			/* change to ARP sender mac address to wlan STA address */
@@ -848,7 +848,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		return 0;
 
 		case NAT25_LOOKUP: {
-			printk("NAT25: Lookup ARP\n");
+			AICWFDBG(LOGTRACE, "NAT25: Lookup ARP\n");
 
 			arp_ptr += arp->ar_hln;
 			sender = (unsigned int *)arp_ptr;
@@ -884,11 +884,11 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		unsigned char *framePtr = skb->data + ETH_HLEN;
 
 		if (protocol == __constant_htons(ETH_P_IPX)) {
-			printk("NAT25: Protocol=IPX (Ethernet II)\n");
+			AICWFDBG(LOGTRACE, "NAT25: Protocol=IPX (Ethernet II)\n");
 			ipx = (struct ipxhdr *)framePtr;
 		} else { /* if(protocol <= __constant_htons(ETH_FRAME_LEN)) */
 			if (!memcmp(ipx_header, framePtr, 2)) {
-				printk("NAT25: Protocol=IPX (Ethernet 802.3)\n");
+				AICWFDBG(LOGTRACE, "NAT25: Protocol=IPX (Ethernet 802.3)\n");
 				ipx = (struct ipxhdr *)framePtr;
 			} else {
 				unsigned char ipx_8022_type =  0xE0;
@@ -904,7 +904,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 					if (!memcmp(ipx_snap_id, framePtr, 5)) {
 						framePtr += 5;	/* eliminate the SNAP header */
 
-						printk("NAT25: Protocol=IPX (Ethernet SNAP)\n");
+						AICWFDBG(LOGTRACE, "NAT25: Protocol=IPX (Ethernet SNAP)\n");
 						ipx = (struct ipxhdr *)framePtr;
 					} else if (!memcmp(aarp_snap_id, framePtr, 5)) {
 						framePtr += 5;	/* eliminate the SNAP header */
@@ -915,7 +915,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 
 						ddp = (struct ddpehdr *)framePtr;
 					} else {
-						printk("NAT25: Protocol=Ethernet SNAP %02x%02x%02x%02x%02x\n", framePtr[0],
+						AICWFDBG(LOGTRACE, "NAT25: Protocol=Ethernet SNAP %02x%02x%02x%02x%02x\n", framePtr[0],
 							framePtr[1], framePtr[2], framePtr[3], framePtr[4]);
 						return -1;
 					}
@@ -923,7 +923,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 					framePtr += 3;	/* eliminate the 802.2 header */
 
 					if (!memcmp(ipx_header, framePtr, 2)) {
-						printk("NAT25: Protocol=IPX (Ethernet 802.2)\n");
+						AICWFDBG(LOGTRACE, "NAT25: Protocol=IPX (Ethernet 802.2)\n");
 						ipx = (struct ipxhdr *)framePtr;
 					} else
 						return -1;
@@ -936,13 +936,13 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			switch (method) {
 			case NAT25_CHECK:
 				if (!memcmp(skb->data + ETH_ALEN, ipx->ipx_source.node, ETH_ALEN)) {
-					printk("NAT25: Check IPX skb_copy\n");
+					AICWFDBG(LOGTRACE, "NAT25: Check IPX skb_copy\n");
 					return 0;
 				}
 				return -1;
 
 			case NAT25_INSERT: {
-				printk("NAT25: Insert IPX, Dest=%08x,%02x%02x%02x%02x%02x%02x,%04x Source=%08x,%02x%02x%02x%02x%02x%02x,%04x\n",
+				AICWFDBG(LOGTRACE, "NAT25: Insert IPX, Dest=%08x,%02x%02x%02x%02x%02x%02x,%04x Source=%08x,%02x%02x%02x%02x%02x%02x,%04x\n",
 					 ipx->ipx_dest.net,
 					 ipx->ipx_dest.node[0],
 					 ipx->ipx_dest.node[1],
@@ -961,7 +961,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 					 ipx->ipx_source.sock);
 
 				if (!memcmp(skb->data + ETH_ALEN, ipx->ipx_source.node, ETH_ALEN)) {
-					printk("NAT25: Use IPX Net, and Socket as network addr\n");
+					AICWFDBG(LOGTRACE, "NAT25: Use IPX Net, and Socket as network addr\n");
 
 					__nat25_generate_ipx_network_addr_with_socket(networkAddr, &ipx->ipx_source.net, &ipx->ipx_source.sock);
 
@@ -978,7 +978,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 
 			case NAT25_LOOKUP: {
 				if (!memcmp(vif->ndev->dev_addr, ipx->ipx_dest.node, ETH_ALEN)) {
-					printk("NAT25: Lookup IPX, Modify Destination IPX Node addr\n");
+					AICWFDBG(LOGTRACE, "NAT25: Lookup IPX, Modify Destination IPX Node addr\n");
 
 					__nat25_generate_ipx_network_addr_with_socket(networkAddr, &ipx->ipx_dest.net, &ipx->ipx_dest.sock);
 
@@ -1003,7 +1003,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		else if (ea != NULL) {
 			/* Sanity check fields. */
 			if (ea->hw_len != ETH_ALEN || ea->pa_len != AARP_PA_ALEN) {
-				printk("NAT25: Appletalk AARP Sanity check fail!\n");
+				AICWFDBG(LOGERROR, "NAT25: Appletalk AARP Sanity check fail!\n");
 				return -1;
 			}
 
@@ -1015,7 +1015,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 				/* change to AARP source mac address to wlan STA address */
 				memcpy(ea->hw_src, vif->ndev->dev_addr, ETH_ALEN);
 
-				printk("NAT25: Insert AARP, Source=%d,%d Destination=%d,%d\n",
+				AICWFDBG(LOGTRACE, "NAT25: Insert AARP, Source=%d,%d Destination=%d,%d\n",
 					 ea->pa_src_net,
 					 ea->pa_src_node,
 					 ea->pa_dst_net,
@@ -1030,7 +1030,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			return 0;
 
 			case NAT25_LOOKUP: {
-				printk("NAT25: Lookup AARP, Source=%d,%d Destination=%d,%d\n",
+				AICWFDBG(LOGTRACE, "NAT25: Lookup AARP, Source=%d,%d Destination=%d,%d\n",
 					 ea->pa_src_net,
 					 ea->pa_src_node,
 					 ea->pa_dst_net,
@@ -1057,7 +1057,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 				return -1;
 
 			case NAT25_INSERT: {
-				printk("NAT25: Insert DDP, Source=%d,%d Destination=%d,%d\n",
+				AICWFDBG(LOGTRACE, "NAT25: Insert DDP, Source=%d,%d Destination=%d,%d\n",
 					 ddp->deh_snet,
 					 ddp->deh_snode,
 					 ddp->deh_dnet,
@@ -1072,7 +1072,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			return 0;
 
 			case NAT25_LOOKUP: {
-				printk("NAT25: Lookup DDP, Source=%d,%d Destination=%d,%d\n",
+				AICWFDBG(LOGTRACE, "NAT25: Lookup DDP, Source=%d,%d Destination=%d,%d\n",
 					 ddp->deh_snet,
 					 ddp->deh_snode,
 					 ddp->deh_dnet,
@@ -1119,7 +1119,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 						if (pOldTag) { /* if SID existed, copy old value and delete it */
 							old_tag_len = ntohs(pOldTag->tag_len);
 							if (old_tag_len + TAG_HDR_LEN + MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN > sizeof(tag_buf)) {
-								printk("SID tag length too long!\n");
+								AICWFDBG(LOGTRACE, "SID tag length too long!\n");
 								return -1;
 							}
 
@@ -1127,7 +1127,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 							       pOldTag->tag_data, old_tag_len);
 
 							if (skb_pull_and_merge(skb, (unsigned char *)pOldTag, TAG_HDR_LEN + old_tag_len) < 0) {
-								printk("call skb_pull_and_merge() failed in PADI/R packet!\n");
+								AICWFDBG(LOGERROR, "call skb_pull_and_merge() failed in PADI/R packet!\n");
 								return -1;
 							}
 							ph->length = htons(ntohs(ph->length) - TAG_HDR_LEN - old_tag_len);
@@ -1145,12 +1145,12 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 						if (__nat25_add_pppoe_tag(skb, tag) < 0)
 							return -1;
 
-						printk("NAT25: Insert PPPoE, forward %s packet\n",
+						AICWFDBG(LOGTRACE, "NAT25: Insert PPPoE, forward %s packet\n",
 							(ph->code == PADI_CODE ? "PADI" : "PADR"));
 					} else { /* not add relay tag */
 						if (vif->pppoe_connection_in_progress &&
 						    memcmp(skb->data + ETH_ALEN, vif->pppoe_addr, ETH_ALEN))	 {
-							printk("Discard PPPoE packet due to another PPPoE connection is in progress!\n");
+							AICWFDBG(LOGTRACE, "Discard PPPoE packet due to another PPPoE connection is in progress!\n");
 							return -2;
 						}
 
@@ -1162,7 +1162,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 				} else
 					return -1;
 			} else {	/* session phase */
-				printk("NAT25: Insert PPPoE, insert session packet to %s\n", skb->dev->name);
+				AICWFDBG(LOGTRACE, "NAT25: Insert PPPoE, insert session packet to %s\n", skb->dev->name);
 
 				__nat25_generate_pppoe_network_addr(networkAddr, skb->data, &(ph->sid));
 
@@ -1187,7 +1187,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 
 					ptr = __nat25_find_pppoe_tag(ph, ntohs(PTT_RELAY_SID));
 					if (ptr == 0) {
-						printk("Fail to find PTT_RELAY_SID in FADO!\n");
+						AICWFDBG(LOGERROR, "Fail to find PTT_RELAY_SID in FADO!\n");
 						return -1;
 					}
 
@@ -1196,13 +1196,13 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 					tagLen = (unsigned short)((ptr[2] << 8) + ptr[3]);
 
 					if ((tagType != ntohs(PTT_RELAY_SID)) || (tagLen < (MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN))) {
-						printk("Invalid PTT_RELAY_SID tag length [%d]!\n", tagLen);
+						AICWFDBG(LOGERROR, "Invalid PTT_RELAY_SID tag length [%d]!\n", tagLen);
 						return -1;
 					}
 
 					pMagic = (unsigned short *)tag->tag_data;
 					if (ntohs(*pMagic) != MAGIC_CODE) {
-						printk("Can't find MAGIC_CODE in %s packet!\n",
+						AICWFDBG(LOGTRACE, "Can't find MAGIC_CODE in %s packet!\n",
 							(ph->code == PADO_CODE ? "PADO" : "PADS"));
 						return -1;
 					}
@@ -1213,18 +1213,18 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 						offset = TAG_HDR_LEN;
 
 					if (skb_pull_and_merge(skb, ptr + offset, TAG_HDR_LEN + MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN - offset) < 0) {
-						printk("call skb_pull_and_merge() failed in PADO packet!\n");
+						AICWFDBG(LOGERROR, "call skb_pull_and_merge() failed in PADO packet!\n");
 						return -1;
 					}
 					ph->length = htons(ntohs(ph->length) - (TAG_HDR_LEN + MAGIC_CODE_LEN + RTL_RELAY_TAG_LEN - offset));
 					if (offset > 0)
 						tag->tag_len = htons(tagLen - MAGIC_CODE_LEN - RTL_RELAY_TAG_LEN);
 
-					printk("NAT25: Lookup PPPoE, forward %s Packet from %s\n",
+					AICWFDBG(LOGTRACE, "NAT25: Lookup PPPoE, forward %s Packet from %s\n",
 						(ph->code == PADO_CODE ? "PADO" : "PADS"),	skb->dev->name);
 				} else { /* not add relay tag */
 					if (!vif->pppoe_connection_in_progress) {
-						printk("Discard PPPoE packet due to no connection in progresss!\n");
+						AICWFDBG(LOGTRACE, "Discard PPPoE packet due to no connection in progresss!\n");
 						return -1;
 					}
 					memcpy(skb->data, vif->pppoe_addr, ETH_ALEN);
@@ -1232,7 +1232,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 				}
 			} else {
 				if (ph->sid != 0) {
-					printk("NAT25: Lookup PPPoE, lookup session packet from %s\n", skb->dev->name);
+					AICWFDBG(LOGTRACE, "NAT25: Lookup PPPoE, lookup session packet from %s\n", skb->dev->name);
 					__nat25_generate_pppoe_network_addr(networkAddr, skb->data + ETH_ALEN, &(ph->sid));
 
 					__nat25_db_network_lookup_and_replace(vif, skb, networkAddr);
@@ -1296,7 +1296,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		struct ipv6hdr *iph = (struct ipv6hdr *)(skb->data + ETH_HLEN);
 
 		if (sizeof(*iph) >= (skb->len - ETH_HLEN)) {
-			printk("NAT25: malformed IPv6 packet !\n");
+			AICWFDBG(LOGTRACE, "NAT25: malformed IPv6 packet !\n");
 			return -1;
 		}
 
@@ -1307,7 +1307,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 			return -1;
 
 		case NAT25_INSERT: {
-			printk("NAT25: Insert IP, SA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x,"
+			AICWFDBG(LOGTRACE, "NAT25: Insert IP, SA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x,"
 				" DA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x\n",
 				iph->saddr.s6_addr16[0], iph->saddr.s6_addr16[1], iph->saddr.s6_addr16[2], iph->saddr.s6_addr16[3],
 				iph->saddr.s6_addr16[4], iph->saddr.s6_addr16[5], iph->saddr.s6_addr16[6], iph->saddr.s6_addr16[7],
@@ -1336,7 +1336,7 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 		return 0;
 
 		case NAT25_LOOKUP:
-			printk("NAT25: Lookup IP, SA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x,"
+			AICWFDBG(LOGTRACE, "NAT25: Lookup IP, SA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x,"
 				 " DA=%4x:%4x:%4x:%4x:%4x:%4x:%4x:%4x\n",
 				iph->saddr.s6_addr16[0], iph->saddr.s6_addr16[1], iph->saddr.s6_addr16[2], iph->saddr.s6_addr16[3],
 				iph->saddr.s6_addr16[4], iph->saddr.s6_addr16[5], iph->saddr.s6_addr16[6], iph->saddr.s6_addr16[7],
@@ -1365,10 +1365,10 @@ int nat25_db_handle(struct rwnx_vif *vif, struct sk_buff *skb, int method)
 
 int nat25_handle_frame(struct rwnx_vif *vif, struct sk_buff *skb)
 {
-	//printk("%s : vif_type=%d \n",__func__,RWNX_VIF_TYPE(vif));
+	//AICWFDBG(LOGTRACE, "%s : vif_type=%d \n",__func__,RWNX_VIF_TYPE(vif));
 #ifdef BR_SUPPORT_DEBUG
 	if ((!vif->ethBrExtInfo.nat25_disable) && (!(skb->data[0] & 1))) {
-		printk("NAT25: Input Frame: DA=%02x%02x%02x%02x%02x%02x SA=%02x%02x%02x%02x%02x%02x\n",
+		AICWFDBG(LOGTRACE, "NAT25: Input Frame: DA=%02x%02x%02x%02x%02x%02x SA=%02x%02x%02x%02x%02x%02x\n",
 			     skb->data[0],
 			     skb->data[1],
 			     skb->data[2],
@@ -1502,7 +1502,7 @@ void dhcp_flag_bcast(struct rwnx_vif *vif, struct sk_buff *skb)
 	//print_hex_dump(KERN_ERR, "SKB DUMP: SKB->DATA== ", DUMP_PREFIX_NONE, 32, 1, skb->data, 64,false);
 	if (!vif->ethBrExtInfo.dhcp_bcst_disable) {
 		unsigned short protocol = *((unsigned short *)(skb->data + 2 * ETH_ALEN));
-		printk("%s  protocol: %04x\n", __func__, protocol);
+		AICWFDBG(LOGTRACE, "%s  protocol: %04x\n", __func__, protocol);
 
 		if (protocol == __constant_htons(ETH_P_IP)) { /* IP */
 			struct iphdr *iph = (struct iphdr *)(skb->data + ETH_HLEN);
@@ -1519,7 +1519,7 @@ void dhcp_flag_bcast(struct rwnx_vif *vif, struct sk_buff *skb)
 						if (!(dhcph->flags & htons(BROADCAST_FLAG))) { /* if not broadcast */
 							register int sum = 0;
 
-							printk("DHCP: change flag of DHCP request to broadcast.\n");
+							AICWFDBG(LOGTRACE, "DHCP: change flag of DHCP request to broadcast.\n");
 					
 						#if 1
 							/* or BROADCAST flag */
@@ -1543,7 +1543,7 @@ void dhcp_flag_bcast(struct rwnx_vif *vif, struct sk_buff *skb)
 void *scdb_findEntry(struct rwnx_vif *vif, unsigned char *macAddr,
 		     unsigned char *ipAddr)
 {
-	printk("%s()\n",__func__);
+	AICWFDBG(LOGTRACE, "%s()\n",__func__);
 	unsigned char networkAddr[MAX_NETWORK_ADDR_LEN];
 	struct nat25_network_db_entry *db;
 	int hash;
diff --git a/aic8800_fdrv/aicwf_compat_8800dc.c b/aic8800_fdrv/aicwf_compat_8800dc.c
index 9ede721..5049440 100644
--- a/aic8800_fdrv/aicwf_compat_8800dc.c
+++ b/aic8800_fdrv/aicwf_compat_8800dc.c
@@ -1725,7 +1725,7 @@ int aicwf_patch_table_load(struct rwnx_hw *rwnx_hw, char *filename)
 	u32 fmacfw_patch_tbl_8800dc_u02_describe_base;//read from patch_tbl
 
     /* Copy the file on the Embedded side */
-    AICWFDBG(LOGINFO, "### Upload %s \n", filename);
+    AICWFDBG(LOGTRACE, "### Upload %s \n", filename);
 
     size = rwnx_request_firmware_common(rwnx_hw, &dst, filename);
     if (!dst) {
@@ -1738,20 +1738,20 @@ int aicwf_patch_table_load(struct rwnx_hw *rwnx_hw, char *filename)
             err = -1;
     }
 
-	AICWFDBG(LOGINFO, "tbl size = %d \n",size);
+	AICWFDBG(LOGTRACE, "tbl size = %d \n",size);
 
 	fmacfw_patch_tbl_8800dc_u02_describe_base = dst[0];
-	AICWFDBG(LOGINFO, "FMACFW_PATCH_TBL_8800DC_U02_DESCRIBE_BASE = %x \n",fmacfw_patch_tbl_8800dc_u02_describe_base);
+	AICWFDBG(LOGTRACE, "FMACFW_PATCH_TBL_8800DC_U02_DESCRIBE_BASE = %x \n",fmacfw_patch_tbl_8800dc_u02_describe_base);
 
 	if (!err && (i < size)) {
 		err = rwnx_send_dbg_mem_block_write_req(rwnx_hw, fmacfw_patch_tbl_8800dc_u02_describe_base, fmacfw_patch_tbl_8800dc_u02_describe_size + 4, dst);
 		if(err){
-			printk("write describe information fail \n");
+			AICWFDBG(LOGERROR, "write describe information fail \n");
 		}
 
 		describle = kzalloc(fmacfw_patch_tbl_8800dc_u02_describe_size, GFP_KERNEL);
 		memcpy(describle, &dst[1], fmacfw_patch_tbl_8800dc_u02_describe_size);
-		AICWFDBG(LOGINFO, "%s", describle);
+		AICWFDBG(LOGTRACE, "%s", describle);
 		kfree(describle);
 		describle = NULL;
 	}
@@ -1837,10 +1837,10 @@ void aicwf_patch_config_8800dc(struct rwnx_hw *rwnx_hw)
         }
         txgain_cfg_addr = cfm.memdata;
 
-       AICWFDBG(LOGINFO, "wifisetting_cfg_addr=%x, ldpc_cfg_addr=%x, agc_cfg_addr=%x, txgain_cfg_addr=%x\n", wifisetting_cfg_addr, ldpc_cfg_addr, agc_cfg_addr, txgain_cfg_addr);
+       AICWFDBG(LOGTRACE, "wifisetting_cfg_addr=%x, ldpc_cfg_addr=%x, agc_cfg_addr=%x, txgain_cfg_addr=%x\n", wifisetting_cfg_addr, ldpc_cfg_addr, agc_cfg_addr, txgain_cfg_addr);
 
         for (cnt = 0; cnt < patch_tbl_wifisetting_num; cnt++) {
-            printk("patch %x = %x\n", wifisetting_cfg_addr + patch_tbl_wifisetting[cnt][0],
+            AICWFDBG(LOGTRACE, "patch %x = %x\n", wifisetting_cfg_addr + patch_tbl_wifisetting[cnt][0],
                 patch_tbl_wifisetting[cnt][1]);
             if ((ret = rwnx_send_dbg_mem_write_req(rwnx_hw, wifisetting_cfg_addr + patch_tbl_wifisetting[cnt][0], patch_tbl_wifisetting[cnt][1]))) {
                 AICWFDBG(LOGERROR, "wifisetting %x write fail\n", patch_tbl_wifisetting[cnt][0]);
@@ -1903,7 +1903,7 @@ void aicwf_patch_config_8800dc(struct rwnx_hw *rwnx_hw)
 		else{
 			ret = aicwf_patch_table_load(rwnx_hw, RWNX_MAC_PATCH_TABLE_8800DC_U02);
 			if(ret){
-				printk("patch_tbl upload fail: err:%d\r\n", ret);
+				AICWFDBG(LOGERROR, "patch_tbl upload fail: err:%d\r\n", ret);
 			}
 #ifdef CONFIG_FOR_IPCAM
             if ((ret = rwnx_send_dbg_mem_write_req(rwnx_hw, 0x00111944, 0x00000101))) {
@@ -1975,7 +1975,7 @@ int aicwf_plat_patch_load_8800dc(struct rwnx_hw *rwnx_hw){
         } else if (chip_sub_id == 1) {
             ret = rwnx_plat_bin_fw_upload_2(rwnx_hw, ROM_FMAC_PATCH_ADDR, RWNX_MAC_PATCH_NAME2_8800DC_U02);
         } else {
-            printk("unsupported id: %d\n", chip_sub_id);
+            AICWFDBG(LOGERROR, "unsupported id: %d\n", chip_sub_id);
         }
 #endif
     } else {
@@ -2009,7 +2009,7 @@ int	rwnx_plat_userconfig_load_8800dc(struct rwnx_hw *rwnx_hw){
 	}
     }
 
-    AICWFDBG(LOGINFO, "userconfig file path:%s \r\n", filename);
+    AICWFDBG(LOGTRACE, "userconfig file path:%s \r\n", filename);
 
     /* load file */
     size = rwnx_request_firmware_common(rwnx_hw, &dst, filename);
@@ -2020,13 +2020,13 @@ int	rwnx_plat_userconfig_load_8800dc(struct rwnx_hw *rwnx_hw){
     }
 
 	/* Copy the file on the Embedded side */
-    AICWFDBG(LOGINFO, "### Load file done: %s, size=%d\n", filename, size);
+    AICWFDBG(LOGTRACE, "### Load file done: %s, size=%d\n", filename, size);
 
 	rwnx_plat_userconfig_parsing((char *)dst, size);
 
     rwnx_release_firmware_common(&dst);
 
-    AICWFDBG(LOGINFO, "userconfig download complete\n\n");
+    AICWFDBG(LOGTRACE, "userconfig download complete\n\n");
     return 0;
 
 }
@@ -2061,7 +2061,7 @@ void rwnx_get_countrycode(struct rwnx_hw *rwnx_hw)
     if (cc != 0xffff) {
 	memcpy(&rwnx_hw->usbdev->country_code[0], &cc, sizeof(cc));
     }
-    printk("cc is %x\n", cc);
+    AICWFDBG(LOGTRACE, "cc is %x\n", cc);
 
 }
 
@@ -2109,13 +2109,13 @@ void system_config_8800dc(struct rwnx_hw *rwnx_hw){
     }
     chip_sub_id = (u8)(rd_mem_addr_cfm.memdata);
     //printk("%x=%x\n", rd_mem_addr_cfm.memaddr, rd_mem_addr_cfm.memdata);
-	AICWFDBG(LOGINFO, "chip_id=%x, chip_sub_id=%x\n", chip_id, chip_sub_id);
+	AICWFDBG(LOGTRACE, "chip_id=%x, chip_sub_id=%x\n", chip_id, chip_sub_id);
 
 
 	ret = rwnx_send_dbg_mem_read_req(rwnx_hw, 0x40500010, &rd_mem_addr_cfm);
-	printk("[0x40500010]=%x\n", rd_mem_addr_cfm.memdata);
+	AICWFDBG(LOGTRACE, "[0x40500010]=%x\n", rd_mem_addr_cfm.memdata);
 	if (ret) {
-	    printk("[0x40500010] rd fail: %d\n", ret);
+	    AICWFDBG(LOGERROR, "[0x40500010] rd fail: %d\n", ret);
 	    return;
 	}
 
diff --git a/aic8800_fdrv/aicwf_sdio.c b/aic8800_fdrv/aicwf_sdio.c
index 1a96f45..6889831 100644
--- a/aic8800_fdrv/aicwf_sdio.c
+++ b/aic8800_fdrv/aicwf_sdio.c
@@ -288,7 +288,7 @@ int platform_wifi_power_on(void)
 	mdelay(1000);
 	sunxi_mmc_rescan_card(wlan_bus_index);
 
-	printk("platform_wifi_power_on");
+	AICWFDBG(LOGTRACE, "platform_wifi_power_on");
 
 	return ret;
 }
@@ -297,15 +297,15 @@ void platform_wifi_power_off(void)
 {
 	int wlan_bus_index = sunxi_wlan_get_bus_index();
     if(wlan_bus_index < 0) {
-		printk("no wlan_bus_index\n");
+		AICWFDBG(LOGERROR, "no wlan_bus_index\n");
 		return ;
 	}
-	printk("power_off\n");
+	AICWFDBG(LOGTRACE, "power_off\n");
 	sunxi_wlan_set_power(0);
     mdelay(100);
     //sunxi_mmc_rescan_card(wlan_bus_index);
 
-    printk("platform_wifi_power_off");
+    AICWFDBG(LOGTRACE, "platform_wifi_power_off");
 }
 #endif
 void aicwf_sdio_register(void)
@@ -325,7 +325,7 @@ void aicwf_sdio_register(void)
 #ifdef CONFIG_NANOPI_M4
     if(aic_host_drv->card == NULL){
         __mmc_claim_host(aic_host_drv,NULL);
-        printk("aic: >>>mmc_rescan_try_freq\n");
+        AICWFDBG(LOGTRACE, "aic: >>>mmc_rescan_try_freq\n");
         mmc_rescan_try_freq(aic_host_drv,aic_max_freqs);
         mmc_release_host(aic_host_drv);
     }
diff --git a/aic8800_fdrv/aicwf_txrxif.c b/aic8800_fdrv/aicwf_txrxif.c
index 4af8d71..5d3a1a2 100644
--- a/aic8800_fdrv/aicwf_txrxif.c
+++ b/aic8800_fdrv/aicwf_txrxif.c
@@ -127,14 +127,14 @@ int aicwf_bus_init(uint bus_hdrlen, struct device *dev)
 #endif
 
 #ifdef CONFIG_RX_TASKLET//AIDEN tasklet
-	AICWFDBG(LOGINFO, "%s use tasklet for rx \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s use tasklet for rx \r\n", __func__);
 	tasklet_init(&((bus_if->bus_priv.usb)->recv_tasklet),
 		(void(*)(unsigned long))aicwf_tasklet_rxframes,
 		(unsigned long)(bus_if->bus_priv.usb)->rx_priv);
 #endif
 
 #ifdef CONFIG_TX_TASKLET//AIDEN tasklet
-	AICWFDBG(LOGINFO, "%s use tasklet for tx \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s use tasklet for tx \r\n", __func__);
 	tasklet_init(&((bus_if->bus_priv.usb)->xmit_tasklet),
 		(void(*)(unsigned long))aicwf_tasklet_tx_process,
 		(unsigned long)(bus_if->bus_priv.usb));
@@ -189,7 +189,7 @@ void aicwf_bus_deinit(struct device *dev)
         txrx_err("device not found\n");
         return;
     }
-	AICWFDBG(LOGINFO, "%s Enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s Enter\r\n", __func__);
 
     bus_if = dev_get_drvdata(dev);
     aicwf_bus_stop(bus_if);
@@ -201,7 +201,7 @@ void aicwf_bus_deinit(struct device *dev)
     if(g_rwnx_plat && g_rwnx_plat->enabled){
         rwnx_platform_deinit(usb->rwnx_hw);
     }else{
-    	AICWFDBG(LOGINFO, "%s g_rwnx_plat->enabled is false\r\n", __func__);
+    	AICWFDBG(LOGTRACE, "%s g_rwnx_plat->enabled is false\r\n", __func__);
 	}
     	
 #endif
@@ -225,7 +225,7 @@ void aicwf_bus_deinit(struct device *dev)
     }
 
     if (bus_if->bustx_thread) {
-		AICWFDBG(LOGINFO, "%s stop to bustx_thread!! \r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s stop to bustx_thread!! \r\n", __func__);
 
 
 		complete_all(&bus_if->bustx_trgg);
@@ -237,7 +237,7 @@ void aicwf_bus_deinit(struct device *dev)
 		tasklet_kill(&usb->xmit_tasklet);
 #endif
 
-	AICWFDBG(LOGINFO, "%s Exit \n", __func__);
+	AICWFDBG(LOGTRACE, "%s Exit \n", __func__);
 }
 
 void aicwf_frame_tx(void *dev, struct sk_buff *skb)
@@ -882,7 +882,7 @@ void aicwf_rx_deinit(struct aicwf_rx_priv* rx_priv)
 #ifdef AICWF_RX_REORDER
     struct reord_ctrl_info *reord_info, *tmp;
 
-	AICWFDBG(LOGINFO, "%s Enter\n", __func__);
+	AICWFDBG(LOGTRACE, "%s Enter\n", __func__);
 	
     list_for_each_entry_safe(reord_info, tmp,
         &rx_priv->stas_reord_list, list) {
@@ -890,7 +890,7 @@ void aicwf_rx_deinit(struct aicwf_rx_priv* rx_priv)
     }
 
 #endif
-	AICWFDBG(LOGINFO, "stio rx thread\n");
+	AICWFDBG(LOGTRACE, "stio rx thread\n");
 #ifdef AICWF_SDIO_SUPPORT
     if (rx_priv->sdiodev->bus_if->busrx_thread) {
         complete_all(&rx_priv->sdiodev->bus_if->busrx_trgg);
@@ -942,7 +942,7 @@ void aicwf_rx_deinit(struct aicwf_rx_priv* rx_priv)
 	//aicwf_prealloc_exit();
 #endif
     kfree(rx_priv);
-	AICWFDBG(LOGINFO, "%s Exit\n", __func__);
+	AICWFDBG(LOGTRACE, "%s Exit\n", __func__);
 
 }
 
@@ -1130,12 +1130,12 @@ struct rx_buff *rxbuff_dequeue(struct rx_frame_queue *pq)
     struct rx_buff *p = NULL;
 
     if (pq->qcnt == 0) {
-        printk("%s %d, rxq is empty\n", __func__, __LINE__);
+        AICWFDBG(LOGERROR, "%s %d, rxq is empty\n", __func__, __LINE__);
         return NULL;
     }
 
     if(list_empty(&pq->queuelist)) {
-        printk("%s %d, rxq is empty\n", __func__, __LINE__);
+        AICWFDBG(LOGERROR, "%s %d, rxq is empty\n", __func__, __LINE__);
         return NULL;
     } else {
         p = list_first_entry(&pq->queuelist, struct rx_buff, queue);
@@ -1151,7 +1151,7 @@ bool aicwf_rxbuff_enqueue(struct device *dev, struct rx_frame_queue *rxq, struct
 //    struct rx_buff *p = NULL;
 
     if ((rxq == NULL) || (pkt == NULL)) {
-        printk("%s %d, rxq or pkt is NULL\n", __func__, __LINE__);
+        AICWFDBG(LOGERROR, "%s %d, rxq or pkt is NULL\n", __func__, __LINE__);
         return false;
     }
 
@@ -1159,11 +1159,11 @@ bool aicwf_rxbuff_enqueue(struct device *dev, struct rx_frame_queue *rxq, struct
         if (rxbuff_queue_penq(rxq, pkt)) {
             return true;
         } else {
-            printk("%s %d, rxbuff enqueue fail\n", __func__, __LINE__);
+            AICWFDBG(LOGERROR, "%s %d, rxbuff enqueue fail\n", __func__, __LINE__);
             return false;
         }
     } else {
-        printk("%s %d, rxq or pkt is full\n", __func__, __LINE__);
+        AICWFDBG(LOGERROR, "%s %d, rxq or pkt is full\n", __func__, __LINE__);
         return false;
     }
 }
diff --git a/aic8800_fdrv/aicwf_usb.c b/aic8800_fdrv/aicwf_usb.c
index 4978387..b4402da 100644
--- a/aic8800_fdrv/aicwf_usb.c
+++ b/aic8800_fdrv/aicwf_usb.c
@@ -290,13 +290,13 @@ static void aicwf_usb_rx_complete(struct urb *urb)
                     if(atomic_read(&aicwf_deinit_atomic) > 0){
                         atomic_set(&aicwf_deinit_atomic, 0);
                         down(&aicwf_deinit_sem);
-                        AICWFDBG(LOGINFO, "%s need to wait for disconnect callback \r\n", __func__);
-                    }else{
-                        g_rwnx_plat->wait_disconnect_cb = false;
-                    }
-                }
+                        AICWFDBG(LOGTRACE, "%s need to wait for disconnect callback \r\n", __func__);
+				}else{
+					g_rwnx_plat->wait_disconnect_cb = false;
+				}
+			}
     
-                return;
+			return;
             }else{
                 //schedule_work(&usb_dev->rx_urb_work);
                 aicwf_usb_rx_submit_all_urb_(usb_dev);
@@ -373,7 +373,7 @@ static void aicwf_usb_rx_complete(struct urb *urb)
 				if(atomic_read(&aicwf_deinit_atomic) > 0){
 					atomic_set(&aicwf_deinit_atomic, 0);
 					down(&aicwf_deinit_sem);
-					AICWFDBG(LOGINFO, "%s need to wait for disconnect callback \r\n", __func__);
+					AICWFDBG(LOGTRACE, "%s need to wait for disconnect callback \r\n", __func__);
 				}else{
 					g_rwnx_plat->wait_disconnect_cb = false;
 				}
@@ -388,7 +388,7 @@ static void aicwf_usb_rx_complete(struct urb *urb)
     }
     #ifdef CONFIG_USB_RX_AGGR
     if (urb->actual_length > 1600 * 30) {
-        printk("r%d\n", urb->actual_length);
+        AICWFDBG(LOGTRACE, "r%d\n", urb->actual_length);
     }
     #endif
 
@@ -987,9 +987,9 @@ int usb_bustx_thread(void *data)
 			sched_setscheduler(current, SCHED_FIFO, &param);
 	}
 #endif
-	AICWFDBG(LOGINFO, "%s the policy of current thread is:%d\n", __func__, current->policy);
-	AICWFDBG(LOGINFO, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
-	AICWFDBG(LOGINFO, "%s the current pid is:%d\n", __func__, current->pid);
+	AICWFDBG(LOGTRACE, "%s the policy of current thread is:%d\n", __func__, current->policy);
+	AICWFDBG(LOGTRACE, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
+	AICWFDBG(LOGTRACE, "%s the current pid is:%d\n", __func__, current->pid);
 
 
     while (1) {
@@ -1001,7 +1001,7 @@ int usb_bustx_thread(void *data)
 		#endif
         if (!wait_for_completion_interruptible(&bus->bustx_trgg)) {
             if(usbdev->bus_if->state == BUS_DOWN_ST){
-				AICWFDBG(LOGINFO, "usb bustx thread will to stop\n");
+				AICWFDBG(LOGTRACE, "usb bustx thread will to stop\n");
                 break;
 			}
             #ifdef CONFIG_USB_TX_AGGR
@@ -1014,7 +1014,7 @@ int usb_bustx_thread(void *data)
     }
 
 	aic_thread_wait_stop();
-	AICWFDBG(LOGINFO, "usb bustx thread stop\n");
+	AICWFDBG(LOGTRACE, "usb bustx thread stop\n");
 
     return 0;
 }
@@ -1031,9 +1031,9 @@ int usb_busrx_thread(void *data)
 			sched_setscheduler(current, SCHED_FIFO, &param);
 	}
 #endif
-	AICWFDBG(LOGINFO, "%s the policy of current thread is:%d\n", __func__, current->policy);
-	AICWFDBG(LOGINFO, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
-	AICWFDBG(LOGINFO, "%s the current pid is:%d\n", __func__, current->pid);
+	AICWFDBG(LOGTRACE, "%s the policy of current thread is:%d\n", __func__, current->policy);
+	AICWFDBG(LOGTRACE, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
+	AICWFDBG(LOGTRACE, "%s the current pid is:%d\n", __func__, current->pid);
 
     while (1) {
 #if 0
@@ -1045,7 +1045,7 @@ int usb_busrx_thread(void *data)
 		//rx_priv->rx_thread_working = 0;//AIDEN
         if (!wait_for_completion_interruptible(&bus_if->busrx_trgg)) {
             if(bus_if->state == BUS_DOWN_ST){
-				AICWFDBG(LOGINFO, "usb busrx thread will to stop\n");
+				AICWFDBG(LOGTRACE, "usb busrx thread will to stop\n");
 				break;
             }
 			//rx_priv->rx_thread_working = 1;//AIDEN
@@ -1054,7 +1054,7 @@ int usb_busrx_thread(void *data)
     }
 
 	aic_thread_wait_stop();
-	AICWFDBG(LOGINFO, "usb busrx thread stop\n");
+	AICWFDBG(LOGTRACE, "usb busrx thread stop\n");
 
     return 0;
 }
@@ -1072,9 +1072,9 @@ int usb_msg_busrx_thread(void *data)
 					sched_setscheduler(current, SCHED_FIFO, &param);
 			}
 #endif
-			AICWFDBG(LOGINFO, "%s the policy of current thread is:%d\n", __func__, current->policy);
-			AICWFDBG(LOGINFO, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
-			AICWFDBG(LOGINFO, "%s the current pid is:%d\n", __func__, current->pid);
+			AICWFDBG(LOGTRACE, "%s the policy of current thread is:%d\n", __func__, current->policy);
+			AICWFDBG(LOGTRACE, "%s the rt_priority of current thread is:%d\n", __func__, current->rt_priority);
+			AICWFDBG(LOGTRACE, "%s the current pid is:%d\n", __func__, current->pid);
 
 
 
@@ -1219,7 +1219,7 @@ static int aicwf_usb_alloc_rx_urb(struct aic_usb_dev *usb_dev)
 {
     int i;
 
-	AICWFDBG(LOGINFO, "%s AICWF_USB_RX_URBS:%d \r\n", __func__, AICWF_USB_RX_URBS);
+	AICWFDBG(LOGTRACE, "%s AICWF_USB_RX_URBS:%d \r\n", __func__, AICWF_USB_RX_URBS);
     for (i = 0; i < AICWF_USB_RX_URBS; i++) {
         struct aicwf_usb_buf *usb_buf = &usb_dev->usb_rx_buf[i];
 
@@ -1247,7 +1247,7 @@ static int aicwf_usb_alloc_tx_urb(struct aic_usb_dev *usb_dev)
 {
     int i;
 
-	AICWFDBG(LOGINFO, "%s AICWF_USB_TX_URBS:%d \r\n", __func__, AICWF_USB_TX_URBS);
+	AICWFDBG(LOGTRACE, "%s AICWF_USB_TX_URBS:%d \r\n", __func__, AICWF_USB_TX_URBS);
     for (i = 0; i < AICWF_USB_TX_URBS; i++) {
         struct aicwf_usb_buf *usb_buf = &usb_dev->usb_tx_buf[i];
 
@@ -1287,7 +1287,7 @@ static int aicwf_usb_alloc_msg_rx_urb(struct aic_usb_dev *usb_dev)
 {
     int i;
     
-    AICWFDBG(LOGINFO, "%s AICWF_USB_MSG_RX_URBS:%d \r\n", __func__, AICWF_USB_MSG_RX_URBS);
+    AICWFDBG(LOGTRACE, "%s AICWF_USB_MSG_RX_URBS:%d \r\n", __func__, AICWF_USB_MSG_RX_URBS);
 
     for (i = 0; i < AICWF_USB_MSG_RX_URBS; i++) {
         struct aicwf_usb_buf *usb_buf = &usb_dev->usb_msg_rx_buf[i];
@@ -1487,7 +1487,7 @@ static int aicwf_usb_bus_txdata(struct device *dev, struct sk_buff *skb)
 
 #ifndef CONFIG_USE_USB_ZERO_PACKET
 	if((buf_len % 512) == 0){
-		printk("%s send zero package buf_len: %d\r\n", __func__, buf_len);
+		AICWFDBG(LOGERROR, "%s send zero package buf_len: %d\r\n", __func__, buf_len);
 		if(txhdr->sw_hdr->need_cfm){
 			buf[buf_len] = 0x00;
 			buf_len = buf_len + 1;
@@ -1623,7 +1623,7 @@ static void aicwf_usb_bus_stop(struct device *dev)
     struct aicwf_bus *bus_if = dev_get_drvdata(dev);
     struct aic_usb_dev *usb_dev = bus_if->bus_priv.usb;
 
-	AICWFDBG(LOGINFO, "%s\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s\r\n", __func__);
     if (usb_dev == NULL)
         return;
 
@@ -1770,7 +1770,7 @@ static int aicwf_parse_usb(struct aic_usb_dev *usb_dev, struct usb_interface *in
     host_interface = &interface->altsetting[0];
     interface_desc = &host_interface->desc;
     endpoints = interface_desc->bNumEndpoints;
-	AICWFDBG(LOGINFO, "%s endpoints = %d\n", __func__, endpoints);
+	AICWFDBG(LOGTRACE, "%s endpoints = %d\n", __func__, endpoints);
 
     /* Check device configuration */
     if (usb->descriptor.bNumConfigurations != 1) {
@@ -1802,7 +1802,7 @@ static int aicwf_parse_usb(struct aic_usb_dev *usb_dev, struct usb_interface *in
 			AICWFDBG(LOGERROR, "AIC8800DC change to AIC8800DW\n");
 			usb_dev->chipid = PRODUCT_ID_AIC8800DW;
         }else if (usb_dev->chipid == PRODUCT_ID_AIC8800DW) {
-            printk("8800dw\n");
+            AICWFDBG(LOGTRACE, "8800dw\n");
 		}
     }
 
@@ -1873,9 +1873,9 @@ static int aicwf_parse_usb(struct aic_usb_dev *usb_dev, struct usb_interface *in
 #endif
 
     if (usb->speed == USB_SPEED_HIGH){
-		AICWFDBG(LOGINFO, "Aic high speed USB device detected\n");
+		AICWFDBG(LOGTRACE, "Aic high speed USB device detected\n");
     }else{
-    	AICWFDBG(LOGINFO, "Aic high speed USB device detected\n");
+    	AICWFDBG(LOGTRACE, "Aic high speed USB device detected\n");
     }
 
     exit:
@@ -1900,7 +1900,7 @@ static irqreturn_t rwnx_irq_handler(int irq, void *para)
     spin_lock_irqsave(&irq_lock, irqflags);
     disable_irq_nosync(hostwake_irq_num);
     //do something
-    printk("%s gpio irq trigger\r\n", __func__);
+    AICWFDBG(LOGTRACE, "%s gpio irq trigger\r\n", __func__);
     spin_unlock_irqrestore(&irq_lock, irqflags);
     atomic_dec(&irq_count);
 	return IRQ_HANDLED;
@@ -1918,9 +1918,9 @@ static int rwnx_register_hostwake_irq(struct device *dev)
 //For Rockchip
 #ifdef CONFIG_PLATFORM_ROCKCHIP
 	hostwake_irq_num = rockchip_wifi_get_oob_irq();
-	printk("%s hostwake_irq_num:%d \r\n", __func__, hostwake_irq_num);
+	AICWFDBG(LOGTRACE, "%s hostwake_irq_num:%d \r\n", __func__, hostwake_irq_num);
 	irq_flags = (IORESOURCE_IRQ | IORESOURCE_IRQ_HIGHLEVEL | IORESOURCE_IRQ_SHAREABLE) & IRQF_TRIGGER_MASK;
-	printk("%s irq_flags:%d \r\n", __func__, irq_flags);
+	AICWFDBG(LOGTRACE, "%s irq_flags:%d \r\n", __func__, irq_flags);
 	wakeup_enable = 1;
 #endif //CONFIG_PLATFORM_ROCKCHIP
 
@@ -1950,7 +1950,7 @@ static int rwnx_unregister_hostwake_irq(struct device *dev)
 {
 	wakeup_enable = 0;
 
-	printk("%s hostwake_irq_num:%d \r\n", __func__, hostwake_irq_num);
+	AICWFDBG(LOGTRACE, "%s hostwake_irq_num:%d \r\n", __func__, hostwake_irq_num);
 	disable_irq_wake(hostwake_irq_num);
 	free_irq(hostwake_irq_num, NULL);
 
@@ -1963,11 +1963,11 @@ static int aicwf_usb_chipmatch(struct aic_usb_dev *usb_dev, u16_l vid, u16_l pid
 
 	if(pid == USB_PRODUCT_ID_AIC8801){
 		usb_dev->chipid = PRODUCT_ID_AIC8801;
-		AICWFDBG(LOGINFO, "%s USE AIC8801\r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s USE AIC8801\r\n", __func__);
 		return 0;
 	}else if(pid == USB_PRODUCT_ID_AIC8800DC){
 		usb_dev->chipid = PRODUCT_ID_AIC8800DC;
-		AICWFDBG(LOGINFO, "%s USE AIC8800DC\r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s USE AIC8800DC\r\n", __func__);
 		return 0;
 	}else if(pid == USB_PRODUCT_ID_AIC8800DW || pid == USB_PRODUCT_ID_TP
 	 || pid == USB_PRODUCT_ID_AIC8800FC ||
@@ -1975,7 +1975,7 @@ static int aicwf_usb_chipmatch(struct aic_usb_dev *usb_dev, u16_l vid, u16_l pid
         || (vid == USB_VENDOR_ID_MERCUSYS && pid == USB_PRODUCT_ID_MERCUSYS)
         || (vid == USB_VENDOR_ID_MERCUSYS && pid == USB_PRODUCT_ID_MERCUSYS_MA14H)){
         usb_dev->chipid = PRODUCT_ID_AIC8800DW;
-		AICWFDBG(LOGINFO, "%s USE AIC8800DW\r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s USE AIC8800DW\r\n", __func__);
         return 0;
     }else{
 		return -1;
@@ -2099,7 +2099,7 @@ static void aicwf_usb_disconnect(struct usb_interface *intf)
 {
     struct aic_usb_dev *usb_dev =
             (struct aic_usb_dev *) usb_get_intfdata(intf);
-        AICWFDBG(LOGINFO, "%s Enter\r\n", __func__);
+        AICWFDBG(LOGTRACE, "%s Enter\r\n", __func__);
 
 	if(g_rwnx_plat->wait_disconnect_cb == false){
 		atomic_set(&aicwf_deinit_atomic, 0);
@@ -2113,7 +2113,7 @@ static void aicwf_usb_disconnect(struct usb_interface *intf)
 
 #if 0
 	if(timer_pending(&usb_dev->rwnx_hw->p2p_alive_timer) && usb_dev->rwnx_hw->is_p2p_alive == 1){
-		printk("%s del timer rwnx_hw->p2p_alive_timer \r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s del timer rwnx_hw->p2p_alive_timer \r\n", __func__);
 		rwnx_del_timer(&usb_dev->rwnx_hw->p2p_alive_timer);
 	}
 #endif
@@ -2130,7 +2130,7 @@ static void aicwf_usb_disconnect(struct usb_interface *intf)
 
     kfree(usb_dev->bus_if);
     kfree(usb_dev);
-	AICWFDBG(LOGINFO, "%s exit\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s exit\r\n", __func__);
 	up(&aicwf_deinit_sem);
 	atomic_set(&aicwf_deinit_atomic, 1);
 }
@@ -2144,7 +2144,7 @@ static int aicwf_usb_suspend(struct usb_interface *intf, pm_message_t state)
 	//unsigned long irqflags;
 #endif
 
-	printk("%s enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter\r\n", __func__);
 
 #ifdef CONFIG_GPIO_WAKEUP
 //	spin_lock_irqsave(&irq_lock, irqflags);
@@ -2173,7 +2173,7 @@ static int aicwf_usb_resume(struct usb_interface *intf)
 	struct rwnx_vif *rwnx_vif, *tmp;
 //	unsigned long irqflags;
 #endif
-	printk("%s enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter\r\n", __func__);
 
 #ifdef CONFIG_GPIO_WAKEUP
 //	spin_lock_irqsave(&irq_lock, irqflags);
@@ -2257,10 +2257,10 @@ void aicwf_usb_exit(void)
 		g_rwnx_plat->wait_disconnect_cb = false;
 	}
 	
-	AICWFDBG(LOGINFO, "%s Enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s Enter\r\n", __func__);
 
 	if(!g_rwnx_plat || !g_rwnx_plat->enabled){
-		AICWFDBG(LOGINFO, "g_rwnx_plat is not ready. waiting for 500ms\r\n");
+		AICWFDBG(LOGTRACE, "g_rwnx_plat is not ready. waiting for 500ms\r\n");
 		mdelay(500);
 	}
 
@@ -2273,7 +2273,7 @@ void aicwf_usb_exit(void)
 	up(&aicwf_deinit_sem);
 	atomic_set(&aicwf_deinit_atomic, 1);
 
-	AICWFDBG(LOGINFO, "%s usb_deregister \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s usb_deregister \r\n", __func__);
 
     usb_deregister(&aicwf_usbdrvr);
 	//mdelay(500);
@@ -2281,6 +2281,6 @@ void aicwf_usb_exit(void)
     	kfree(g_rwnx_plat);
 	}
 	
-	AICWFDBG(LOGINFO, "%s exit\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s exit\r\n", __func__);
 
 }
diff --git a/aic8800_fdrv/aicwf_wext_linux.c b/aic8800_fdrv/aicwf_wext_linux.c
index f316f93..ba177c4 100644
--- a/aic8800_fdrv/aicwf_wext_linux.c
+++ b/aic8800_fdrv/aicwf_wext_linux.c
@@ -784,11 +784,11 @@ static inline char *aicwf_get_iwe_stream_wpa_wpa2(struct rwnx_hw* rwnx_hw,
 					p += sprintf(p, "%02x", wpa_ie[i]);
 
 				if (wpa_len > 100) {
-					printk("-----------------Len %d----------------\n", wpa_len);
+					AICWFDBG(LOGTRACE, "-----------------Len %d----------------\n", wpa_len);
 					for (i = 0; i < wpa_len; i++)
-						printk("%02x ", wpa_ie[i]);
-					printk("\n");
-					printk("-----------------Len %d----------------\n", wpa_len);
+						AICWFDBG(LOGTRACE, "%02x ", wpa_ie[i]);
+					AICWFDBG(LOGTRACE, "\n");
+					AICWFDBG(LOGTRACE, "-----------------Len %d----------------\n", wpa_len);
 				}
 
 				memset(iwe, 0, sizeof(*iwe));
@@ -935,7 +935,7 @@ static int aicwf_set_scan(struct net_device *dev, struct iw_request_info *a,
 	AICWFDBG(LOGTRACE, "%s Enter", __func__);
 
 	if(wiphy == NULL){
-		printk("aic_wiphy error \r\n");
+		AICWFDBG(LOGERROR, "aic_wiphy error \r\n");
 
 	}
 	
@@ -1183,7 +1183,7 @@ struct iw_handler_def aic_handlers_def = {
 
 void aicwf_set_wireless_ext( struct net_device *ndev, struct rwnx_hw *rwnx_hw){
 
-	AICWFDBG(LOGINFO, "%s Enter", __func__);
+	AICWFDBG(LOGTRACE, "%s Enter", __func__);
 	
 	init_completion(&rwnx_hw->wext_scan_com);
 	INIT_LIST_HEAD(&rwnx_hw->wext_scanre_list);
diff --git a/aic8800_fdrv/rwnx_cfgfile.c b/aic8800_fdrv/rwnx_cfgfile.c
index 2af5c30..ba64c50 100644
--- a/aic8800_fdrv/rwnx_cfgfile.c
+++ b/aic8800_fdrv/rwnx_cfgfile.c
@@ -60,7 +60,7 @@ int rwnx_parse_configfile(struct rwnx_hw *rwnx_hw, const char *filename,
     RWNX_DBG(RWNX_FN_ENTRY_STR);
 
     if ((ret = request_firmware(&config_fw, filename, rwnx_hw->dev))) {
-        printk(KERN_CRIT "%s: Failed to get %s (%d)\n", __func__, filename, ret);
+        AICWFDBG(LOGERROR, "%s: Failed to get %s (%d)\n", __func__, filename, ret);
         return ret;
     }
 
@@ -98,7 +98,7 @@ int rwnx_parse_phy_configfile(struct rwnx_hw *rwnx_hw, const char *filename,
     RWNX_DBG(RWNX_FN_ENTRY_STR);
 
     if ((ret = request_firmware(&config_fw, filename, rwnx_hw->dev))) {
-        printk(KERN_CRIT "%s: Failed to get %s (%d)\n", __func__, filename, ret);
+        AICWFDBG(LOGERROR, "%s: Failed to get %s (%d)\n", __func__, filename, ret);
         return ret;
     }
 
diff --git a/aic8800_fdrv/rwnx_cmds.c b/aic8800_fdrv/rwnx_cmds.c
index 28d7ee1..f012d14 100644
--- a/aic8800_fdrv/rwnx_cmds.c
+++ b/aic8800_fdrv/rwnx_cmds.c
@@ -33,7 +33,7 @@ void rwnx_cmd_free(struct rwnx_cmd *cmd);
 
 static void cmd_dump(const struct rwnx_cmd *cmd)
 {
-    printk(KERN_CRIT "tkn[%d]  flags:%04x  result:%3d  cmd:%4d-%-24s - reqcfm(%4d-%-s)\n",
+    AICWFDBG(LOGTRACE, "tkn[%d]  flags:%04x  result:%3d  cmd:%4d-%-24s - reqcfm(%4d-%-s)\n",
            cmd->tkn, cmd->flags, cmd->result, cmd->id, RWNX_ID2STR(cmd->id),
            cmd->reqid, cmd->reqid != (lmac_msg_id_t)-1 ? RWNX_ID2STR(cmd->reqid) : "none");
 }
@@ -71,7 +71,7 @@ int cmd_mgr_queue_force_defer(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd
     spin_lock_bh(&cmd_mgr->lock);
 
     if (cmd_mgr->state == RWNX_CMD_MGR_STATE_CRASHED) {
-        printk(KERN_CRIT"cmd queue crashed\n");
+        AICWFDBG(LOGERROR, "cmd queue crashed\n");
         cmd->result = -EPIPE;
         spin_unlock_bh(&cmd_mgr->lock);
         return -EPIPE;
@@ -80,7 +80,7 @@ int cmd_mgr_queue_force_defer(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd
     #ifndef CONFIG_RWNX_FHOST
     if (!list_empty(&cmd_mgr->cmds)) {
         if (cmd_mgr->queue_sz == cmd_mgr->max_queue_sz) {
-            printk(KERN_CRIT"Too many cmds (%d) already queued\n",
+            AICWFDBG(LOGERROR, "Too many cmds (%d) already queued\n",
                    cmd_mgr->max_queue_sz);
             cmd->result = -ENOMEM;
             spin_unlock_bh(&cmd_mgr->lock);
@@ -131,7 +131,7 @@ static int cmd_mgr_queue(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd)
     spin_lock_bh(&cmd_mgr->lock);
 
     if (cmd_mgr->state == RWNX_CMD_MGR_STATE_CRASHED) {
-        printk(KERN_CRIT"cmd queue crashed\n");
+        AICWFDBG(LOGERROR, "cmd queue crashed\n");
         cmd->result = -EPIPE;
         spin_unlock_bh(&cmd_mgr->lock);
         return -EPIPE;
@@ -142,7 +142,7 @@ static int cmd_mgr_queue(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd)
         struct rwnx_cmd *last;
 
         if (cmd_mgr->queue_sz == cmd_mgr->max_queue_sz) {
-            printk(KERN_CRIT"Too many cmds (%d) already queued\n",
+            AICWFDBG(LOGERROR, "Too many cmds (%d) already queued\n",
                    cmd_mgr->max_queue_sz);
             cmd->result = -ENOMEM;
             spin_unlock_bh(&cmd_mgr->lock);
@@ -152,7 +152,7 @@ static int cmd_mgr_queue(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd)
         if (last->flags & (RWNX_CMD_FLAG_WAIT_ACK | RWNX_CMD_FLAG_WAIT_PUSH | RWNX_CMD_FLAG_WAIT_CFM)) {
 #if 0 // queue even NONBLOCK command.
             if (cmd->flags & RWNX_CMD_FLAG_NONBLOCK) {
-                printk(KERN_CRIT"cmd queue busy\n");
+                AICWFDBG(LOGERROR, "cmd queue busy\n");
                 cmd->result = -EBUSY;
                 spin_unlock_bh(&cmd_mgr->lock);
                 return -EBUSY;
@@ -218,7 +218,7 @@ static int cmd_mgr_queue(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd)
         #else
         unsigned long tout = msecs_to_jiffies(RWNX_80211_CMD_TIMEOUT_MS/*AIDEN workaround* cmd_mgr->queue_sz*/);
         if (!wait_for_completion_killable_timeout(&cmd->complete, tout)) {
-            printk(KERN_CRIT"%s cmd timed-out cmd_mgr->queue_sz:%d\n", __func__,cmd_mgr->queue_sz);
+            AICWFDBG(LOGERROR, "%s cmd timed-out cmd_mgr->queue_sz:%d\n", __func__,cmd_mgr->queue_sz);
         #ifdef AICWF_SDIO_SUPPORT
             ret = aicwf_sdio_writeb(sdiodev, SDIOWIFI_WAKEUP_REG, 2);
             if (ret < 0) {
@@ -275,7 +275,7 @@ static int cmd_mgr_llind(struct rwnx_cmd_mgr *cmd_mgr, struct rwnx_cmd *cmd)
         }
     }
     if (!acked) {
-        printk(KERN_CRIT "Error: acked cmd not found\n");
+        AICWFDBG(LOGERROR, "Error: acked cmd not found\n");
     } else {
         cmd->flags &= ~RWNX_CMD_FLAG_WAIT_ACK;
         if (RWNX_CMD_WAIT_COMPLETE(cmd->flags))
@@ -338,7 +338,7 @@ void cmd_mgr_task_process(struct work_struct *work)
 
             tout = msecs_to_jiffies(RWNX_80211_CMD_TIMEOUT_MS * cmd_mgr->queue_sz);
             if (!wait_for_completion_killable_timeout(&next->complete, tout)) {
-                printk(KERN_CRIT"%s cmd timed-out cmd_mgr->queue_sz:%d\n", __func__, cmd_mgr->queue_sz);
+                AICWFDBG(LOGERROR, "%s cmd timed-out cmd_mgr->queue_sz:%d\n", __func__, cmd_mgr->queue_sz);
                 cmd_dump(next);
                 spin_lock_bh(&cmd_mgr->lock);
                 //AIDEN  workaround  
@@ -509,7 +509,7 @@ void aicwf_set_cmd_tx(void *dev, struct lmac_msg *msg, uint len)
 	struct aic_usb_dev *usbdev = (struct aic_usb_dev *)dev;
 	struct aicwf_bus *bus = NULL;
     if (!usbdev->state) {
-        printk("down msg \n");
+        AICWFDBG(LOGERROR, "down msg \n");
         return;
     }
 	bus = usbdev->bus_if;
diff --git a/aic8800_fdrv/rwnx_debugfs.c b/aic8800_fdrv/rwnx_debugfs.c
index 3e39c8a..207f8ed 100644
--- a/aic8800_fdrv/rwnx_debugfs.c
+++ b/aic8800_fdrv/rwnx_debugfs.c
@@ -797,7 +797,7 @@ static ssize_t rwnx_dbgfs_fw_log_read(struct file *file,
 	size_t nb_cpy;
 	char *log = fw_log_buffer;
 
-	printk("%s, %d, %p, %p\n", __func__, priv->debugfs.fw_log.buf.size, priv->debugfs.fw_log.buf.start, priv->debugfs.fw_log.buf.dataend);
+	AICWFDBG(LOGTRACE, "%s, %d, %p, %p\n", __func__, priv->debugfs.fw_log.buf.size, priv->debugfs.fw_log.buf.start, priv->debugfs.fw_log.buf.dataend);
 	//spin_lock_bh(&priv->debugfs.fw_log.lock);
 
 	if ((priv->debugfs.fw_log.buf.start + priv->debugfs.fw_log.buf.size) >= priv->debugfs.fw_log.buf.dataend) {
@@ -815,7 +815,7 @@ static ssize_t rwnx_dbgfs_fw_log_read(struct file *file,
 	priv->debugfs.fw_log.buf.size -= nb_cpy;
 	//spin_unlock_bh(&priv->debugfs.fw_log.lock);
 
-	printk("nb_cpy=%lu, not_cpy=%lu, start=%p, end=%p\n", (long unsigned int)nb_cpy, (long unsigned int)not_cpy, priv->debugfs.fw_log.buf.start, priv->debugfs.fw_log.buf.end);
+	AICWFDBG(LOGTRACE, "nb_cpy=%lu, not_cpy=%lu, start=%p, end=%p\n", (long unsigned int)nb_cpy, (long unsigned int)not_cpy, priv->debugfs.fw_log.buf.start, priv->debugfs.fw_log.buf.end);
 	return nb_cpy;
 }
 
@@ -825,7 +825,7 @@ static ssize_t rwnx_dbgfs_fw_log_write(struct file *file,
 {
 	//struct rwnx_hw *priv = file->private_data;
 
-	printk("%s\n", __func__);
+	AICWFDBG(LOGTRACE, "%s\n", __func__);
 	return count;
 }
 DEBUGFS_READ_WRITE_FILE_OPS(fw_log);
@@ -1245,12 +1245,12 @@ static ssize_t rwnx_dbgfs_regdbg_write(struct file *file,
 
     	buf[len] = '\0';
 
-	if (sscanf(buf, "%x %x %x" , &oper, &addr, &val ) > 0) 
-		printk("addr=%x, val=%x,oper=%d\n", addr, val, oper);
+		if (sscanf(buf, "%x %x %x" , &oper, &addr, &val ) > 0)
+		AICWFDBG(LOGTRACE, "addr=%x, val=%x,oper=%d\n", addr, val, oper);
 
     	if(oper== 0) {
-		ret = rwnx_send_dbg_mem_read_req(priv, addr, &mem_read_cfm);
-        	printk("[0x%x] = [0x%x]\n", mem_read_cfm.memaddr, mem_read_cfm.memdata);
+     	ret = rwnx_send_dbg_mem_read_req(priv, addr, &mem_read_cfm);
+         	AICWFDBG(LOGTRACE, "[0x%x] = [0x%x]\n", mem_read_cfm.memaddr, mem_read_cfm.memdata);
     	}
 
 	return count;
@@ -1268,7 +1268,7 @@ static ssize_t rwnx_dbgfs_vendor_hwconfig_write(struct file *file,
 	u32_l hwconfig_id;
 	size_t len = min_t(size_t,count,sizeof(buf)-1);
 	int ret;
-    printk("%s\n",__func__);
+    AICWFDBG(LOGTRACE, "%s\n",__func__);
 	//choose the type of write info by struct
 	//struct mm_set_vendor_trx_param_req trx_param;
 
@@ -1280,51 +1280,51 @@ static ssize_t rwnx_dbgfs_vendor_hwconfig_write(struct file *file,
 	ret = sscanf(buf, "%x %x %x %x %x %x %x %x %x %x",
                             &hwconfig_id, &addr[0], &addr[1], &addr[2], &addr[3], &addr[4], &addr[5], &addr[6], &addr[7], &addr[8]);
 	if(ret > 10) {
-		printk("param error > 10\n");
+		AICWFDBG(LOGERROR, "param error > 10\n");
 	} else {
 		switch(hwconfig_id)
 		    {
 		    case 0:
 			if(ret != 5) {
-			    printk("param error  != 5\n");
+			    AICWFDBG(LOGERROR, "param error  != 5\n");
 			    break;}
 			ret = rwnx_send_vendor_hwconfig_req(priv, hwconfig_id, addr);
-			printk("ACS_TXOP_REQ bk:0x%x be:0x%x vi:0x%x vo:0x%x\n",addr[0],  addr[1], addr[2], addr[3]);
+			AICWFDBG(LOGTRACE, "ACS_TXOP_REQ bk:0x%x be:0x%x vi:0x%x vo:0x%x\n",addr[0],  addr[1], addr[2], addr[3]);
 			break;
 		    case 1:
 			if(ret != 10) {
-			    printk("param error  != 10\n");
+			    AICWFDBG(LOGERROR, "param error  != 10\n");
 			    break;}
 			ret = rwnx_send_vendor_hwconfig_req(priv, hwconfig_id, addr);
-			printk("CHANNEL_ACCESS_REQ edca:%x,%x,%x,%x, vif:%x, retry_cnt:%x, rts:%x, long_nav:%x, cfe:%x\n",
+			AICWFDBG(LOGTRACE, "CHANNEL_ACCESS_REQ edca:%x,%x,%x,%x, vif:%x, retry_cnt:%x, rts:%x, long_nav:%x, cfe:%x\n",
                                 addr[0],  addr[1], addr[2], addr[3], addr[4], addr[5], addr[6], addr[7], addr[8]);
 			break;
 		    case 2:
 			if(ret != 7) {
-		            printk("param error  != 7\n");
+		            AICWFDBG(LOGERROR, "param error  != 7\n");
 			    break;}
 			ret = rwnx_send_vendor_hwconfig_req(priv, hwconfig_id, addr);
-			printk("MAC_TIMESCALE_REQ sifsA:%x,sifsB:%x,slot:%x,ofdm_delay:%x,long_delay:%x,short_delay:%x\n",
+			AICWFDBG(LOGTRACE, "MAC_TIMESCALE_REQ sifsA:%x,sifsB:%x,slot:%x,ofdm_delay:%x,long_delay:%x,short_delay:%x\n",
                                 addr[0],  addr[1], addr[2], addr[3], addr[4], addr[5]);
 			break;
 		    case 3:
                         if(ret != 6) {
-		            printk("param error  != 6\n");
+		            AICWFDBG(LOGERROR, "param error  != 6\n");
 			    break;}
 			addr[1] = ~addr[1] + 1;
 			addr[2] = ~addr[2] + 1;
 			addr[3] = ~addr[3] + 1;
 			addr[4] = ~addr[4] + 1;
 			ret = rwnx_send_vendor_hwconfig_req(priv, hwconfig_id, addr);
-			printk("CCA_THRESHOLD_REQ auto_cca:%d, cca20p_rise:%d cca20s_rise:%d cca20p_fail:%d cca20s_fail:%d\n",
+			AICWFDBG(LOGTRACE, "CCA_THRESHOLD_REQ auto_cca:%d, cca20p_rise:%d cca20s_rise:%d cca20p_fail:%d cca20s_fail:%d\n",
                                 addr[0],  addr[1], addr[2], addr[3], addr[4]);
 			break;
 		    default:
-			printk("param error\n");
+			AICWFDBG(LOGERROR, "param error\n");
 			break;
 		}
 		if(ret) {
-		    printk("rwnx_send_vendor_hwconfig_req fail: %x\n", ret);
+		    AICWFDBG(LOGERROR, "rwnx_send_vendor_hwconfig_req fail: %x\n", ret);
 		}
 	}
 
@@ -1580,7 +1580,7 @@ static void idx_to_rate_cfg1(unsigned int formatmod,
             break;
         }
         default:
-            printk("Don't have the formatmod");
+            AICWFDBG(LOGERROR, "Don't have the formatmod");
     }
 }
 
@@ -1775,7 +1775,7 @@ static ssize_t rwnx_dbgfs_rc_fixed_rate_idx_write(struct file *file,
         idx_to_rate_cfg1(formatmod, mcs, nss, bwTx, gi, &rate_config, NULL);
     }
 	/*union rwnx_rate_ctrl_info *r_cfg=&rate_config;
-	printk("formatModTx=%u mcsIndexTx=%u bwTx=%u giAndPreTypeTx=%u\n",r_cfg->formatModTx,r_cfg->mcsIndexTx,r_cfg->bwTx,r_cfg->giAndPreTypeTx);
+	AICWFDBG(LOGTRACE, "formatModTx=%u mcsIndexTx=%u bwTx=%u giAndPreTypeTx=%u\n",r_cfg->formatModTx,r_cfg->mcsIndexTx,r_cfg->bwTx,r_cfg->giAndPreTypeTx);
 	printk("you wen ti");*/
 	// Forward the request to the LMAC
     if ((error = rwnx_send_me_rc_set_rate(priv, sta->sta_idx,
diff --git a/aic8800_fdrv/rwnx_fw_dump.c b/aic8800_fdrv/rwnx_fw_dump.c
index eb47c02..ead022c 100644
--- a/aic8800_fdrv/rwnx_fw_dump.c
+++ b/aic8800_fdrv/rwnx_fw_dump.c
@@ -481,7 +481,7 @@ int rwnx_um_helper(struct rwnx_debugfs *rwnx_debugfs, const char *cmd)
 
     if ((ret = call_usermodehelper(argv[0], argv, envp,
                                    UMH_WAIT_PROC | UMH_KILLABLE)))
-        printk(KERN_CRIT "Failed to call %s (%s returned %d)\n",
+        AICWFDBG(LOGERROR, "Failed to call %s (%s returned %d)\n",
                argv[0], cmd, ret);
     argv_free(argv);
 
diff --git a/aic8800_fdrv/rwnx_fw_trace.c b/aic8800_fdrv/rwnx_fw_trace.c
index fa96583..bbde9d4 100644
--- a/aic8800_fdrv/rwnx_fw_trace.c
+++ b/aic8800_fdrv/rwnx_fw_trace.c
@@ -15,6 +15,7 @@
 #include <linux/fs.h>
 #include <linux/delay.h>
 #include "rwnx_fw_trace.h"
+#include "aicwf_debug.h"
 
 int rwnx_fw_log_init(struct rwnx_fw_log *fw_log)
 {
@@ -29,7 +30,7 @@ int rwnx_fw_log_init(struct rwnx_fw_log *fw_log)
 	fw_log->buf.dataend = fw_log->buf.data + FW_LOG_SIZE;
 	spin_lock_init(&fw_log->lock);
 
-	printk("fw_log_init: %lx, %lx\n", (unsigned long)fw_log->buf.start, (unsigned long)(fw_log->buf.dataend));
+	AICWFDBG(LOGTRACE, "fw_log_init: %lx, %lx\n", (unsigned long)fw_log->buf.start, (unsigned long)(fw_log->buf.dataend));
 	return 0;
 }
 
diff --git a/aic8800_fdrv/rwnx_main.c b/aic8800_fdrv/rwnx_main.c
index cbb1ca4..0f52688 100644
--- a/aic8800_fdrv/rwnx_main.c
+++ b/aic8800_fdrv/rwnx_main.c
@@ -674,59 +674,59 @@ void rwnx_frame_parser(char* tag, char* data, unsigned long len){
 
 #if 0
 	if(data[0] == ASSOC_REQ){
-		printk("%s %s ASSOC_REQ \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s ASSOC_REQ \r\n", __func__, tag);
 	}else if(data[0] == ASSOC_RSP){
-		printk("%s %s ASSOC_RSP \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s ASSOC_RSP \r\n", __func__, tag);
 	}else if(data[0] == PROBE_REQ){
-		printk("%s %s PROBE_REQ \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s PROBE_REQ \r\n", __func__, tag);
 	}else if(data[0] == PROBE_RSP){
-		printk("%s %s PROBE_RSP \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s PROBE_RSP \r\n", __func__, tag);
 	}else if(data[0] == ACTION){
-		printk("%s %s ACTION ", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s ACTION ", __func__, tag);
 		if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x00){
-			printk("GO NEG REQ \r\n");
+			AICWFDBG(LOGTRACE, "GO NEG REQ \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x01){
-			printk("GO NEG RSP \r\n");
+			AICWFDBG(LOGTRACE, "GO NEG RSP \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x02){
-			printk("GO NEG CFM \r\n");
+			AICWFDBG(LOGTRACE, "GO NEG CFM \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x03){
-			printk("P2P INV REQ \r\n");
+			AICWFDBG(LOGTRACE, "P2P INV REQ \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x04){
-			printk("P2P INV RSP \r\n");
+			AICWFDBG(LOGTRACE, "P2P INV RSP \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x05){
-			printk("DD REQ \r\n");
+			AICWFDBG(LOGTRACE, "DD REQ \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x06){
-			printk("DD RSP \r\n");
+			AICWFDBG(LOGTRACE, "DD RSP \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x07){
-			printk("PD REQ \r\n");
+			AICWFDBG(LOGTRACE, "PD REQ \r\n");
 		}else if(data[ACTION_MAC_HDR_LEN] == 0x04 && data[ACTION_MAC_HDR_LEN + 6] == 0x08){
-			printk("PD RSP \r\n");
+			AICWFDBG(LOGTRACE, "PD RSP \r\n");
 		}else{
-			printk("\r\n");
+			AICWFDBG(LOGTRACE, "\r\n");
 		}
 
 	}else if(data[0] == AUTH){
-		printk("%s %s AUTH \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s AUTH \r\n", __func__, tag);
 	}else if(data[0] == DEAUTH){
-		printk("%s %s DEAUTH \r\n", __func__, tag);
+		AICWFDBG(LOGTRACE, "%s %s DEAUTH \r\n", __func__, tag);
 	}else if(data[0] == QOS){
 		if(data[QOS_MAC_HDR_LEN + 6] == 0x88 && data[QOS_MAC_HDR_LEN + 7] == 0x8E){
-			printk("%s %s QOS 802.1X ", __func__, tag);
+			AICWFDBG(LOGTRACE, "%s %s QOS 802.1X ", __func__, tag);
 			if(data[QOS_MAC_HDR_LEN + 9] == 0x03){
-				printk("EAPOL");
+				AICWFDBG(LOGTRACE, "EAPOL");
 			}else if(data[QOS_MAC_HDR_LEN + 9] == 0x00){
-				printk("EAP PACKAGE ");
+				AICWFDBG(LOGTRACE, "EAP PACKAGE ");
 				if(data[QOS_MAC_HDR_LEN + 12] == 0x01){
-					printk("EAP REQ\r\n");
+					AICWFDBG(LOGTRACE, "EAP REQ\r\n");
 				}else if(data[QOS_MAC_HDR_LEN + 12] == 0x02){
-					printk("EAP RSP\r\n");
+					AICWFDBG(LOGTRACE, "EAP RSP\r\n");
 				}else if(data[QOS_MAC_HDR_LEN + 12] == 0x04){
-					printk("EAP FAIL\r\n");
+					AICWFDBG(LOGTRACE, "EAP FAIL\r\n");
 				}else{
-					printk("\r\n");
+					AICWFDBG(LOGTRACE, "\r\n");
 				}
 			}else if(data[QOS_MAC_HDR_LEN + 9] == 0x01){
-				printk("EAP START \r\n");
+				AICWFDBG(LOGTRACE, "EAP START \r\n");
 
 			}
 		}
@@ -807,7 +807,7 @@ void rwnx_insert_vendor_extension_in_bcn(struct rwnx_bcn *bcn){
 				bcn->tail[index + 5] == vendor_extension_subelement[2]){
 				index = index + 2;
 				vendor_extension_subelement_len = bcn->tail[index];
-				printk("%s find vendor_extension_subelement,index:%d len:%d\r\n", __func__, index, bcn->tail[index]);
+				AICWFDBG(LOGTRACE, "%s find vendor_extension_subelement,index:%d len:%d\r\n", __func__, index, bcn->tail[index]);
 				break;
 			}
 		}
@@ -1190,10 +1190,10 @@ void netdev_br_init(struct net_device *netdev)
 			if (br_netdev) {
 				memcpy(rwnx_vif->br_mac, br_netdev->dev_addr, ETH_ALEN);
 				dev_put(br_netdev);
-				AICWFDBG(LOGINFO, FUNC_NDEV_FMT" bind bridge dev "NDEV_FMT"("MAC_FMT")\n"
+				AICWFDBG(LOGTRACE, FUNC_NDEV_FMT" bind bridge dev "NDEV_FMT"("MAC_FMT")\n"
 					, FUNC_NDEV_ARG(netdev), NDEV_ARG(br_netdev), MAC_ARG(br_netdev->dev_addr));
 			} else {
-				AICWFDBG(LOGINFO, FUNC_NDEV_FMT" can't get bridge dev by name \"%s\"\n"
+				AICWFDBG(LOGTRACE, FUNC_NDEV_FMT" can't get bridge dev by name \"%s\"\n"
 					, FUNC_NDEV_ARG(netdev), CONFIG_BR_SUPPORT_BRNAME);
 			}
 		}
@@ -1308,7 +1308,7 @@ static int rwnx_open(struct net_device *dev)
     //netif_carrier_off(dev);
     #if defined(CONFIG_RWNX_MON_XMIT)
     netif_carrier_on(dev);
-	AICWFDBG(LOGINFO, "monitor xmit: netif_carrier_on\n");
+	AICWFDBG(LOGTRACE, "monitor xmit: netif_carrier_on\n");
     #endif
 
 #ifdef CONFIG_BR_SUPPORT
@@ -1362,7 +1362,7 @@ static int rwnx_close(struct net_device *dev)
 #if 0
     netdev_info(dev, "CLOSE");
 #endif
-	AICWFDBG(LOGINFO, "%s %s Enter\n", __func__, dev->name);
+	AICWFDBG(LOGTRACE, "%s %s Enter\n", __func__, dev->name);
 
 #ifdef CONFIG_USE_P2P0
     if(rwnx_hw->p2p_dev_vif){
@@ -1397,7 +1397,7 @@ static int rwnx_close(struct net_device *dev)
 #if defined(AICWF_USB_SUPPORT)
     if (usbdev != NULL) {
         if (usbdev->bus_if->state != BUS_DOWN_ST && usbdev->state != USB_DOWN_ST)
-			AICWFDBG(LOGINFO, "state: %d %d \r\n", (int)usbdev->bus_if->state, (int)usbdev->state);
+			AICWFDBG(LOGTRACE, "state: %d %d \r\n", (int)usbdev->bus_if->state, (int)usbdev->state);
 
 		if(RWNX_VIF_TYPE(rwnx_vif) == NL80211_IFTYPE_STATION ||
 			RWNX_VIF_TYPE(rwnx_vif) == NL80211_IFTYPE_P2P_CLIENT){
@@ -1424,7 +1424,7 @@ static int rwnx_close(struct net_device *dev)
 #endif
 
     if (rwnx_hw->roc_elem && (rwnx_hw->roc_elem->wdev == &rwnx_vif->wdev)) {
-		AICWFDBG(LOGINFO, "%s clear roc\n", __func__);
+		AICWFDBG(LOGTRACE, "%s clear roc\n", __func__);
         /* Initialize RoC element pointer to NULL, indicate that RoC can be started */
         rwnx_hw->roc_elem = NULL;
     }
@@ -1667,7 +1667,7 @@ static int parse_line (char *line, char *argv[])
         *line++ = '\0';         /* terminate current arg     */
     }
 
-    printk("** Too many args (max. %d) **\n", CMD_MAXARGS);
+    AICWFDBG(LOGERROR, "** Too many args (max. %d) **\n", CMD_MAXARGS);
 
     return (nargs);
 }
@@ -1760,14 +1760,14 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
         #endif
         #ifdef CONFIG_RFTEST
         if (strcasecmp(argv[0], "GET_RX_RESULT") ==0) {
-            AICWFDBG(LOGINFO, "get_rx_result\n");
+            AICWFDBG(LOGTRACE, "get_rx_result\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_RX_RESULT, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 8);
             bytes_written = 8;
         } else if (strcasecmp(argv[0], "SET_TX") == 0) {
-            AICWFDBG(LOGINFO, "set_tx\n");
+            AICWFDBG(LOGTRACE, "set_tx\n");
             if (argc < 6) {
-                printk("wrong param\n");
+                AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
                 break;
             }
@@ -1776,21 +1776,21 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             settx_param.mode = command_strtoul(argv[3], NULL, 10);
             settx_param.rate = command_strtoul(argv[4], NULL, 10);
             settx_param.length = command_strtoul(argv[5], NULL, 10);
-            AICWFDBG(LOGINFO, "txparam:%d,%d,%d,%d,%d\n", settx_param.chan, settx_param.bw,
+            AICWFDBG(LOGTRACE, "txparam:%d,%d,%d,%d,%d\n", settx_param.chan, settx_param.bw,
                 settx_param.mode, settx_param.rate, settx_param.length);
             rwnx_send_rftest_req(p_rwnx_hw, SET_TX, sizeof(cmd_rf_settx_t), (u8_l *)&settx_param, NULL);
         } else if (strcasecmp(argv[0], "SET_TXSTOP") == 0) {
-            AICWFDBG(LOGINFO, "settx_stop\n");
+            AICWFDBG(LOGTRACE, "settx_stop\n");
             rwnx_send_rftest_req(p_rwnx_hw, SET_TXSTOP, 0, NULL, NULL);
         } else if (strcasecmp(argv[0], "SET_TXTONE") == 0) {
-            AICWFDBG(LOGINFO, "set_tx_tone,argc:%d\n",argc);
+            AICWFDBG(LOGTRACE, "set_tx_tone,argc:%d\n",argc);
             if ((argc == 2) || (argc == 3)) {
-                AICWFDBG(LOGINFO, "argv 1:%s\n",argv[1]);
+                AICWFDBG(LOGTRACE, "argv 1:%s\n",argv[1]);
                 //u8_l func = (u8_l)command_strtoul(argv[1], NULL, 16);
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
                 //s8_l freq;
                 if (argc == 3) {
-                    AICWFDBG(LOGINFO, "argv 2:%s\n",argv[2]);
+                    AICWFDBG(LOGTRACE, "argv 2:%s\n",argv[2]);
                     freq_ = (u8_l)command_strtoul(argv[2], NULL, 10);
                 } else {
                     freq_ = 0;
@@ -1800,12 +1800,12 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
 				buf[1] = (u8_l)freq_;
                 rwnx_send_rftest_req(p_rwnx_hw, SET_TXTONE, argc - 1, buf, NULL);
             } else {
-                AICWFDBG(LOGINFO, "wrong args\n");
+                AICWFDBG(LOGTRACE, "wrong args\n");
                 bytes_written = -EINVAL;
                 break;
             }
         } else if (strcasecmp(argv[0], "SET_RX") == 0) {
-            AICWFDBG(LOGINFO, "set_rx\n");
+            AICWFDBG(LOGTRACE, "set_rx\n");
             if (argc < 3) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -1815,38 +1815,38 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             setrx_param.bw = command_strtoul(argv[2], NULL, 10);
             rwnx_send_rftest_req(p_rwnx_hw, SET_RX, sizeof(cmd_rf_rx_t), (u8_l *)&setrx_param, NULL);
         } else if (strcasecmp(argv[0], "SET_RXSTOP") == 0) {
-            AICWFDBG(LOGINFO, "set_rxstop\n");
+            AICWFDBG(LOGTRACE, "set_rxstop\n");
             rwnx_send_rftest_req(p_rwnx_hw, SET_RXSTOP, 0, NULL, NULL);
         } else if (strcasecmp(argv[0], "SET_RX_METER") == 0) {
-            AICWFDBG(LOGINFO, "set_rx_meter\n");
+            AICWFDBG(LOGTRACE, "set_rx_meter\n");
             freq = (int)command_strtoul(argv[1], NULL, 10);
             rwnx_send_rftest_req(p_rwnx_hw, SET_RX_METER, sizeof(freq), (u8_l *)&freq, NULL);
         } else if (strcasecmp(argv[0], "SET_FREQ_CAL") == 0) {
-            AICWFDBG(LOGINFO, "set_freq_cal\n");
+            AICWFDBG(LOGTRACE, "set_freq_cal\n");
             if (argc < 2) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
                 break;
             }
             cmd_setfreq.val = command_strtoul(argv[1], NULL, 16);
-            AICWFDBG(LOGINFO, "param:%x\r\n", cmd_setfreq.val);
+            AICWFDBG(LOGTRACE, "param:%x\r\n", cmd_setfreq.val);
             rwnx_send_rftest_req(p_rwnx_hw, SET_FREQ_CAL, sizeof(cmd_rf_setfreq_t), (u8_l *)&cmd_setfreq, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "SET_FREQ_CAL_FINE") == 0) {
-            AICWFDBG(LOGINFO, "set_freq_cal_fine\n");
+            AICWFDBG(LOGTRACE, "set_freq_cal_fine\n");
             if (argc < 2) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
                 break;
             }
             cmd_setfreq.val = command_strtoul(argv[1], NULL, 16);
-            AICWFDBG(LOGINFO, "param:%x\r\n", cmd_setfreq.val);
+            AICWFDBG(LOGTRACE, "param:%x\r\n", cmd_setfreq.val);
             rwnx_send_rftest_req(p_rwnx_hw, SET_FREQ_CAL_FINE, sizeof(cmd_rf_setfreq_t), (u8_l *)&cmd_setfreq, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "GET_EFUSE_BLOCK") == 0) {
-            AICWFDBG(LOGINFO, "get_efuse_block\n");
+            AICWFDBG(LOGTRACE, "get_efuse_block\n");
             if (argc < 2) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -1854,11 +1854,11 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             }
             getefuse_param.block = command_strtoul(argv[1], NULL, 10);
             rwnx_send_rftest_req(p_rwnx_hw, GET_EFUSE_BLOCK, sizeof(cmd_rf_getefuse_t), (u8_l *)&getefuse_param, &cfm);
-            AICWFDBG(LOGINFO, "get val=%x\r\n", cfm.rftest_result[0]);
+            AICWFDBG(LOGTRACE, "get val=%x\r\n", cfm.rftest_result[0]);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "SET_POWER") == 0) {
-            AICWFDBG(LOGINFO, "set_power\n");
+            AICWFDBG(LOGTRACE, "set_power\n");
             ana_pwr = command_strtoul(argv[1], NULL, 16);
             dig_pwr = command_strtoul(argv[2], NULL, 16);
             pwr = (ana_pwr << 4 | dig_pwr);
@@ -1867,45 +1867,45 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 bytes_written = -EINVAL;
                 break;
             }
-            AICWFDBG(LOGINFO, "pwr =%x\r\n", pwr);
+            AICWFDBG(LOGTRACE, "pwr =%x\r\n", pwr);
             rwnx_send_rftest_req(p_rwnx_hw, SET_POWER, sizeof(pwr), (u8_l *)&pwr, NULL);
         } else if (strcasecmp(argv[0], "SET_PAPR") == 0) {
-            printk("set papr\n");
+            AICWFDBG(LOGTRACE, "set papr\n");
             if (argc > 1) {
                 papr = command_strtoul(argv[1], NULL, 10);
-                printk("papr %d\r\n", func);
+                AICWFDBG(LOGTRACE, "papr %d\r\n", func);
                 rwnx_send_rftest_req(p_rwnx_hw, SET_PAPR, sizeof(papr), (u8_l *)&papr, NULL);
             } else {
-                printk("wrong args\n");
+                AICWFDBG(LOGERROR, "wrong args\n");
                 bytes_written = -EINVAL;
                 break;
             }
         } else if (strcasecmp(argv[0], "SET_XTAL_CAP")==0) {
-            AICWFDBG(LOGINFO, "set_xtal_cap\n");
+            AICWFDBG(LOGTRACE, "set_xtal_cap\n");
             if (argc < 2) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
                 break;
             }
             xtal_cap = command_strtoul(argv[1], NULL, 10);
-            AICWFDBG(LOGINFO, "xtal_cap =%x\r\n", xtal_cap);
+            AICWFDBG(LOGTRACE, "xtal_cap =%x\r\n", xtal_cap);
             rwnx_send_rftest_req(p_rwnx_hw, SET_XTAL_CAP, sizeof(xtal_cap), (u8_l *)&xtal_cap, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "SET_XTAL_CAP_FINE")==0) {
-            AICWFDBG(LOGINFO, "set_xtal_cap_fine\n");
+            AICWFDBG(LOGTRACE, "set_xtal_cap_fine\n");
             if (argc < 2) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
                 break;
             }
             xtal_cap_fine = command_strtoul(argv[1], NULL, 10);
-            AICWFDBG(LOGINFO, "xtal_cap_fine =%x\r\n", xtal_cap_fine);
+            AICWFDBG(LOGTRACE, "xtal_cap_fine =%x\r\n", xtal_cap_fine);
             rwnx_send_rftest_req(p_rwnx_hw, SET_XTAL_CAP_FINE, sizeof(xtal_cap_fine), (u8_l *)&xtal_cap_fine, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "SET_MAC_ADDR")==0) {
-            AICWFDBG(LOGINFO, "set_mac_addr\n");
+            AICWFDBG(LOGTRACE, "set_mac_addr\n");
             if (argc < 7) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -1917,11 +1917,11 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             mac_addr[2] = command_strtoul(argv[4], NULL, 16);
             mac_addr[1] = command_strtoul(argv[5], NULL, 16);
             mac_addr[0] = command_strtoul(argv[6], NULL, 16);
-            AICWFDBG(LOGINFO, "set macaddr:%x,%x,%x,%x,%x,%x\n", mac_addr[5], mac_addr[4], mac_addr[3], mac_addr[2], mac_addr[1], mac_addr[0]);
+            AICWFDBG(LOGTRACE, "set macaddr:%x,%x,%x,%x,%x,%x\n", mac_addr[5], mac_addr[4], mac_addr[3], mac_addr[2], mac_addr[1], mac_addr[0]);
             rwnx_send_rftest_req(p_rwnx_hw, SET_MAC_ADDR, sizeof(mac_addr), (u8_l *)&mac_addr, NULL);
         } else if (strcasecmp(argv[0], "GET_MAC_ADDR")==0) {
             u32_l addr0, addr1;
-            AICWFDBG(LOGINFO, "get mac addr\n");
+            AICWFDBG(LOGTRACE, "get mac addr\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_MAC_ADDR, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 8);
             bytes_written = 8;
@@ -1930,13 +1930,13 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 (g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW)) {
                 int rem_cnt = (cfm.rftest_result[1] >> 16) & 0x00FF;
                 addr1 = cfm.rftest_result[1] & 0x0000FFFF;
-                AICWFDBG(LOGINFO, "0x%x,0x%x (remain:%x)\n", addr0, addr1, rem_cnt);
+                AICWFDBG(LOGTRACE, "0x%x,0x%x (remain:%x)\n", addr0, addr1, rem_cnt);
             } else {
                 addr1 = cfm.rftest_result[1];
-                AICWFDBG(LOGINFO, "0x%x,0x%x\n", addr0, addr1);
+                AICWFDBG(LOGTRACE, "0x%x,0x%x\n", addr0, addr1);
             }
         } else if (strcasecmp(argv[0], "SET_BT_MAC_ADDR") == 0) {
-            AICWFDBG(LOGINFO, "set_bt_mac_addr\n");
+            AICWFDBG(LOGTRACE, "set_bt_mac_addr\n");
             if (argc < 7) {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -1948,11 +1948,11 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             mac_addr[2] = command_strtoul(argv[4], NULL, 16);
             mac_addr[1] = command_strtoul(argv[5], NULL, 16);
             mac_addr[0] = command_strtoul(argv[6], NULL, 16);
-            AICWFDBG(LOGINFO, "set bt macaddr:%x,%x,%x,%x,%x,%x\n", mac_addr[5], mac_addr[4], mac_addr[3], mac_addr[2], mac_addr[1], mac_addr[0]);
+            AICWFDBG(LOGTRACE, "set bt macaddr:%x,%x,%x,%x,%x,%x\n", mac_addr[5], mac_addr[4], mac_addr[3], mac_addr[2], mac_addr[1], mac_addr[0]);
             rwnx_send_rftest_req(p_rwnx_hw, SET_BT_MAC_ADDR, sizeof(mac_addr), (u8_l *)&mac_addr, NULL);
         } else if (strcasecmp(argv[0], "GET_BT_MAC_ADDR")==0) {
             u32_l addr0, addr1;
-            AICWFDBG(LOGINFO, "get bt mac addr\n");
+            AICWFDBG(LOGTRACE, "get bt mac addr\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_BT_MAC_ADDR, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 8);
             bytes_written = 8;
@@ -1961,14 +1961,14 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 (g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW)) {
                 int rem_cnt = (cfm.rftest_result[1] >> 16) & 0x00FF;
                 addr1 = cfm.rftest_result[1] & 0x0000FFFF;
-                AICWFDBG(LOGINFO, "0x%x,0x%x (remain:%x)\n", addr0, addr1, rem_cnt);
+                AICWFDBG(LOGTRACE, "0x%x,0x%x (remain:%x)\n", addr0, addr1, rem_cnt);
             } else {
                 addr1 = cfm.rftest_result[1];
-                AICWFDBG(LOGINFO, "0x%x,0x%x\n", addr0, addr1);
+                AICWFDBG(LOGTRACE, "0x%x,0x%x\n", addr0, addr1);
             }
         } else if (strcasecmp(argv[0], "SET_VENDOR_INFO")==0) {
             vendor_info = command_strtoul(argv[1], NULL, 16);
-            AICWFDBG(LOGINFO, "set vendor info:%x\n", vendor_info);
+            AICWFDBG(LOGTRACE, "set vendor info:%x\n", vendor_info);
             rwnx_send_rftest_req(p_rwnx_hw, SET_VENDOR_INFO, 1, &vendor_info, &cfm);
             if ((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DC) ||
                 (g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW)) {
@@ -1978,9 +1978,9 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 memcpy(command, &cfm.rftest_result[0], 1);
                 bytes_written = 1;
             }
-            AICWFDBG(LOGINFO, "0x%x\n", cfm.rftest_result[0]);
+            AICWFDBG(LOGTRACE, "0x%x\n", cfm.rftest_result[0]);
         } else if (strcasecmp(argv[0], "GET_VENDOR_INFO")==0) {
-            AICWFDBG(LOGINFO, "get vendor info\n");
+            AICWFDBG(LOGTRACE, "get vendor info\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_VENDOR_INFO, 0, NULL, &cfm);
             if ((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DC) ||
                 (g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW)) {
@@ -1990,29 +1990,29 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 memcpy(command, &cfm.rftest_result[0], 1);
                 bytes_written = 1;
             }
-            AICWFDBG(LOGINFO, "0x%x\n", cfm.rftest_result[0]);
+            AICWFDBG(LOGTRACE, "0x%x\n", cfm.rftest_result[0]);
         } else if (strcasecmp(argv[0], "GET_FREQ_CAL") == 0) {
             unsigned int val;
-            AICWFDBG(LOGINFO, "get freq cal\n");
+            AICWFDBG(LOGTRACE, "get freq cal\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_FREQ_CAL, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
             val = cfm.rftest_result[0];
             if ((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DC) ||
                 (g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DW)) {
-                AICWFDBG(LOGINFO, "cap=0x%x (remain:%x), cap_fine=%x (remain:%x)\n",
+                AICWFDBG(LOGTRACE, "cap=0x%x (remain:%x), cap_fine=%x (remain:%x)\n",
                         val & 0xff, (val >> 8) & 0xff, (val >> 16) & 0xff, (val >> 24) & 0xff);
             } else {
-                AICWFDBG(LOGINFO, "cap=0x%x, cap_fine=0x%x\n", val & 0xff, (val >> 8) & 0xff);
+                AICWFDBG(LOGTRACE, "cap=0x%x, cap_fine=0x%x\n", val & 0xff, (val >> 8) & 0xff);
             }
         } else if (strcasecmp(argv[0], "RDWR_PWRMM") == 0) {
-            AICWFDBG(LOGINFO, "read/write txpwr manul mode\n");
+            AICWFDBG(LOGTRACE, "read/write txpwr manul mode\n");
             if (argc <= 1) { // read cur
                 rwnx_send_rftest_req(p_rwnx_hw, RDWR_PWRMM, 0, NULL, &cfm);
             } else { // write
                 u8_l pwrmm = (u8_l)command_strtoul(argv[1], NULL, 16);
                 pwrmm = (pwrmm) ? 1 : 0;
-                AICWFDBG(LOGINFO, "set pwrmm = %x\r\n", pwrmm);
+                AICWFDBG(LOGTRACE, "set pwrmm = %x\r\n", pwrmm);
                 rwnx_send_rftest_req(p_rwnx_hw, RDWR_PWRMM, sizeof(pwrmm), (u8_l *)&pwrmm, &cfm);
             }
             memcpy(command, &cfm.rftest_result[0], 4);
@@ -2026,7 +2026,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     break;
             }
             #endif
-            AICWFDBG(LOGINFO, "read/write txpwr index\n");
+            AICWFDBG(LOGTRACE, "read/write txpwr index\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2037,7 +2037,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     u8_l type = (u8_l)command_strtoul(argv[2], NULL, 16);
                     u8_l pwridx = (u8_l)command_strtoul(argv[3], NULL, 10);
                     u8_l buf[3] = {func, type, pwridx};
-                    AICWFDBG(LOGINFO, "set pwridx:[%x][%x]=%x\r\n", func, type, pwridx);
+                    AICWFDBG(LOGTRACE, "set pwridx:[%x][%x]=%x\r\n", func, type, pwridx);
                     rwnx_send_rftest_req(p_rwnx_hw, RDWR_PWRIDX, sizeof(buf), buf, &cfm);
                 } else {
                     AICWFDBG(LOGERROR, "wrong args\n");
@@ -2061,7 +2061,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     break;
             }
             #endif
-            printk("read/write txpwr level\n");
+            AICWFDBG(LOGTRACE, "read/write txpwr level\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2073,7 +2073,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     u8_l idx, size;
                     u8_l buf[14] = {func, grp,};
                     if (argc > 12) { // set all grp
-                        printk("set pwrlvl %s:\n"
+                        AICWFDBG(LOGTRACE, "set pwrlvl %s:\n"
                                "  [%x] =", (func == 1) ? "2.4g" : "5g", grp);
                         if (grp == 1) { // TXPWR_LVL_GRP_11N_11AC
                             size = 10;
@@ -2084,11 +2084,11 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                             s8_l pwrlvl = (s8_l)command_strtoul(argv[3 + idx], NULL, 10);
                             buf[2 + idx] = (u8_l)pwrlvl;
                             if (idx && !(idx & 0x3)) {
-                                printk(" ");
-                            }
-                            printk(" %2d", pwrlvl);
+                            AICWFDBG(LOGTRACE, " ");
                         }
-                        printk("\n");
+                        AICWFDBG(LOGTRACE, " %2d", pwrlvl);
+                        }
+                        AICWFDBG(LOGTRACE, "\n");
                         size += 2;
                     } else { // set grp[idx]
                         u8_l idx = (u8_l)command_strtoul(argv[3], NULL, 10);
@@ -2096,21 +2096,21 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                         buf[2] = idx;
                         buf[3] = (u8_l)pwrlvl;
                         size = 4;
-                        printk("set pwrlvl %s:\n"
+                        AICWFDBG(LOGTRACE, "set pwrlvl %s:\n"
                                "  [%x][%d] = %d\n", (func == 1) ? "2.4g" : "5g", grp, idx, pwrlvl);
                     }
                     rwnx_send_rftest_req(p_rwnx_hw, RDWR_PWRLVL, size, buf, &cfm);
                 } else {
-                    printk("wrong args\n");
+                    AICWFDBG(LOGERROR, "wrong args\n");
                 }
             } else {
-                printk("wrong func: %x\n", func);
+                AICWFDBG(LOGERROR, "wrong func: %x\n", func);
             }
             memcpy(command, &cfm.rftest_result[0], 3 * 12);
             bytes_written = 3 * 12;
         } else if (strcasecmp(argv[0], "RDWR_PWROFST") == 0) {
             u8_l func = 0;
-            AICWFDBG(LOGINFO, "read/write txpwr offset\n");
+            AICWFDBG(LOGTRACE, "read/write txpwr offset\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2121,7 +2121,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     u8_l chgrp = (u8_l)command_strtoul(argv[2], NULL, 16);
                     s8_l pwrofst = (u8_l)command_strtoul(argv[3], NULL, 10);
                     u8_l buf[3] = {func, chgrp, (u8_l)pwrofst};
-                    AICWFDBG(LOGINFO, "set pwrofst:[%x][%x]=%d\r\n", func, chgrp, pwrofst);
+                    AICWFDBG(LOGTRACE, "set pwrofst:[%x][%x]=%d\r\n", func, chgrp, pwrofst);
                     rwnx_send_rftest_req(p_rwnx_hw, RDWR_PWROFST, sizeof(buf), buf, &cfm);
                 } else {
                     AICWFDBG(LOGERROR, "wrong args\n");
@@ -2137,7 +2137,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             bytes_written = 7;
         } else if (strcasecmp(argv[0], "RDWR_DRVIBIT") == 0) {
             u8_l func = 0;
-            AICWFDBG(LOGINFO, "read/write pa drv_ibit\n");
+            AICWFDBG(LOGTRACE, "read/write pa drv_ibit\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2147,7 +2147,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 if (argc > 2) {
                     u8_l ibit = (u8_l)command_strtoul(argv[2], NULL, 16);
                     u8_l buf[2] = {func, ibit};
-                    AICWFDBG(LOGINFO, "set drvibit:[%x]=%x\r\n", func, ibit);
+                    AICWFDBG(LOGTRACE, "set drvibit:[%x]=%x\r\n", func, ibit);
                     rwnx_send_rftest_req(p_rwnx_hw, RDWR_DRVIBIT, sizeof(buf), buf, &cfm);
                 } else {
                     AICWFDBG(LOGERROR, "wrong args\n");
@@ -2163,7 +2163,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             bytes_written = 16;
         } else if (strcasecmp(argv[0], "RDWR_EFUSE_PWROFST") == 0) {
             u8_l func = 0;
-            AICWFDBG(LOGINFO, "read/write txpwr offset into efuse\n");
+            AICWFDBG(LOGTRACE, "read/write txpwr offset into efuse\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2174,7 +2174,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                     u8_l chgrp = (u8_l)command_strtoul(argv[2], NULL, 16);
                     s8_l pwrofst = (u8_l)command_strtoul(argv[3], NULL, 10);
                     u8_l buf[3] = {func, chgrp, (u8_l)pwrofst};
-                    AICWFDBG(LOGINFO, "set efuse pwrofst:[%x][%x]=%d\r\n", func, chgrp, pwrofst);
+                    AICWFDBG(LOGTRACE, "set efuse pwrofst:[%x][%x]=%d\r\n", func, chgrp, pwrofst);
                     rwnx_send_rftest_req(p_rwnx_hw, RDWR_EFUSE_PWROFST, sizeof(buf), buf, &cfm);
                 } else {
                     AICWFDBG(LOGERROR, "wrong args\n");
@@ -2196,7 +2196,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             }
         } else if (strcasecmp(argv[0], "RDWR_EFUSE_DRVIBIT") == 0) {
             u8_l func = 0;
-            AICWFDBG(LOGINFO, "read/write pa drv_ibit into efuse\n");
+            AICWFDBG(LOGTRACE, "read/write pa drv_ibit into efuse\n");
             if (argc > 1) {
                 func = (u8_l)command_strtoul(argv[1], NULL, 16);
             }
@@ -2206,7 +2206,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 if (argc > 2) {
                 u8_l ibit = (u8_l)command_strtoul(argv[2], NULL, 16);
                 u8_l buf[2] = {func, ibit};
-                AICWFDBG(LOGINFO, "set efuse drvibit:[%x]=%x\r\n", func, ibit);
+                AICWFDBG(LOGTRACE, "set efuse drvibit:[%x]=%x\r\n", func, ibit);
                 rwnx_send_rftest_req(p_rwnx_hw, RDWR_EFUSE_DRVIBIT, sizeof(buf), buf, &cfm);
                 } else {
                     AICWFDBG(LOGERROR, "wrong args\n");
@@ -2221,16 +2221,16 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
         } else if (strcasecmp(argv[0], "SET_CAL_XTAL") == 0) {
-            AICWFDBG(LOGINFO, "set_cal_xtal\n");
+            AICWFDBG(LOGTRACE, "set_cal_xtal\n");
             rwnx_send_rftest_req(p_rwnx_hw, SET_CAL_XTAL, 0, NULL, NULL);
         } else if (strcasecmp(argv[0], "GET_CAL_XTAL_RES") == 0) {
-            AICWFDBG(LOGINFO, "get_cal_xtal_res\n");
+            AICWFDBG(LOGTRACE, "get_cal_xtal_res\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_CAL_XTAL_RES, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 4);
             bytes_written = 4;
-            AICWFDBG(LOGINFO, "cap=0x%x, cap_fine=0x%x\n", cfm.rftest_result[0] & 0x0000ffff, (cfm.rftest_result[0] >> 16) & 0x0000ffff);
+            AICWFDBG(LOGTRACE, "cap=0x%x, cap_fine=0x%x\n", cfm.rftest_result[0] & 0x0000ffff, (cfm.rftest_result[0] >> 16) & 0x0000ffff);
 	} else if (strcasecmp(argv[0], "SET_COB_CAL") == 0) {
-	    AICWFDBG(LOGINFO, "set_cob_cal\n");
+	    AICWFDBG(LOGTRACE, "set_cob_cal\n");
 	    if (argc < 3) {
 	    	AICWFDBG(LOGERROR, "wrong param\n");
 	    	bytes_written = -EINVAL;
@@ -2240,14 +2240,14 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
 	    setcob_cal.chip_num = command_strtoul(argv[2], NULL, 10);
 	    rwnx_send_rftest_req(p_rwnx_hw, SET_COB_CAL, sizeof(cmd_rf_setcobcal_t), (u8_l *)&setcob_cal, NULL);
 	} else if (strcasecmp(argv[0], "GET_COB_CAL_RES")==0) {
-            AICWFDBG(LOGINFO, "get_cob_cal_res\n");
+            AICWFDBG(LOGTRACE, "get_cob_cal_res\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_COB_CAL_RES, 0, NULL, &cfm);
             state = (cfm.rftest_result[0] >> 16) & 0x000000ff;
             if (!state){
-            	AICWFDBG(LOGINFO, "cap= 0x%x, cap_fine= 0x%x, freq_ofst= %d Hz\n",
+            	AICWFDBG(LOGTRACE, "cap= 0x%x, cap_fine= 0x%x, freq_ofst= %d Hz\n",
             	cfm.rftest_result[0] & 0x000000ff, (cfm.rftest_result[0] >> 8) & 0x000000ff, cfm.rftest_result[1]);
                 cob_result_ptr = (cob_result_ptr_t *) & (cfm.rftest_result[2]);
-                AICWFDBG(LOGINFO, "golden_rcv_dut= %d , tx_rssi= %d dBm, snr = %d dB\ndut_rcv_godlden= %d , rx_rssi= %d dBm",
+                AICWFDBG(LOGTRACE, "golden_rcv_dut= %d , tx_rssi= %d dBm, snr = %d dB\ndut_rcv_godlden= %d , rx_rssi= %d dBm",
                 cob_result_ptr->golden_rcv_dut_num, cob_result_ptr->rssi_static, cob_result_ptr->snr_static,
 		cob_result_ptr->dut_rcv_golden_num, cob_result_ptr->dut_rssi_static);
                 memcpy(command, &cfm.rftest_result, 16);
@@ -2258,7 +2258,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
                 break;
             }
         } else if (strcasecmp(argv[0], "DO_COB_TEST") == 0) {
-	    AICWFDBG(LOGINFO, "do_cob_test\n");
+	    AICWFDBG(LOGTRACE, "do_cob_test\n");
 		setcob_cal.dutid = 1;
                 setcob_cal.chip_num = 1;
 		rwnx_send_rftest_req(p_rwnx_hw, SET_COB_CAL, sizeof(cmd_rf_setcobcal_t), (u8_l *)&setcob_cal, NULL);
@@ -2266,10 +2266,10 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             rwnx_send_rftest_req(p_rwnx_hw, GET_COB_CAL_RES, 0, NULL, &cfm);
             state = (cfm.rftest_result[0] >> 16) & 0x000000ff;
             if (!state){
-                AICWFDBG(LOGINFO, "cap= 0x%x, cap_fine= 0x%x, freq_ofst= %d Hz\n",
+                AICWFDBG(LOGTRACE, "cap= 0x%x, cap_fine= 0x%x, freq_ofst= %d Hz\n",
                 cfm.rftest_result[0] & 0x000000ff, (cfm.rftest_result[0] >> 8) & 0x000000ff, cfm.rftest_result[1]);
                 cob_result_ptr = (cob_result_ptr_t *) & (cfm.rftest_result[2]);
-                AICWFDBG(LOGINFO, "golden_rcv_dut= %d , tx_rssi= %d dBm, snr = %d dB\ndut_rcv_godlden= %d , rx_rssi= %d dBm",
+                AICWFDBG(LOGTRACE, "golden_rcv_dut= %d , tx_rssi= %d dBm, snr = %d dB\ndut_rcv_godlden= %d , rx_rssi= %d dBm",
                 cob_result_ptr->golden_rcv_dut_num, cob_result_ptr->rssi_static, cob_result_ptr->snr_static,
                 cob_result_ptr->dut_rcv_golden_num, cob_result_ptr->dut_rssi_static);
                 memcpy(command, &cfm.rftest_result, 16);
@@ -2284,12 +2284,12 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
         #ifdef CONFIG_USB_BT
         else if (strcasecmp(argv[0], "BT_RESET") == 0) {
             if (argc == 5) {
-                AICWFDBG(LOGINFO, "btrf reset\n");
+                AICWFDBG(LOGTRACE, "btrf reset\n");
                 for (bt_index = 0; bt_index < 4; bt_index++) {
                     dh_cmd_reset[bt_index] = command_strtoul(argv[bt_index+1], NULL, 16);
-                    AICWFDBG(LOGINFO, "0x%x ",dh_cmd_reset[bt_index]);
+                    AICWFDBG(LOGTRACE, "0x%x ",dh_cmd_reset[bt_index]);
                 }
-                AICWFDBG(LOGINFO, "\n");
+                AICWFDBG(LOGTRACE, "\n");
             } else {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -2298,12 +2298,12 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             rwnx_send_rftest_req(p_rwnx_hw, BT_RESET, sizeof(dh_cmd_reset), (u8_l *)&dh_cmd_reset, NULL);
         } else if (strcasecmp(argv[0], "BT_TXDH") == 0) {
             if (argc == 19) {
-                AICWFDBG(LOGINFO, "btrf txdh\n");
+                AICWFDBG(LOGTRACE, "btrf txdh\n");
                 for (bt_index = 0; bt_index < 18; bt_index++) {
                     dh_cmd_txdh[bt_index] = command_strtoul(argv[bt_index+1], NULL, 16);
-                    AICWFDBG(LOGINFO, "0x%x ", dh_cmd_txdh[bt_index]);
+                    AICWFDBG(LOGTRACE, "0x%x ", dh_cmd_txdh[bt_index]);
                 }
-                AICWFDBG(LOGINFO, "\n");
+                AICWFDBG(LOGTRACE, "\n");
             } else {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -2312,12 +2312,12 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             rwnx_send_rftest_req(p_rwnx_hw, BT_TXDH, sizeof(dh_cmd_txdh), (u8_l *)&dh_cmd_txdh, NULL);
         } else if (strcasecmp(argv[0], "BT_RXDH") == 0) {
             if (argc == 18) {
-                AICWFDBG(LOGINFO, "btrf rxdh\n");
+                AICWFDBG(LOGTRACE, "btrf rxdh\n");
                 for (bt_index = 0; bt_index < 17; bt_index++) {
                     dh_cmd_rxdh[bt_index] = command_strtoul(argv[bt_index+1], NULL, 16);
-                    AICWFDBG(LOGINFO, "0x%x ", dh_cmd_rxdh[bt_index]);
+                    AICWFDBG(LOGTRACE, "0x%x ", dh_cmd_rxdh[bt_index]);
                 }
-                AICWFDBG(LOGINFO, "\n");
+                AICWFDBG(LOGTRACE, "\n");
             } else {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -2326,12 +2326,12 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             rwnx_send_rftest_req(p_rwnx_hw, BT_RXDH, sizeof(dh_cmd_rxdh), (u8_l *)&dh_cmd_rxdh, NULL);
         } else if (strcasecmp(argv[0], "BT_STOP") == 0) {
             if (argc == 6) {
-                AICWFDBG(LOGINFO, "btrf stop\n");
+                AICWFDBG(LOGTRACE, "btrf stop\n");
                 for (bt_index = 0; bt_index < 5; bt_index++) {
                     dh_cmd_stop[bt_index] = command_strtoul(argv[bt_index+1], NULL, 16);
-                    AICWFDBG(LOGINFO, "0x%x ", dh_cmd_stop[bt_index]);
+                    AICWFDBG(LOGTRACE, "0x%x ", dh_cmd_stop[bt_index]);
                 }
-                AICWFDBG(LOGINFO, "\n");
+                AICWFDBG(LOGTRACE, "\n");
             } else {
                 AICWFDBG(LOGERROR, "wrong param\n");
                 bytes_written = -EINVAL;
@@ -2339,7 +2339,7 @@ int handle_private_cmd(struct net_device *net, char *command, u32 cmd_len)
             }
             rwnx_send_rftest_req(p_rwnx_hw, BT_STOP, sizeof(dh_cmd_stop), (u8_l *)&dh_cmd_stop, NULL);
         } else if (strcasecmp(argv[0], "GET_BT_RX_RESULT") ==0) {
-            AICWFDBG(LOGINFO, "get_bt_rx_result\n");
+            AICWFDBG(LOGTRACE, "get_bt_rx_result\n");
             rwnx_send_rftest_req(p_rwnx_hw, GET_BT_RX_RESULT, 0, NULL, &cfm);
             memcpy(command, &cfm.rftest_result[0], 12);
             bytes_written = 12;
@@ -2385,7 +2385,7 @@ void set_vendor_extension_ie(char *command){
 	vendor_extension_len = 0;
 	memcpy(databyte, command + command_index, 2);
 	vendor_extension_len = command_strtoul(databyte, NULL, 16);
-	AICWFDBG(LOGINFO, "%s len:%d \r\n", __func__, vendor_extension_len);
+	AICWFDBG(LOGTRACE, "%s len:%d \r\n", __func__, vendor_extension_len);
 
 	//parser command and save data in vendor_extension_data
 	for(data_index = 0;data_index < vendor_extension_len; data_index++){
@@ -2473,9 +2473,9 @@ int android_priv_cmd(struct net_device *net, struct ifreq *ifr, int cmd)
     command[priv_cmd.total_len] = '\0';
 
     /* outputs */
-    AICWFDBG(LOGINFO, "%s: Android private cmd \"%s\" on %s\n", __FUNCTION__, command, ifr->ifr_name);
-    AICWFDBG(LOGINFO, "cmd = %d\n", cmd);
-    AICWFDBG(LOGINFO, "buf_size=%d\n", buf_size);
+    AICWFDBG(LOGTRACE, "%s: Android private cmd \"%s\" on %s\n", __FUNCTION__, command, ifr->ifr_name);
+    AICWFDBG(LOGTRACE, "cmd = %d\n", cmd);
+    AICWFDBG(LOGTRACE, "buf_size=%d\n", buf_size);
 
 
 #if 1//Handle Android command
@@ -2493,11 +2493,11 @@ int android_priv_cmd(struct net_device *net, struct ifreq *ifr, int cmd)
 			if((ret = regulatory_set_wiphy_regd(vif->rwnx_hw->wiphy, regdomain))){
 				AICWFDBG(LOGERROR, "regulatory_set_wiphy_regd fail \r\n");
 			}else{
-				AICWFDBG(LOGINFO, "regulatory_set_wiphy_regd ok \r\n");
+				AICWFDBG(LOGTRACE, "regulatory_set_wiphy_regd ok \r\n");
 			}
 		}
 #endif
-		AICWFDBG(LOGINFO, "%s country code:%c%c\n", __func__, toupper(country[0]), toupper(country[1]));
+		AICWFDBG(LOGTRACE, "%s country code:%c%c\n", __func__, toupper(country[0]), toupper(country[1]));
 		regdomain = getRegdomainFromRwnxDB(vif->rwnx_hw->wiphy, country);
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 0, 0)
 		if((ret = regulatory_set_wiphy_regd(vif->rwnx_hw->wiphy, regdomain))){
@@ -2542,7 +2542,7 @@ int android_priv_cmd(struct net_device *net, struct ifreq *ifr, int cmd)
             command[0] = '\0';
         }
         if (bytes_written >= priv_cmd.total_len) {
-            AICWFDBG(LOGINFO, "%s: err. bytes_written:%d >= buf_size:%d \n",
+            AICWFDBG(LOGTRACE, "%s: err. bytes_written:%d >= buf_size:%d \n",
                 __FUNCTION__, bytes_written, buf_size);
             goto exit;
         }
@@ -2578,16 +2578,16 @@ static int rwnx_do_ioctl(struct net_device *net, struct ifreq *req, int cmd)
     switch(cmd)
     {
         case IOCTL_HOSTAPD:
-            AICWFDBG(LOGINFO, "IOCTL_HOSTAPD\n");
+            AICWFDBG(LOGTRACE, "IOCTL_HOSTAPD\n");
             break;
         case IOCTL_WPAS:
-            AICWFDBG(LOGINFO, "IOCTL_WPAS\n");
+            AICWFDBG(LOGTRACE, "IOCTL_WPAS\n");
             break;
         case SIOCDEVPRIVATE:
-            AICWFDBG(LOGINFO, "IOCTL SIOCDEVPRIVATE\n");
+            AICWFDBG(LOGTRACE, "IOCTL SIOCDEVPRIVATE\n");
             break;
         case (SIOCDEVPRIVATE+1):
-            AICWFDBG(LOGINFO, "IOCTL PRIVATE\n");
+            AICWFDBG(LOGTRACE, "IOCTL PRIVATE\n");
             ret = android_priv_cmd(net, req, cmd);
 	    break;
         default:
@@ -2640,7 +2640,7 @@ static int rwnx_set_mac_address(struct net_device *dev, void *addr)
     struct sockaddr *sa = addr;
     int ret;
     struct rwnx_vif *rwnx_vif = netdev_priv(dev);
-	printk("%s enter \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter \r\n", __func__);
 
     ret = eth_mac_addr(dev, sa);
     memcpy(rwnx_vif->wdev.address, dev->dev_addr, 6);
@@ -2725,7 +2725,7 @@ static struct rwnx_vif *rwnx_interface_add(struct rwnx_hw *rwnx_hw,
 		    nx_nb_ndev_txq = NX_NB_NDEV_TXQ_FOR_OLD_IC;
     }
 
-	AICWFDBG(LOGINFO, "rwnx_interface_add: %s, %d, %d\r\n", name, type, NL80211_IFTYPE_P2P_DEVICE);
+	AICWFDBG(LOGTRACE, "rwnx_interface_add: %s, %d, %d\r\n", name, type, NL80211_IFTYPE_P2P_DEVICE);
     // Look for an available VIF
     if (type == NL80211_IFTYPE_AP_VLAN) {
         min_idx = NX_VIRT_DEV_MAX;
@@ -2861,7 +2861,7 @@ static struct rwnx_vif *rwnx_interface_add(struct rwnx_hw *rwnx_hw,
 #endif
     }
 
-	AICWFDBG(LOGINFO, "interface add:%x %x %x %x %x %x\n", vif->wdev.address[0], vif->wdev.address[1],
+	AICWFDBG(LOGTRACE, "interface add:%x %x %x %x %x %x\n", vif->wdev.address[0], vif->wdev.address[1],
         vif->wdev.address[2], vif->wdev.address[3], vif->wdev.address[4], vif->wdev.address[5]);
 
     if (params) {
@@ -2972,7 +2972,7 @@ static struct wireless_dev *rwnx_virtual_interface_add(struct rwnx_hw *rwnx_hw,
     int vif_idx = -1;
     int i;
 
-    AICWFDBG(LOGINFO, "rwnx_virtual_interface_add: %d, %s\n", type, name);
+    AICWFDBG(LOGTRACE, "rwnx_virtual_interface_add: %d, %s\n", type, name);
 
     if (type == NL80211_IFTYPE_AP_VLAN) {
         min_idx = NX_VIRT_DEV_MAX;
@@ -3003,7 +3003,7 @@ static struct wireless_dev *rwnx_virtual_interface_add(struct rwnx_hw *rwnx_hw,
     wdev->wiphy = rwnx_hw->wiphy;
     wdev->iftype = type;
 
-    AICWFDBG(LOGINFO, "rwnx_virtual_interface_add, ifname=%s, wdev=%p, vif_idx=%d\n", name, wdev, vif_idx);
+    AICWFDBG(LOGTRACE, "rwnx_virtual_interface_add, ifname=%s, wdev=%p, vif_idx=%d\n", name, wdev, vif_idx);
 
     #ifndef CONFIG_USE_P2P0
     vif->is_p2p_vif = 1;
@@ -3145,7 +3145,7 @@ static int rwnx_cfg80211_del_iface(struct wiphy *wiphy, struct wireless_dev *wde
 #endif
    // printk("del_iface: %p\n",wdev);
 
-	AICWFDBG(LOGINFO, "del_iface: %p, %x\n",wdev, wdev->address[5]);
+	AICWFDBG(LOGTRACE, "del_iface: %p, %x\n",wdev, wdev->address[5]);
 
     if (!dev || !rwnx_vif->ndev) {
 #if 0
@@ -3168,7 +3168,7 @@ static int rwnx_cfg80211_del_iface(struct wiphy *wiphy, struct wireless_dev *wde
     netdev_info(dev, "Remove Interface");
 #endif
 
-	AICWFDBG(LOGINFO, "%s Remove Interface \r\n", dev->name);
+	AICWFDBG(LOGTRACE, "%s Remove Interface \r\n", dev->name);
     if (dev->reg_state == NETREG_REGISTERED) {
         /* Will call rwnx_close if interface is UP */
         unregister_netdevice(dev);
@@ -3204,7 +3204,7 @@ static int rwnx_cfg80211_change_iface(struct wiphy *wiphy,
     int ret;
 
     RWNX_DBG(RWNX_FN_ENTRY_STR);
-	AICWFDBG(LOGINFO, "change_if: %d to %d, %d, %d", vif->wdev.iftype, type, NL80211_IFTYPE_P2P_CLIENT, NL80211_IFTYPE_STATION);
+	AICWFDBG(LOGTRACE, "change_if: %d to %d, %d, %d", vif->wdev.iftype, type, NL80211_IFTYPE_P2P_CLIENT, NL80211_IFTYPE_STATION);
 
 #ifdef CONFIG_COEX
     if (type == NL80211_IFTYPE_AP || type == NL80211_IFTYPE_P2P_GO)
@@ -3289,7 +3289,7 @@ static int rwnx_cfg80211_change_iface(struct wiphy *wiphy,
 	        return ret;
 	    }
 		vif->rwnx_hw->vif_table[vif->vif_index] = NULL;
-		AICWFDBG(LOGINFO, "change_if from %d \n", vif->vif_index);
+		AICWFDBG(LOGTRACE, "change_if from %d \n", vif->vif_index);
 	    if ((ret = rwnx_send_add_if(vif->rwnx_hw, vif->wdev.address,
 	                                              RWNX_VIF_TYPE(vif), p2p, &add_if_cfm))) {
 	            AICWFDBG(LOGERROR, "add if fail\n");
@@ -3300,7 +3300,7 @@ static int rwnx_cfg80211_change_iface(struct wiphy *wiphy,
 	            return -EIO;
 	    }
 
-		AICWFDBG(LOGINFO, "change_if to %d \n", add_if_cfm.inst_nbr);
+		AICWFDBG(LOGTRACE, "change_if to %d \n", add_if_cfm.inst_nbr);
 	    /* Save the index retrieved from LMAC */
 	    spin_lock_bh(&vif->rwnx_hw->cb_lock);
 	    vif->vif_index = add_if_cfm.inst_nbr;
@@ -3328,7 +3328,7 @@ static int rwnx_cfgp2p_start_p2p_device(struct wiphy *wiphy, struct wireless_dev
     int ret = 0;
 
     //do nothing
-    AICWFDBG(LOGINFO, "P2P interface started\n");
+    AICWFDBG(LOGTRACE, "P2P interface started\n");
 
     return ret;
 }
@@ -3400,7 +3400,7 @@ static void rwnx_cfgp2p_stop_p2p_device(struct wiphy *wiphy, struct wireless_dev
 		}
 	}
 
-	AICWFDBG(LOGINFO, "Exit. P2P interface stopped\n");
+	AICWFDBG(LOGTRACE, "Exit. P2P interface stopped\n");
 
     return;
 }
@@ -3455,19 +3455,19 @@ static int rwnx_cfg80211_scan(struct wiphy *wiphy,
 #ifndef CONFIG_STA_SCAN_WHEN_P2P_WORKING
 	if (p2p_working && RWNX_VIF_TYPE(rwnx_vif) != NL80211_IFTYPE_P2P_CLIENT &&
 		!rwnx_send_check_p2p(request)) {
-		AICWFDBG(LOGINFO, "p2p is working, scan abort\n");
+		AICWFDBG(LOGTRACE, "p2p is working, scan abort\n");
 		return -EBUSY;
 	}
 #endif
     if (scanning) {
-		AICWFDBG(LOGINFO, "is scanning, abort\n");
+		AICWFDBG(LOGTRACE, "is scanning, abort\n");
         if ((error =  rwnx_send_scanu_cancel_req(rwnx_hw, NULL)))
             return error;
         msleep(150);
     }
 
 	if((RWNX_VIF_TYPE(rwnx_vif) == NL80211_IFTYPE_STATION ||RWNX_VIF_TYPE(rwnx_vif) == NL80211_IFTYPE_P2P_CLIENT) &&  rwnx_vif->sta.external_auth){
-		AICWFDBG(LOGINFO, "scan about: external auth\r\n");
+		AICWFDBG(LOGTRACE, "scan about: external auth\r\n");
 		return -EBUSY;
 	}
 
@@ -3713,15 +3713,15 @@ static int rwnx_cfg80211_connect(struct wiphy *wiphy, struct net_device *dev,
                     else
                         sme->auth_type = NL80211_AUTHTYPE_SHARED_KEY;
             }
-			AICWFDBG(LOGINFO, "auto: use sme->auth_type = %d\r\n", sme->auth_type);
+			AICWFDBG(LOGTRACE, "auto: use sme->auth_type = %d\r\n", sme->auth_type);
         } else {
             if (rwnx_vif->wep_enabled && rwnx_vif->wep_auth_err && (sme->auth_type == rwnx_vif->last_auth_type)) {
                 if(sme->auth_type == NL80211_AUTHTYPE_SHARED_KEY) {
                     sme->auth_type = NL80211_AUTHTYPE_OPEN_SYSTEM;
-					AICWFDBG(LOGINFO, "start connect, auth_type changed, shared --> open\n");
+					AICWFDBG(LOGTRACE, "start connect, auth_type changed, shared --> open\n");
                 } else if(sme->auth_type == NL80211_AUTHTYPE_OPEN_SYSTEM) {
                     sme->auth_type = NL80211_AUTHTYPE_SHARED_KEY;
-					AICWFDBG(LOGINFO, "start connect, auth_type changed, open --> shared\n");
+					AICWFDBG(LOGTRACE, "start connect, auth_type changed, open --> shared\n");
                 }
             }
         }
@@ -3795,7 +3795,7 @@ static int rwnx_cfg80211_disconnect(struct wiphy *wiphy, struct net_device *dev,
     struct rwnx_vif *rwnx_vif = netdev_priv(dev);
 
     RWNX_DBG(RWNX_FN_ENTRY_STR);
-	AICWFDBG(LOGINFO, "%s drv_vif_index:%d disconnect reason:%d \r\n",
+	AICWFDBG(LOGTRACE, "%s drv_vif_index:%d disconnect reason:%d \r\n",
 		__func__, rwnx_vif->drv_vif_index, reason_code);
 
 #if 0
@@ -3816,7 +3816,7 @@ static int rwnx_cfg80211_disconnect(struct wiphy *wiphy, struct net_device *dev,
 	}
 #endif
 	if(atomic_read(&rwnx_vif->drv_conn_state) == RWNX_DRV_STATUS_CONNECTING){
-		AICWFDBG(LOGINFO, "%s call cfg80211_connect_result reason:%d \r\n",
+		AICWFDBG(LOGTRACE, "%s call cfg80211_connect_result reason:%d \r\n",
 			__func__, reason_code);
 		msleep(500);
 	}
@@ -4075,11 +4075,11 @@ static int rwnx_cfg80211_del_station_compat(struct wiphy *wiphy,
 			}
 			else if ((rwnx_vif->wdev.iftype == NL80211_IFTYPE_AP) || (rwnx_vif->wdev.iftype == NL80211_IFTYPE_P2P_GO)){
 				macaddr = cur->mac_addr;
-				AICWFDBG(LOGINFO, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0],macaddr[1],macaddr[2], \
+				AICWFDBG(LOGTRACE, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0],macaddr[1],macaddr[2], \
 									   macaddr[3],macaddr[4],macaddr[5]);
 				list_for_each_entry_safe(reord_info, reord_tmp,
 					&rx_priv->stas_reord_list, list) {
-                    			AICWFDBG(LOGINFO, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0],reord_info->mac_addr[1],reord_info->mac_addr[2], \
+                    			AICWFDBG(LOGTRACE, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0],reord_info->mac_addr[1],reord_info->mac_addr[2], \
 										   reord_info->mac_addr[3],reord_info->mac_addr[4],reord_info->mac_addr[5]);
 					if (!memcmp(reord_info->mac_addr, macaddr, 6)) {
 						reord_deinit_sta(rx_priv, reord_info);
@@ -4143,7 +4143,7 @@ void apm_staloss_work_process(struct work_struct *work)
         }
     }
 
-	AICWFDBG(LOGINFO, "apm vif idx=%d, found=%d, mac addr=%pM\n", rwnx_hw->apm_vif_idx, found, mac);
+	AICWFDBG(LOGTRACE, "apm vif idx=%d, found=%d, mac addr=%pM\n", rwnx_hw->apm_vif_idx, found, mac);
     if (!found || !rwnx_vif || (RWNX_VIF_TYPE(rwnx_vif) != NL80211_IFTYPE_AP && RWNX_VIF_TYPE(rwnx_vif) != NL80211_IFTYPE_P2P_GO))
     {
         return;
@@ -4192,12 +4192,12 @@ void apm_staloss_work_process(struct work_struct *work)
 			BUG();//should be other function
 		} else if ((rwnx_vif->wdev.iftype == NL80211_IFTYPE_AP) || (rwnx_vif->wdev.iftype == NL80211_IFTYPE_P2P_GO)) {
 			macaddr = cur->mac_addr;
-			AICWFDBG(LOGINFO, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0], macaddr[1], macaddr[2], \
+			AICWFDBG(LOGTRACE, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0], macaddr[1], macaddr[2], \
 								   macaddr[3], macaddr[4], macaddr[5]);
 			//spin_lock_bh(&rx_priv->stas_reord_lock);
 			list_for_each_entry_safe(reord_info, reord_tmp,
 				&rx_priv->stas_reord_list, list) {
-				AICWFDBG(LOGINFO, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0], reord_info->mac_addr[1], reord_info->mac_addr[2], \
+				AICWFDBG(LOGTRACE, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0], reord_info->mac_addr[1], reord_info->mac_addr[2], \
 									   reord_info->mac_addr[3], reord_info->mac_addr[4], reord_info->mac_addr[5]);
 				if (!memcmp(reord_info->mac_addr, macaddr, 6)) {
 					reord_deinit_sta(rx_priv, reord_info);
@@ -4224,7 +4224,7 @@ void apm_staloss_work_process(struct work_struct *work)
 		rwnx_dbgfs_unregister_rc_stat(rwnx_hw, cur);
 #endif
 	}else {
-		printk("sta not found: %pM\n", mac);
+		AICWFDBG(LOGERROR, "sta not found: %pM\n", mac);
 		return;
 	}
 
@@ -4252,7 +4252,7 @@ void apm_probe_sta_work_process(struct work_struct *work)
        }
 	   spin_unlock_bh(&rwnx_vif->rwnx_hw->cb_lock);
 
-       printk("sta %pM found = %d\n", mac, found);
+       AICWFDBG(LOGTRACE, "sta %pM found = %d\n", mac, found);
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 17, 0)
        if(found)
                cfg80211_probe_status(rwnx_vif->ndev, mac, (u64)rwnx_vif->sta_probe.probe_id, 1, 0, false, GFP_ATOMIC);
@@ -4950,7 +4950,7 @@ rwnx_cfg80211_remain_on_channel(struct wiphy *wiphy,
 
 #if 1// Fix up layer to send 50ms duration.
 	if(duration < 100){
-		AICWFDBG(LOGINFO, "%s duration time change to 200ms \r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s duration time change to 200ms \r\n", __func__);
 		duration = 200;
 	}
 #endif
@@ -4971,9 +4971,9 @@ rwnx_cfg80211_remain_on_channel(struct wiphy *wiphy,
         }
     }
 	//msleep(500);
-	AICWFDBG(LOGINFO, "remain:%d,%d,%d,duration:%d\n",rwnx_vif->vif_index, rwnx_vif->is_p2p_vif, rwnx_hw->is_p2p_alive, duration);
+	AICWFDBG(LOGTRACE, "remain:%d,%d,%d,duration:%d\n",rwnx_vif->vif_index, rwnx_vif->is_p2p_vif, rwnx_hw->is_p2p_alive, duration);
 	//if (rwnx_vif->is_p2p_vif) {
-	AICWFDBG(LOGINFO, "rwnx_vif:%p rwnx_hw->p2p_dev_vif:%p rwnx_vif->up:%d\r\n",
+	AICWFDBG(LOGTRACE, "rwnx_vif:%p rwnx_hw->p2p_dev_vif:%p rwnx_vif->up:%d\r\n",
 			rwnx_vif, rwnx_hw->p2p_dev_vif, rwnx_vif->up);
 #ifdef CONFIG_USE_P2P0
     if (rwnx_vif->is_p2p_vif) {
@@ -5310,7 +5310,7 @@ static int rwnx_cfg80211_mgmt_tx(struct wiphy *wiphy, struct wireless_dev *wdev,
         u64 cookie;
         int error;
 
-		AICWFDBG(LOGINFO, "mgmt rx remain on chan\n");
+		AICWFDBG(LOGTRACE, "mgmt rx remain on chan\n");
 
         /* Start a ROC procedure for 30ms */
 #if (LINUX_VERSION_CODE >= KERNEL_VERSION(3, 8, 0))
@@ -5574,13 +5574,13 @@ rwnx_cfg80211_tdls_mgmt(struct wiphy *wiphy,
     if ((action_code == WLAN_TDLS_SETUP_REQUEST) &&
         (rwnx_vif->sta.tdls_sta) &&
         (rwnx_vif->tdls_status == TDLS_LINK_ACTIVE)) {
-        printk("%s: only one TDLS link is supported!\n", __func__);
+        AICWFDBG(LOGERROR, "%s: only one TDLS link is supported!\n", __func__);
         return -ENOTSUPP;
     }
 
     if ((action_code == WLAN_TDLS_DISCOVERY_REQUEST) &&
         (rwnx_hw->mod_params->ps_on)) {
-        printk("%s: discovery request is not supported when "
+        AICWFDBG(LOGERROR, "%s: discovery request is not supported when "
                 "power-save is enabled!\n", __func__);
         return -ENOTSUPP;
     }
@@ -5591,7 +5591,7 @@ rwnx_cfg80211_tdls_mgmt(struct wiphy *wiphy,
         if ((status_code == 0) &&
             (rwnx_vif->sta.tdls_sta) &&
             (rwnx_vif->tdls_status == TDLS_LINK_ACTIVE)) {
-            printk("%s: only one TDLS link is supported!\n", __func__);
+            AICWFDBG(LOGERROR, "%s: only one TDLS link is supported!\n", __func__);
             status_code = WLAN_STATUS_REQUEST_DECLINED;
         }
         /* fall-through */
@@ -5605,7 +5605,7 @@ rwnx_cfg80211_tdls_mgmt(struct wiphy *wiphy,
         break;
 
     default:
-        printk("%s: Unknown TDLS mgmt/action frame %pM\n",
+        AICWFDBG(LOGERROR, "%s: Unknown TDLS mgmt/action frame %pM\n",
                 __func__, peer);
         ret = -EOPNOTSUPP;
         break;
@@ -5647,7 +5647,7 @@ rwnx_cfg80211_tdls_oper(struct wiphy *wiphy,
         return 0;
 
     if (!rwnx_vif->sta.tdls_sta) {
-        printk("%s: TDLS station %pM does not exist\n", __func__, peer);
+        AICWFDBG(LOGERROR, "%s: TDLS station %pM does not exist\n", __func__, peer);
         return -ENOLINK;
     }
 
@@ -5708,12 +5708,12 @@ rwnx_cfg80211_tdls_channel_switch(struct wiphy *wiphy,
     int error;
 
     if ((!rwnx_sta) || (memcmp(addr, rwnx_sta->mac_addr, ETH_ALEN))) {
-        printk("%s: TDLS station %pM doesn't exist\n", __func__, addr);
+        AICWFDBG(LOGERROR, "%s: TDLS station %pM doesn't exist\n", __func__, addr);
         return -ENOLINK;
     }
 
     if (!rwnx_sta->tdls.chsw_allowed) {
-        printk("%s: TDLS station %pM does not support TDLS channel switch\n", __func__, addr);
+        AICWFDBG(LOGERROR, "%s: TDLS station %pM does not support TDLS channel switch\n", __func__, addr);
         return -ENOTSUPP;
     }
 
@@ -5727,7 +5727,7 @@ rwnx_cfg80211_tdls_channel_switch(struct wiphy *wiphy,
         rwnx_sta->tdls.chsw_en = true;
         return 0;
     } else {
-        printk("%s: TDLS channel switch already enabled and only one is supported\n", __func__);
+        AICWFDBG(LOGERROR, "%s: TDLS channel switch already enabled and only one is supported\n", __func__);
         return -EALREADY;
     }
 }
@@ -8490,7 +8490,7 @@ static void system_config_8800(struct rwnx_hw *rwnx_hw){
     }
     chip_sub_id = (u8)(rd_mem_addr_cfm.memdata >> 4);
     //printk("%x=%x\n", rd_mem_addr_cfm.memaddr, rd_mem_addr_cfm.memdata);
-	AICWFDBG(LOGINFO, "chip_id=%x, chip_sub_id=%x\n", chip_id, chip_sub_id);
+	AICWFDBG(LOGTRACE, "chip_id=%x, chip_sub_id=%x\n", chip_id, chip_sub_id);
 
 
 #ifdef CONFIG_PMIC_SETTING
@@ -8510,7 +8510,7 @@ static void system_config_8800(struct rwnx_hw *rwnx_hw){
 	if(rwnx_hw->usbdev->chipid == PRODUCT_ID_AIC8801){
 	    if ((chip_id == CHIP_REV_U03) && (chip_sub_id == CHIP_SUB_REV_U04)) {
 	        syscfg_num = sizeof(syscfg_tbl_u04) / sizeof(u32) / 2;
-			AICWFDBG(LOGINFO, "cfg u04\n");
+			AICWFDBG(LOGTRACE, "cfg u04\n");
 	        for (cnt = 0; cnt < syscfg_num; cnt++) {
 	            ret = rwnx_send_dbg_mem_write_req(rwnx_hw, syscfg_tbl_u04[cnt][0], syscfg_tbl_u04[cnt][1]);
 	            if (ret) {
@@ -8564,7 +8564,7 @@ static int wdt_config(struct rwnx_hw *rwnx_hw)
     ret = rwnx_send_dbg_mem_mask_write_req(rwnx_hw,
                 wdt_tbl_masked[0][0], wdt_tbl_masked[0][1], wdt_tbl_masked[0][2]);
     if (ret) {
-        printk("wdt config %x write fail: %d\n", wdt_tbl_masked[0][0], ret);
+        AICWFDBG(LOGERROR, "wdt config %x write fail: %d\n", wdt_tbl_masked[0][0], ret);
     }
     return ret;
 }
@@ -8575,7 +8575,7 @@ static void rf_config(struct rwnx_hw *rwnx_hw)
     ret = rwnx_send_dbg_mem_mask_write_req(rwnx_hw,
                 rf_tbl_masked[0][0], rf_tbl_masked[0][1], rf_tbl_masked[0][2]);
     if (ret) {
-        printk("rf config %x write fail: %d\n", rf_tbl_masked[0][0], ret);
+        AICWFDBG(LOGERROR, "rf config %x write fail: %d\n", rf_tbl_masked[0][0], ret);
     }
 }
 #endif
@@ -8593,14 +8593,14 @@ static int start_from_bootrom(struct rwnx_hw *rwnx_hw)
 	const u32 fw_addr = RAM_FMAC_FW_ADDR;
 #endif
 	struct dbg_mem_read_cfm rd_cfm;
-	printk("Read FW mem: %08x\n", rd_addr);
+	AICWFDBG(LOGTRACE, "Read FW mem: %08x\n", rd_addr);
 	if ((ret = rwnx_send_dbg_mem_read_req(rwnx_hw, rd_addr, &rd_cfm))) {
 		return -1;
 	}
-	printk("cfm: [%08x] = %08x\n", rd_cfm.memaddr, rd_cfm.memdata);
+	AICWFDBG(LOGTRACE, "cfm: [%08x] = %08x\n", rd_cfm.memaddr, rd_cfm.memdata);
 
 	/* fw start */
-	printk("Start app: %08x\n", fw_addr);
+	AICWFDBG(LOGTRACE, "Start app: %08x\n", fw_addr);
 	if ((ret = rwnx_send_dbg_start_app_req(rwnx_hw, fw_addr, HOST_START_APP_AUTO))) {
 		return -1;
 	}
@@ -8638,7 +8638,7 @@ static void aicwf_usb_config(struct rwnx_hw *rwnx_hw)
         AICWFDBG(LOGERROR, "%x rd fail: %d\n", mem_addr, ret);
         return;
     }
-    AICWFDBG(LOGINFO, "usb config read %x\n", rd_mem_addr_cfm.memdata);
+    AICWFDBG(LOGTRACE, "usb config read %x\n", rd_mem_addr_cfm.memdata);
     if ((rd_mem_addr_cfm.memdata & 0xffff) == 0x119) {
         cnt = 0;
         usbcfg_num = sizeof(usbcfg_tbl) / sizeof(u32) / 2;
@@ -8667,11 +8667,11 @@ static int start_from_bootrom(struct rwnx_hw *rwnx_hw)
 #endif
     u32 boot_type;
 	struct dbg_mem_read_cfm rd_cfm;
-	AICWFDBG(LOGINFO, "Read FW mem: %08x\n", rd_addr);
+	AICWFDBG(LOGTRACE, "Read FW mem: %08x\n", rd_addr);
 	if ((ret = rwnx_send_dbg_mem_read_req(rwnx_hw, rd_addr, &rd_cfm))) {
 		return -1;
 	}
-	AICWFDBG(LOGINFO, "cfm: [%08x] = %08x\n", rd_cfm.memaddr, rd_cfm.memdata);
+	AICWFDBG(LOGTRACE, "cfm: [%08x] = %08x\n", rd_cfm.memaddr, rd_cfm.memdata);
 
     if (testmode == 0) {
         boot_type = HOST_START_APP_DUMMY;
@@ -8679,7 +8679,7 @@ static int start_from_bootrom(struct rwnx_hw *rwnx_hw)
         boot_type = HOST_START_APP_AUTO;
     }
 	/* fw start */
-	AICWFDBG(LOGINFO, "Start app: %08x, %d\n", fw_addr, boot_type);
+	AICWFDBG(LOGTRACE, "Start app: %08x, %d\n", fw_addr, boot_type);
 	if ((ret = rwnx_send_dbg_start_app_req(rwnx_hw, fw_addr, boot_type))) {
 		return -1;
 	}
@@ -8777,7 +8777,7 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
 #endif
 //#endif
     /* create a new wiphy for use with cfg80211 */
-    AICWFDBG(LOGINFO, "%s sizeof(struct rwnx_hw):%d \r\n", __func__, (int)sizeof(struct rwnx_hw));
+    AICWFDBG(LOGTRACE, "%s sizeof(struct rwnx_hw):%d \r\n", __func__, (int)sizeof(struct rwnx_hw));
     wiphy = wiphy_new(&rwnx_cfg80211_ops, sizeof(struct rwnx_hw));
 
     if (!wiphy) {
@@ -8889,12 +8889,12 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
         goto err_lmac_reqs;
     }
 
-	AICWFDBG(LOGINFO, "is 5g support = %d, vendor_info = 0x%02X\n", set_start_cfm.is_5g_support, set_start_cfm.vendor_info);
+	AICWFDBG(LOGTRACE, "is 5g support = %d, vendor_info = 0x%02X\n", set_start_cfm.is_5g_support, set_start_cfm.vendor_info);
 	rwnx_hw->band_5g_support = set_start_cfm.is_5g_support;
 
     ret = rwnx_send_get_fw_version_req(rwnx_hw, &fw_version);
     memcpy(wiphy->fw_version, fw_version.fw_version, fw_version.fw_version_len>32? 32 : fw_version.fw_version_len>32);
-	AICWFDBG(LOGINFO, "Firmware Version: %s\r\n", fw_version.fw_version);
+	AICWFDBG(LOGTRACE, "Firmware Version: %s\r\n", fw_version.fw_version);
 
     wiphy->bands[NL80211_BAND_2GHZ] = &rwnx_band_2GHz;
 //#ifdef USE_5G
@@ -8917,7 +8917,7 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
 
 #ifdef CONFIG_GPIO_WAKEUP
 		/* Set WoWLAN flags */
-		printk("%s Wowlan support\r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s Wowlan support\r\n", __func__);
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 11, 0)
 		wiphy->wowlan = &aic_wowlan_support;
 #else
@@ -8991,7 +8991,7 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
     	AICWFDBG(LOGERROR, "no mac address in efuse!");
 	}
 
-	AICWFDBG(LOGINFO, "get macaddr:%x,%x\r\n", mac_addr_efuse[0], mac_addr_efuse[5]);
+	AICWFDBG(LOGTRACE, "get macaddr:%x,%x\r\n", mac_addr_efuse[0], mac_addr_efuse[5]);
 
     memcpy(wiphy->perm_addr, init_conf.mac_addr, ETH_ALEN);
 
@@ -9071,7 +9071,7 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
     wiphy_info(wiphy, "New interface create %s", vif->ndev->name);
 #endif
 
-	AICWFDBG(LOGINFO, "New interface create %s\r\n", vif->ndev->name);
+	AICWFDBG(LOGTRACE, "New interface create %s\r\n", vif->ndev->name);
 
 #ifdef  CONFIG_USE_P2P0
 
@@ -9089,7 +9089,7 @@ if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) ||
         }
 
         //wiphy_info(wiphy, "New interface create %s", vif->ndev->name);
-        AICWFDBG(LOGINFO, "New interface create %s \r\n", vif->ndev->name);
+        AICWFDBG(LOGTRACE, "New interface create %s \r\n", vif->ndev->name);
 
 #if LINUX_VERSION_CODE < KERNEL_VERSION(4, 15, 0)
         init_timer(&rwnx_hw->p2p_alive_timer);
@@ -9150,7 +9150,7 @@ void rwnx_cfg80211_deinit(struct rwnx_hw *rwnx_hw)
 
     rwnx_wdev_unregister(rwnx_hw);
 	if(rwnx_hw->wiphy){
-		AICWFDBG(LOGINFO, "%s wiphy_unregister \r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s wiphy_unregister \r\n", __func__);
     	wiphy_unregister(rwnx_hw->wiphy);
 	}
     rwnx_radar_detection_deinit(&rwnx_hw->radar);
@@ -9191,7 +9191,7 @@ static int __init rwnx_mod_init(void)
 
     RWNX_DBG(RWNX_FN_ENTRY_STR);
     rwnx_print_version();
-	AICWFDBG(LOGINFO, "RELEASE DATE:%s \r\n", RELEASE_DATE);
+	AICWFDBG(LOGTRACE, "RELEASE DATE:%s \r\n", RELEASE_DATE);
 	rwnx_init_cmd_array();
 
 	sema_init(&aicwf_deinit_sem, 1);
@@ -9240,7 +9240,7 @@ static void __exit rwnx_mod_exit(void)
     aicwf_usb_exit();
 #endif
 	rwnx_free_cmd_array();
-	AICWFDBG(LOGINFO, "%s exit\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s exit\r\n", __func__);
 }
 
 module_init(rwnx_mod_init);
diff --git a/aic8800_fdrv/rwnx_mod_params.c b/aic8800_fdrv/rwnx_mod_params.c
index fbb500b..5fc99f5 100644
--- a/aic8800_fdrv/rwnx_mod_params.c
+++ b/aic8800_fdrv/rwnx_mod_params.c
@@ -320,7 +320,7 @@ void rwnx_get_countrycode_channels(struct wiphy *wiphy,
 #ifdef CONFIG_USE_WIRELESS_EXT
 	rwnx_hw->support_freqs_number = support_freqs_counter;
 #endif
-	AICWFDBG(LOGINFO, "%s support channel:%s\r\n", __func__, ccode_channels);
+	AICWFDBG(LOGTRACE, "%s support channel:%s\r\n", __func__, ccode_channels);
 }
 
 
@@ -335,7 +335,7 @@ struct ieee80211_regdomain *getRegdomainFromRwnxDBIndex(struct wiphy *wiphy,
 	country_code[0] = reg_regdb[idx]->alpha2[0];
 	country_code[1] = reg_regdb[idx]->alpha2[1];
 
-	AICWFDBG(LOGINFO, "%s set ccode:%s \r\n", __func__, country_code);
+	AICWFDBG(LOGTRACE, "%s set ccode:%s \r\n", __func__, country_code);
 
 	rwnx_get_countrycode_channels(wiphy, reg_regdb[idx]);
 
@@ -351,7 +351,7 @@ struct ieee80211_regdomain *getRegdomainFromRwnxDB(struct wiphy *wiphy,
 
 	memset(country_code, 0, 4);
 	
-	AICWFDBG(LOGINFO, "%s set ccode:%s \r\n", __func__, alpha2);
+	AICWFDBG(LOGTRACE, "%s set ccode:%s \r\n", __func__, alpha2);
 
 	idx = 0;
 
@@ -370,7 +370,7 @@ struct ieee80211_regdomain *getRegdomainFromRwnxDB(struct wiphy *wiphy,
 
 	AICWFDBG(LOGERROR, "%s(): Error, wrong country = %s\n",
 			__func__, alpha2);
-	AICWFDBG(LOGINFO, "Set as default 00\n");
+	AICWFDBG(LOGTRACE, "Set as default 00\n");
 
 	memcpy(country_code, default_ccode, sizeof(default_ccode));
 	rwnx_get_countrycode_channels(wiphy, reg_regdb[0]);
@@ -786,7 +786,7 @@ static void rwnx_set_vht_capa(struct rwnx_hw *rwnx_hw, struct wiphy *wiphy)
 			rwnx_hw->vht_cap_2G.vht_mcs.rx_mcs_map |= cpu_to_le16(mcs_map << (i*2));
 			rwnx_hw->vht_cap_2G.vht_mcs.rx_highest = MAX_VHT_RATE(mcs_map, nss, bw_max);
 			mcs_map = IEEE80211_VHT_MCS_SUPPORT_0_7;
-			printk("lemon map=%x\n", rwnx_hw->vht_cap_2G.vht_mcs.rx_mcs_map);
+			AICWFDBG(LOGTRACE, "lemon map=%x\n", rwnx_hw->vht_cap_2G.vht_mcs.rx_mcs_map);
 		}
 		for (; i < 8; i++) {
 			rwnx_hw->vht_cap_2G.vht_mcs.rx_mcs_map |= cpu_to_le16(
@@ -814,7 +814,7 @@ static void rwnx_set_vht_capa(struct rwnx_hw *rwnx_hw, struct wiphy *wiphy)
 		}
 
 			rwnx_hw->vht_cap_2G.cap |= IEEE80211_VHT_CAP_MAX_A_MPDU_LENGTH_EXPONENT_MASK;
-		printk("%s, vht_capa_info=0x%x\n", __func__, rwnx_hw->vht_cap_2G.cap);
+		AICWFDBG(LOGTRACE, "%s, vht_capa_info=0x%x\n", __func__, rwnx_hw->vht_cap_2G.cap);
 #ifdef USE_5G
 	if (rwnx_hw->band_5g_support) {
 	    rwnx_hw->vht_cap_5G.vht_supported = true;
@@ -1564,7 +1564,7 @@ static void rwnx_set_wiphy_params(struct rwnx_hw *rwnx_hw, struct wiphy *wiphy)
         // function, that needs to be called after wiphy registration
         memcpy(country_code, default_ccode, sizeof(default_ccode));
 		regdomain = getRegdomainFromRwnxDB(wiphy, default_ccode);
-        printk(KERN_CRIT
+        AICWFDBG(LOGERROR,
                "\n\n%s: CAUTION: USING PERMISSIVE CUSTOM REGULATORY RULES\n\n",
                __func__);
         wiphy->regulatory_flags |= REGULATORY_CUSTOM_REG;
@@ -1573,7 +1573,7 @@ static void rwnx_set_wiphy_params(struct rwnx_hw *rwnx_hw, struct wiphy *wiphy)
 #elif (LINUX_VERSION_CODE < KERNEL_VERSION(3, 14, 0))
         memcpy(country_code, default_ccode, sizeof(default_ccode));
 		regdomain = getRegdomainFromRwnxDB(wiphy, default_ccode);
-		printk(KERN_CRIT"%s: Registering custom regulatory\n", __func__);
+		AICWFDBG(LOGERROR,"%s: Registering custom regulatory\n", __func__);
 		wiphy->flags |= WIPHY_FLAG_CUSTOM_REGULATORY;
 		wiphy_apply_custom_regulatory(wiphy, regdomain);
 #endif
diff --git a/aic8800_fdrv/rwnx_msg_rx.c b/aic8800_fdrv/rwnx_msg_rx.c
index 5a86f62..a9042fe 100644
--- a/aic8800_fdrv/rwnx_msg_rx.c
+++ b/aic8800_fdrv/rwnx_msg_rx.c
@@ -838,7 +838,7 @@ static inline int rwnx_rx_sm_connect_ind(struct rwnx_hw *rwnx_hw,
 
 
     // Fill-in the AP information
-	AICWFDBG(LOGINFO, "%s ind->status_code:%d \r\n", __func__, ind->status_code);
+	AICWFDBG(LOGTRACE, "%s ind->status_code:%d \r\n", __func__, ind->status_code);
     if (ind->status_code == 0)
     {
         struct rwnx_sta *sta = &rwnx_hw->sta_table[ind->ap_idx];
@@ -939,7 +939,7 @@ static inline int rwnx_rx_sm_connect_ind(struct rwnx_hw *rwnx_hw,
     } else if (ind->status_code == WLAN_STATUS_NOT_SUPPORTED_AUTH_ALG) {
         if (rwnx_vif->wep_enabled) {
             rwnx_vif->wep_auth_err = true;
-            printk("con ind wep_auth_err %d\n", rwnx_vif->wep_auth_err);
+            AICWFDBG(LOGERROR, "con ind wep_auth_err %d\n", rwnx_vif->wep_auth_err);
         }
 		atomic_set(&rwnx_vif->drv_conn_state, (int)RWNX_DRV_STATUS_DISCONNECTED);
     }else{
@@ -947,7 +947,7 @@ static inline int rwnx_rx_sm_connect_ind(struct rwnx_hw *rwnx_hw,
 	}
 
 
-    AICWFDBG(LOGINFO, "%s ind->roamed:%d ind->status_code:%d rwnx_vif->drv_conn_state:%d\r\n", 
+    AICWFDBG(LOGTRACE, "%s ind->roamed:%d ind->status_code:%d rwnx_vif->drv_conn_state:%d\r\n", 
         __func__, 
         ind->roamed, 
         ind->status_code,
@@ -962,7 +962,7 @@ static inline int rwnx_rx_sm_connect_ind(struct rwnx_hw *rwnx_hw,
     }
     else {//roaming
         if(ind->status_code != 0){
-            AICWFDBG(LOGINFO, "%s roaming fail to notify disconnect \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s roaming fail to notify disconnect \r\n", __func__);
 			cfg80211_disconnected(dev, 0, NULL, 0,1, GFP_ATOMIC);
         }else{
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 12, 0)
@@ -984,11 +984,11 @@ static inline int rwnx_rx_sm_connect_ind(struct rwnx_hw *rwnx_hw,
         info.req_ie_len = ind->assoc_req_ie_len;
         info.resp_ie = rsp_ie;
         info.resp_ie_len = ind->assoc_rsp_ie_len;
-        AICWFDBG(LOGINFO, "%s roaming success to notify roam \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s roaming success to notify roam \r\n", __func__);
         cfg80211_roamed(dev, &info, GFP_ATOMIC);
 #else
         chan = ieee80211_get_channel(rwnx_hw->wiphy, ind->center_freq);
-        AICWFDBG(LOGINFO, "%s roaming success to notify roam \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s roaming success to notify roam \r\n", __func__);
         cfg80211_roamed(dev
 #if LINUX_VERSION_CODE > KERNEL_VERSION(2, 6, 39) || defined(COMPAT_KERNEL_RELEASE)
             , chan
@@ -1028,7 +1028,7 @@ void rwnx_cfg80211_unlink_bss(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif
 
 	if (bss) {
 		cfg80211_unlink_bss(wiphy, bss);
-		AICWFDBG(LOGINFO, "%s(): cfg80211_unlink %s!!\n", __func__, rwnx_vif->sta.ssid);
+		AICWFDBG(LOGTRACE, "%s(): cfg80211_unlink %s!!\n", __func__, rwnx_vif->sta.ssid);
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 9, 0)
 		cfg80211_put_bss(wiphy, bss);
 #else
@@ -1103,12 +1103,12 @@ static inline int rwnx_rx_sm_disconnect_ind(struct rwnx_hw *rwnx_hw,
 #endif
     if((rwnx_vif->wdev.iftype == NL80211_IFTYPE_STATION) || (rwnx_vif->wdev.iftype == NL80211_IFTYPE_P2P_CLIENT)) {
         macaddr = rwnx_vif->ndev->dev_addr;
-		AICWFDBG(LOGINFO, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0],macaddr[1],macaddr[2], \
+		AICWFDBG(LOGTRACE, "deinit:macaddr:%x,%x,%x,%x,%x,%x\r\n", macaddr[0],macaddr[1],macaddr[2], \
                                macaddr[3],macaddr[4],macaddr[5]);
         list_for_each_entry_safe(reord_info, tmp, &rx_priv->stas_reord_list, list) {
             macaddr = rwnx_vif->ndev->dev_addr;
-			AICWFDBG(LOGINFO, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0],reord_info->mac_addr[1],reord_info->mac_addr[2], \
-                                   reord_info->mac_addr[3],reord_info->mac_addr[4],reord_info->mac_addr[5]);
+		AICWFDBG(LOGTRACE, "reord_mac:%x,%x,%x,%x,%x,%x\r\n", reord_info->mac_addr[0],reord_info->mac_addr[1],reord_info->mac_addr[2], \
+                               reord_info->mac_addr[3],reord_info->mac_addr[4],reord_info->mac_addr[5]);
             if (!memcmp(reord_info->mac_addr, macaddr, 6)) {
                 reord_deinit_sta(rx_priv, reord_info);
                 break;
@@ -1523,7 +1523,7 @@ void rwnx_rx_handle_print(struct rwnx_hw *rwnx_hw, u8 *msg, u32 len)
         return;
     }
 
-    printk("FWLOG: %s", msg);
+    AICWFDBG(LOGTRACE, "FWLOG: %s", msg);
 
 #ifdef CONFIG_RWNX_DEBUGFS
     data_end = rwnx_hw->debugfs.fw_log.buf.dataend;
diff --git a/aic8800_fdrv/rwnx_msg_tx.c b/aic8800_fdrv/rwnx_msg_tx.c
index 859867e..beccedc 100644
--- a/aic8800_fdrv/rwnx_msg_tx.c
+++ b/aic8800_fdrv/rwnx_msg_tx.c
@@ -234,15 +234,15 @@ void rwnx_free_cmd_array(void){
 
 
 #if 0
-int rwnx_init_msg_array(void){
+	int rwnx_init_msg_array(void){
 
-	printk("%s enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter\r\n", __func__);
 	spin_lock_init(&msg_array_lock);
 
 	for(msg_array_index = 0; msg_array_index < RWNX_MSG_ARRAY_SIZE; msg_array_index++){
-		printk("%s msg_queue[%d]:%p \r\n", __func__, msg_array_index, &msg_array[msg_array_index]);
+		AICWFDBG(LOGTRACE, "%s msg_queue[%d]:%p \r\n", __func__, msg_array_index, &msg_array[msg_array_index]);
 	}
-	printk("%s exit\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s exit\r\n", __func__);
 
 	return 0;
 
@@ -251,28 +251,28 @@ int rwnx_init_msg_array(void){
 
 void rwnx_free_msg_array(void){
 
-	printk("%s enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter\r\n", __func__);
 #if 1
 	for(msg_array_index = 0; msg_array_index < RWNX_MSG_ARRAY_SIZE; msg_array_index++){
 		msg_array[msg_array_index].param_len = 0;
 	}
 #endif
-	printk("%s exit\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s exit\r\n", __func__);
 }
 
-struct lmac_msg *rwnx_msg_malloc_(void){
+	struct lmac_msg *rwnx_msg_malloc_(void){
 	struct lmac_msg *msg = NULL;
 
 
 	spin_lock(&msg_array_lock);
-	printk("%s enter\r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter\r\n", __func__);
 	for(msg_array_index = 0; msg_array_index < RWNX_MSG_ARRAY_SIZE; msg_array_index++){
 		if(msg_array[msg_array_index].param_len== 0){
-			printk("%s get msg_array[%d]:%p \r\n", __func__, msg_array_index, &msg_array[msg_array_index]);
+			AICWFDBG(LOGTRACE, "%s get msg_array[%d]:%p \r\n", __func__, msg_array_index, &msg_array[msg_array_index]);
 			msg = &msg_array[msg_array_index];
 			break;
 		}else{
-			printk("%s msg_array[%d] in used param_len= %d \r\n",
+			AICWFDBG(LOGTRACE, "%s msg_array[%d] in used param_len= %d \r\n",
 				__func__,
 				msg_array_index,
 				msg_array[msg_array_index].param_len);
@@ -280,7 +280,7 @@ struct lmac_msg *rwnx_msg_malloc_(void){
 	}
 
 	if(!msg){
-		printk("%s array is empty...\r\n", __func__);
+		AICWFDBG(LOGERROR, "%s array is empty...\r\n", __func__);
 	}
 	spin_unlock(&msg_array_lock);
 
@@ -290,7 +290,7 @@ struct lmac_msg *rwnx_msg_malloc_(void){
 void rwnx_msg_free_(struct lmac_msg *msg){
 
 	spin_lock(&msg_array_lock);
-	printk("%s enter \r\n", __func__);
+	AICWFDBG(LOGTRACE, "%s enter \r\n", __func__);
 
 	for(msg_array_index = 0; msg_array_index < RWNX_MSG_ARRAY_SIZE; msg_array_index++){
 		if(msg == &msg_array[msg_array_index]){
@@ -304,7 +304,7 @@ void rwnx_msg_free_(struct lmac_msg *msg){
 	msg->src_id = 0;
 	msg->param_len = 0;
 
-	printk("%s msg_array[%d]:%p \r\n", __func__, msg_array_index, msg);
+	AICWFDBG(LOGTRACE, "%s msg_array[%d]:%p \r\n", __func__, msg_array_index, msg);
 	spin_unlock(&msg_array_lock);
 }
 #endif
@@ -353,8 +353,8 @@ static inline void *rwnx_msg_zalloc(lmac_msg_id_t const id,
 
     msg = (struct lmac_msg *)kzalloc(sizeof(struct lmac_msg) + param_len,
                                      flags);
-    if (msg == NULL) {
-        printk(KERN_CRIT "%s: msg allocation failed\n", __func__);
+        if (msg == NULL) {
+        AICWFDBG(LOGERROR, "%s: msg allocation failed\n", __func__);
         return NULL;
     }
     msg->id = id;
@@ -418,7 +418,7 @@ static int rwnx_send_msg(struct rwnx_hw *rwnx_hw, const void *msg_params,
         reqid != MM_START_CFM && reqid != MM_SET_IDLE_CFM &&
         reqid != ME_CONFIG_CFM && reqid != MM_SET_PS_MODE_CFM &&
         reqid != ME_CHAN_CONFIG_CFM) {
-        printk(KERN_CRIT "%s: bypassing (RWNX_DEV_RESTARTING set) 0x%02x\n",
+        AICWFDBG(LOGERROR, "%s: bypassing (RWNX_DEV_RESTARTING set) 0x%02x\n",
                __func__, reqid);
         kfree(msg);
         return -EBUSY;
@@ -426,7 +426,7 @@ static int rwnx_send_msg(struct rwnx_hw *rwnx_hw, const void *msg_params,
     #endif
 #if 0
     else if (!rwnx_hw->ipc_env) {
-        printk(KERN_CRIT "%s: bypassing (restart must have failed)\n", __func__);
+        AICWFDBG(LOGERROR, "%s: bypassing (restart must have failed)\n", __func__);
         kfree(msg);
         return -EBUSY;
     }
@@ -1158,7 +1158,7 @@ int rwnx_send_rf_calib_req(struct rwnx_hw *rwnx_hw, struct mm_set_rf_calib_cfm *
 	get_userconfig_xtal_cap(&xtal_cap);
 
 	if (xtal_cap.enable) {
-		AICWFDBG(LOGINFO, "user xtal cap: %d, cap_fine: %d\n", xtal_cap.xtal_cap, xtal_cap.xtal_cap_fine);
+		AICWFDBG(LOGTRACE, "user xtal cap: %d, cap_fine: %d\n", xtal_cap.xtal_cap, xtal_cap.xtal_cap_fine);
 		rf_calib_req->xtal_cap = xtal_cap.xtal_cap;
 		rf_calib_req->xtal_cap_fine = xtal_cap.xtal_cap_fine;
 	} else {
@@ -1338,7 +1338,7 @@ int rwnx_send_vendor_hwconfig_req(struct rwnx_hw *rwnx_hw, uint32_t hwconfig_id,
 		req0->txop_bk = param[1];
 		req0->txop_vi = param[2];
 		req0->txop_vo = param[3];
-		printk("set_acs_txop_req: be: %x,bk: %x,vi: %x,vo: %x\n",
+		AICWFDBG(LOGTRACE, "set_acs_txop_req: be: %x,bk: %x,vi: %x,vo: %x\n",
                         req0->txop_be, req0->txop_bk, req0->txop_vi, req0->txop_vo);
 		/* Send the MM_SET_VENDOR_HWCONFIG_CFM  message to UMAC FW */
 		error = rwnx_send_msg(rwnx_hw, req0, 1, MM_SET_VENDOR_HWCONFIG_CFM, NULL);
@@ -1359,7 +1359,7 @@ int rwnx_send_vendor_hwconfig_req(struct rwnx_hw *rwnx_hw, uint32_t hwconfig_id,
 		req1->rts_en = param[6];
 		req1->long_nav_en = param[7];
 		req1->cfe_en = param[8];
-		printk("set_channel_access_req:edca[]= %x %x %x %x\nvif_idx: %x, retry_cnt: %x, rts_en: %x, long_nav_en: %x, cfe_en: %x\n",
+		AICWFDBG(LOGTRACE, "set_channel_access_req:edca[]= %x %x %x %x\nvif_idx: %x, retry_cnt: %x, rts_en: %x, long_nav_en: %x, cfe_en: %x\n",
 			req1->edca[0], req1->edca[1], req1->edca[2], req1->edca[3], req1->vif_idx, req1->retry_cnt, req1->rts_en, req1->long_nav_en, req1->cfe_en);
 		/* Send the MM_SET_VENDOR_HWCONFIG_CFM  message to UMAC FW */
 		error = rwnx_send_msg(rwnx_hw, req1, 1, MM_SET_VENDOR_HWCONFIG_CFM, NULL);
@@ -1377,7 +1377,7 @@ int rwnx_send_vendor_hwconfig_req(struct rwnx_hw *rwnx_hw, uint32_t hwconfig_id,
 		req2->rx_startdelay_ofdm = param[3];
 		req2->rx_startdelay_long = param[4];
 		req2->rx_startdelay_short = param[5];
-		printk("set_mac_timescale_req:sifsA_time: %x, sifsB_time: %x, slot_time: %x, rx_startdelay ofdm:%x long %x short %x\n",
+		AICWFDBG(LOGTRACE, "set_mac_timescale_req:sifsA_time: %x, sifsB_time: %x, slot_time: %x, rx_startdelay ofdm:%x long %x short %x\n",
 			req2->sifsA_time, req2->sifsB_time, req2->slot_time, req2->rx_startdelay_ofdm, req2->rx_startdelay_long, req2->rx_startdelay_short);
 		/* Send the MM_SET_VENDOR_HWCONFIG_CFM  message to UMAC FW */
 		error = rwnx_send_msg(rwnx_hw, req2, 1, MM_SET_VENDOR_HWCONFIG_CFM, NULL);
@@ -1394,7 +1394,7 @@ int rwnx_send_vendor_hwconfig_req(struct rwnx_hw *rwnx_hw, uint32_t hwconfig_id,
 		req3->cca20s_rise_th = param[2];
 		req3->cca20p_fall_th = param[3];
 		req3->cca20s_fall_th = param[4];
-		printk("cca_threshold_req: auto_cca_en:%d\ncca20p_rise_th = %d\ncca20s_rise_th = %d\ncca20p_fall_th = %d\ncca20s_fall_th = %d\n",
+		AICWFDBG(LOGTRACE, "cca_threshold_req: auto_cca_en:%d\ncca20p_rise_th = %d\ncca20s_rise_th = %d\ncca20p_fall_th = %d\ncca20s_fall_th = %d\n",
 			req3->auto_cca_en, req3->cca20p_rise_th, req3->cca20s_rise_th, req3->cca20p_fall_th, req3->cca20s_fall_th);
 		/* Send the MM_SET_VENDOR_HWCONFIG_CFM  message to UMAC FW */
 		error = rwnx_send_msg(rwnx_hw, req3, 1, MM_SET_VENDOR_HWCONFIG_CFM, NULL);
@@ -1456,16 +1456,16 @@ int rwnx_send_txpwr_idx_req(struct rwnx_hw *rwnx_hw)
 
 	get_userconfig_txpwr_idx(txpwr_idx);
 
-	AICWFDBG(LOGINFO, "%s:enable:%d\r\n", __func__, txpwr_idx->enable);
-	AICWFDBG(LOGINFO, "%s:dsss:%d\r\n", __func__, txpwr_idx->dsss);
-	AICWFDBG(LOGINFO, "%s:ofdmlowrate_2g4:%d\r\n", __func__, txpwr_idx->ofdmlowrate_2g4);
-	AICWFDBG(LOGINFO, "%s:ofdm64qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm64qam_2g4);
-	AICWFDBG(LOGINFO, "%s:ofdm256qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm256qam_2g4);
-	AICWFDBG(LOGINFO, "%s:ofdm1024qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm1024qam_2g4);
-	AICWFDBG(LOGINFO, "%s:ofdmlowrate_5g:%d\r\n", __func__, txpwr_idx->ofdmlowrate_5g);
-	AICWFDBG(LOGINFO, "%s:ofdm64qam_5g:%d\r\n", __func__, txpwr_idx->ofdm64qam_5g);
-	AICWFDBG(LOGINFO, "%s:ofdm256qam_5g:%d\r\n", __func__, txpwr_idx->ofdm256qam_5g);
-	AICWFDBG(LOGINFO, "%s:ofdm1024qam_5g:%d\r\n", __func__, txpwr_idx->ofdm1024qam_5g);
+	AICWFDBG(LOGTRACE, "%s:enable:%d\r\n", __func__, txpwr_idx->enable);
+	AICWFDBG(LOGTRACE, "%s:dsss:%d\r\n", __func__, txpwr_idx->dsss);
+	AICWFDBG(LOGTRACE, "%s:ofdmlowrate_2g4:%d\r\n", __func__, txpwr_idx->ofdmlowrate_2g4);
+	AICWFDBG(LOGTRACE, "%s:ofdm64qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm64qam_2g4);
+	AICWFDBG(LOGTRACE, "%s:ofdm256qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm256qam_2g4);
+	AICWFDBG(LOGTRACE, "%s:ofdm1024qam_2g4:%d\r\n", __func__, txpwr_idx->ofdm1024qam_2g4);
+	AICWFDBG(LOGTRACE, "%s:ofdmlowrate_5g:%d\r\n", __func__, txpwr_idx->ofdmlowrate_5g);
+	AICWFDBG(LOGTRACE, "%s:ofdm64qam_5g:%d\r\n", __func__, txpwr_idx->ofdm64qam_5g);
+	AICWFDBG(LOGTRACE, "%s:ofdm256qam_5g:%d\r\n", __func__, txpwr_idx->ofdm256qam_5g);
+	AICWFDBG(LOGTRACE, "%s:ofdm1024qam_5g:%d\r\n", __func__, txpwr_idx->ofdm1024qam_5g);
 
     /* Send the MM_SET_TXPWR_IDX_REQ message to UMAC FW */
     error = rwnx_send_msg(rwnx_hw, txpwr_idx_req, 1, MM_SET_TXPWR_IDX_LVL_CFM, NULL);
@@ -1503,44 +1503,44 @@ int rwnx_send_txpwr_lvl_req(struct rwnx_hw *rwnx_hw)
         rwnx_msg_free(rwnx_hw, txpwr_lvl_req);
         return 0;
     } else {
-        AICWFDBG(LOGINFO, "%s:enable:%d\r\n",               __func__, txpwr_lvl_v2->enable);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_1m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[0]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_2m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[1]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_5m5_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[2]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_11m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[3]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_6m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[4]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_9m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[5]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_12m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[6]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_18m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[7]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_24m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[8]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_36m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[9]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_48m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[10]);
-        AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_54m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[11]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs0_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[0]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs1_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[1]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs2_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[2]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs3_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[3]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs4_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[4]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs5_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[5]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs6_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[6]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs7_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[7]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs8_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[8]);
-        AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs9_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[9]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs0_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[0]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs1_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[1]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs2_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[2]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs3_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[3]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs4_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[4]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs5_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[5]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs6_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[6]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs7_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[7]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs8_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[8]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs9_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[9]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs10_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[10]);
-        AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs11_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[11]);
+        AICWFDBG(LOGTRACE, "%s:enable:%d\r\n",               __func__, txpwr_lvl_v2->enable);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_1m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[0]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_2m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[1]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_5m5_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[2]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_11m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[3]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_6m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[4]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_9m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[5]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_12m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[6]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_18m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[7]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_24m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[8]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_36m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[9]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_48m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[10]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_54m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[11]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs0_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[0]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs1_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[1]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs2_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[2]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs3_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[3]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs4_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[4]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs5_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[5]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs6_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[6]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs7_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[7]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs8_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[8]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs9_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[9]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs0_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[0]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs1_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[1]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs2_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[2]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs3_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[3]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs4_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[4]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs5_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[5]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs6_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[6]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs7_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[7]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs8_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[8]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs9_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[9]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs10_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[10]);
+        AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs11_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[11]);
 
     if (txpwr_loss->loss_enable == 1) {
-        AICWFDBG(LOGINFO, "%s:loss_value:%d\r\n", __func__, txpwr_loss->loss_value);
+        AICWFDBG(LOGTRACE, "%s:loss_value:%d\r\n", __func__, txpwr_loss->loss_value);
 
         for (i = 0; i <= 11; i++)
             txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[i] += txpwr_loss->loss_value;
@@ -1607,19 +1607,19 @@ int rwnx_send_txpwr_ofst_req(struct rwnx_hw *rwnx_hw)
 	}
 	if(txpwr_ofst->enable){
 
-		AICWFDBG(LOGINFO, "%s:enable:%d\r\n", __func__, txpwr_ofst->enable);
-		AICWFDBG(LOGINFO, "%s:chan_1_4:%d\r\n", __func__, txpwr_ofst->chan_1_4);
-		AICWFDBG(LOGINFO, "%s:chan_5_9:%d\r\n", __func__, txpwr_ofst->chan_5_9);
-		AICWFDBG(LOGINFO, "%s:chan_10_13:%d\r\n", __func__, txpwr_ofst->chan_10_13);
-		AICWFDBG(LOGINFO, "%s:chan_36_64:%d\r\n", __func__, txpwr_ofst->chan_36_64);
-		AICWFDBG(LOGINFO, "%s:chan_100_120:%d\r\n", __func__, txpwr_ofst->chan_100_120);
-		AICWFDBG(LOGINFO, "%s:chan_122_140:%d\r\n", __func__, txpwr_ofst->chan_122_140);
-		AICWFDBG(LOGINFO, "%s:chan_142_165:%d\r\n", __func__, txpwr_ofst->chan_142_165);
+		AICWFDBG(LOGTRACE, "%s:enable:%d\r\n", __func__, txpwr_ofst->enable);
+		AICWFDBG(LOGTRACE, "%s:chan_1_4:%d\r\n", __func__, txpwr_ofst->chan_1_4);
+		AICWFDBG(LOGTRACE, "%s:chan_5_9:%d\r\n", __func__, txpwr_ofst->chan_5_9);
+		AICWFDBG(LOGTRACE, "%s:chan_10_13:%d\r\n", __func__, txpwr_ofst->chan_10_13);
+		AICWFDBG(LOGTRACE, "%s:chan_36_64:%d\r\n", __func__, txpwr_ofst->chan_36_64);
+		AICWFDBG(LOGTRACE, "%s:chan_100_120:%d\r\n", __func__, txpwr_ofst->chan_100_120);
+		AICWFDBG(LOGTRACE, "%s:chan_122_140:%d\r\n", __func__, txpwr_ofst->chan_122_140);
+		AICWFDBG(LOGTRACE, "%s:chan_142_165:%d\r\n", __func__, txpwr_ofst->chan_142_165);
 
 	    /* Send the MM_SET_TXPWR_OFST_REQ message to UMAC FW */
 	    error = rwnx_send_msg(rwnx_hw, txpwr_ofst_req, 1, MM_SET_TXPWR_OFST_CFM, NULL);
 	}else{
-		AICWFDBG(LOGINFO, "%s:Do not use txpwr_ofst\r\n", __func__);
+		AICWFDBG(LOGTRACE, "%s:Do not use txpwr_ofst\r\n", __func__);
 		rwnx_msg_free(rwnx_hw, txpwr_ofst_req);
 	}
 
@@ -1838,9 +1838,9 @@ int rwnx_send_me_config_req(struct rwnx_hw *rwnx_hw)
                                                                req->he_supp);
 #endif
 
-	AICWFDBG(LOGINFO, "HT supp %d, VHT supp %d, HE supp %d\n", req->ht_supp,
-                                                               req->vht_supp,
-                                                               req->he_supp);
+	AICWFDBG(LOGTRACE, "HT supp %d, VHT supp %d, HE supp %d\n", req->ht_supp,
+								req->vht_supp,
+								req->he_supp);
 
 	/* Send the ME_CONFIG_REQ message to LMAC FW */
     return rwnx_send_msg(rwnx_hw, req, 1, ME_CONFIG_CFM, NULL);
@@ -1930,7 +1930,7 @@ int rwnx_send_me_sta_add(struct rwnx_hw *rwnx_hw, struct station_parameters *par
     struct rwnx_vif *rwnx_vif = rwnx_hw->vif_table[inst_nbr];
     #if (defined CONFIG_HE_FOR_OLD_KERNEL) || (defined CONFIG_VHT_FOR_OLD_KERNEL)
     struct aic_sta *sta = &rwnx_hw->aic_table[rwnx_vif->ap.aic_index];
-    printk("assoc_req idx %d, he: %d, vht: %d\n ", rwnx_vif->ap.aic_index, sta->he, sta->vht);
+    AICWFDBG(LOGTRACE, "assoc_req idx %d, he: %d, vht: %d\n ", rwnx_vif->ap.aic_index, sta->he, sta->vht);
     if (rwnx_vif->ap.aic_index < NX_REMOTE_STA_MAX + NX_VIRT_DEV_MAX)
         rwnx_vif->ap.aic_index++;
     else
@@ -2337,7 +2337,7 @@ int rwnx_send_sm_connect_req(struct rwnx_hw *rwnx_hw,
 	memcpy(rwnx_vif->sta.ssid, sme->ssid, rwnx_vif->sta.ssid_len);
 	memcpy(rwnx_vif->sta.bssid, sme->bssid, ETH_ALEN);
 
-	AICWFDBG(LOGINFO, "%s drv_vif_index:%d connect to %s(%d) channel:%d auth_type:%d\r\n",
+	AICWFDBG(LOGTRACE, "%s drv_vif_index:%d connect to %s(%d) channel:%d auth_type:%d\r\n",
 		__func__,
 		rwnx_vif->drv_vif_index,
 		rwnx_vif->sta.ssid,
@@ -2345,7 +2345,7 @@ int rwnx_send_sm_connect_req(struct rwnx_hw *rwnx_hw,
 		req->chan.freq,
 		req->auth_type);
 
-    printk("connect mac %x %x %x %x %x %x\n", rwnx_vif->sta.bssid[0],
+    AICWFDBG(LOGTRACE, "connect mac %x %x %x %x %x %x\n", rwnx_vif->sta.bssid[0],
         rwnx_vif->sta.bssid[1],
         rwnx_vif->sta.bssid[2],
         rwnx_vif->sta.bssid[3],
@@ -2632,7 +2632,7 @@ int rwnx_send_scanu_req(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif,
 
         if (!memcmp(P2P_WILDCARD_SSID, param->ssids[i].ssid,
             P2P_WILDCARD_SSID_LEN)) {
-            AICWFDBG(LOGINFO, "p2p scanu:%d,%d,%d\n",rwnx_vif->vif_index, rwnx_vif->is_p2p_vif, rwnx_hw->is_p2p_alive);
+            AICWFDBG(LOGTRACE, "p2p scanu:%d,%d,%d\n",rwnx_vif->vif_index, rwnx_vif->is_p2p_vif, rwnx_hw->is_p2p_alive);
 #ifdef CONFIG_USE_P2P0
             if (rwnx_vif->is_p2p_vif && !rwnx_hw->is_p2p_alive) {
 #else
@@ -2848,7 +2848,7 @@ int rwnx_send_mesh_start_req(struct rwnx_hw *rwnx_hw, struct rwnx_vif *vif,
 
         /* Check DMA mapping result */
         if (dma_mapping_error(rwnx_hw->dev, req->ie_addr)) {
-            printk(KERN_CRIT "%s - DMA Mapping error on additional IEs\n", __func__);
+            AICWFDBG(LOGERROR, "%s - DMA Mapping error on additional IEs\n", __func__);
 
             /* Consider there is no Additional IEs */
             req->ie_len = 0;
diff --git a/aic8800_fdrv/rwnx_platform.c b/aic8800_fdrv/rwnx_platform.c
index c8c3bfe..897242b 100644
--- a/aic8800_fdrv/rwnx_platform.c
+++ b/aic8800_fdrv/rwnx_platform.c
@@ -259,16 +259,16 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
 	int size = 0;
 	int ret = 0;
 
-	AICWFDBG(LOGINFO, "%s: request firmware = %s \n", __func__ ,name);
+	AICWFDBG(LOGTRACE, "%s: request firmware = %s \n", __func__ ,name);
 
 	ret = request_firmware(&fw, name, NULL);
-	
+
 	if (ret < 0) {
 		AICWFDBG(LOGERROR, "Load %s fail\n", name);
 		release_firmware(fw);
 		return -1;
 	}
-	
+
 	size = fw->size;
 	dst = (u32 *)fw->data;
 
@@ -277,7 +277,7 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
 		release_firmware(fw);
 		return -1;
 	}
-	
+
 	buffer = vmalloc(size);
 	memset(buffer, 0, size);
 	memcpy(buffer, dst, size);
@@ -287,7 +287,7 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
 	MD5Init(&md5);
 	MD5Update(&md5, (unsigned char *)buffer, size);
 	MD5Final(&md5, decrypt);
-	AICWFDBG(LOGINFO, MD5PINRT, MD5(decrypt));
+	AICWFDBG(LOGTRACE, MD5PINRT, MD5(decrypt));
 
 	release_firmware(fw);
 
@@ -308,7 +308,7 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
         *fw_buf = NULL;
         return -1;
     }
-	
+
 	len = snprintf(path, FW_PATH_MAX_LEN, "%s/%s", aic_fw_path, name);
 
     //len = snprintf(path, FW_PATH_MAX_LEN, "%s", name);
@@ -319,7 +319,7 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
         return -1;
     }
 
-    AICWFDBG(LOGINFO, "%s :firmware path = %s  \n", __func__, path);
+    AICWFDBG(LOGTRACE, "%s :firmware path = %s  \n", __func__, path);
 
     /* open the firmware file */
     fp = filp_open(path, O_RDONLY, 0);
@@ -400,8 +400,8 @@ static int rwnx_load_firmware(u32 **fw_buf, const char *name, struct device *dev
 	MD5Update(&md5, (unsigned char *)dst, size);
 	MD5Final(&md5, decrypt);
 
-	AICWFDBG(LOGINFO, MD5PINRT, MD5(decrypt));
-	
+	AICWFDBG(LOGTRACE, MD5PINRT, MD5(decrypt));
+
     return size;
 #endif
 }
@@ -417,8 +417,8 @@ static void rwnx_restore_firmware(u32 **fw_buf)
 int rwnx_request_firmware_common(struct rwnx_hw *rwnx_hw, u32** buffer, const char *filename)
 {
     int size;
-	
-    AICWFDBG(LOGINFO, "### Load file %s\n", filename);
+
+    AICWFDBG(LOGTRACE, "### Load file %s\n", filename);
 
     size = rwnx_load_firmware(buffer, filename, NULL);
 
@@ -451,7 +451,7 @@ int rwnx_plat_bin_fw_upload_2(struct rwnx_hw *rwnx_hw, u32 fw_addr,
 	u32 *dst = NULL;
 
     /* Copy the file on the Embedded side */
-    AICWFDBG(LOGINFO, "### Upload %s firmware, @ = %x\n", filename, fw_addr);
+    AICWFDBG(LOGTRACE, "### Upload %s firmware, @ = %x\n", filename, fw_addr);
 
     size = rwnx_request_firmware_common(rwnx_hw, &dst, filename);
     if (!dst) {
@@ -465,7 +465,7 @@ int rwnx_plat_bin_fw_upload_2(struct rwnx_hw *rwnx_hw, u32 fw_addr,
 		return -1;
     }
 
-	AICWFDBG(LOGINFO, "size=%d, dst[0]=%x\n", size, dst[0]);
+	AICWFDBG(LOGTRACE, "size=%d, dst[0]=%x\n", size, dst[0]);
     if (size > 512) {
         for (; i < (size - 512); i += 512) {
             //printk("wr blk 0: %p -> %x\r\n", dst + i / 4, fw_addr + i);
@@ -516,21 +516,21 @@ static int aic_load_firmware(u32 ** fw_buf, const char *name,
 
         len = snprintf(path, FW_PATH_MAX_LEN, "%s/%s",aic_fw_path, name);
         if (len >= FW_PATH_MAX_LEN) {
-                printk("%s: %s file's path too long\n", __func__, name);
+                AICWFDBG(LOGERROR, "%s: %s file's path too long\n", __func__, name);
                 *fw_buf=NULL;
                 __putname(path);
                 return -1;
         }
 
-        printk("%s :firmware path = %s  \n", __func__ ,path);
+        AICWFDBG(LOGTRACE, "%s :firmware path = %s  \n", __func__ ,path);
 
 
         /* open the firmware file */
         fp=filp_open(path, O_RDONLY, 0);
         if(IS_ERR(fp) || (!fp)){
-	        printk("%s: %s file failed to open\n", __func__, name);
+	        AICWFDBG(LOGERROR, "%s: %s file failed to open\n", __func__, name);
                 if(IS_ERR(fp))
-			printk("is_Err\n");
+			AICWFDBG(LOGERROR, "is_Err\n");
 		*fw_buf=NULL;
                 __putname(path);
                 fp=NULL;
@@ -539,7 +539,7 @@ static int aic_load_firmware(u32 ** fw_buf, const char *name,
 
         size = i_size_read(file_inode(fp));
         if(size<=0){
-                printk("%s: %s file size invalid %d\n", __func__, name, size);
+                AICWFDBG(LOGERROR, "%s: %s file size invalid %d\n", __func__, name, size);
                 *fw_buf=NULL;
                 __putname(path);
                 filp_close(fp,NULL);
@@ -565,7 +565,7 @@ static int aic_load_firmware(u32 ** fw_buf, const char *name,
         #endif
 
         if(size != rdlen){
-                printk("%s: %s file rdlen invalid %ld\n", __func__, name, (long int)rdlen);
+                AICWFDBG(LOGERROR, "%s: %s file rdlen invalid %ld\n", __func__, name, (long int)rdlen);
                 *fw_buf=NULL;
                 __putname(path);
                 filp_close(fp,NULL);
@@ -581,7 +581,7 @@ static int aic_load_firmware(u32 ** fw_buf, const char *name,
 
        /*start to transform the data format*/
         src = (u32*)buffer;
-        printk("malloc dst\n");
+        AICWFDBG(LOGTRACE, "malloc dst\n");
         dst = (u32*)kzalloc(size,GFP_KERNEL);
 
         if(!dst){
@@ -629,7 +629,7 @@ static int rwnx_plat_bin_fw_upload_android(struct rwnx_hw *rwnx_hw, u32 fw_addr,
         /* load aic firmware */
         size = aic_load_firmware(&dst, filename, dev);
         if(size<=0){
-                printk("wrong size of firmware file\n");
+                AICWFDBG(LOGERROR, "wrong size of firmware file\n");
                 kfree(dst);
                 dst = NULL;
                 return -1;
@@ -637,13 +637,13 @@ static int rwnx_plat_bin_fw_upload_android(struct rwnx_hw *rwnx_hw, u32 fw_addr,
 
 
     /* Copy the file on the Embedded side */
-    printk("\n### Upload %s firmware, @ = %x  size=%d\n", filename, fw_addr, size);
+    AICWFDBG(LOGTRACE, "\n### Upload %s firmware, @ = %x  size=%d\n", filename, fw_addr, size);
 
     if (size > 1024) {// > 1KB data
         for (i = 0; i < (size - 1024); i += 1024) {//each time write 1KB
             err = rwnx_send_dbg_mem_block_write_req(rwnx_hw, fw_addr + i, 1024, dst + i / 4);
                         if (err) {
-                printk("bin upload fail: %x, err:%d\r\n", fw_addr + i, err);
+AICWFDBG(LOGERROR, "bin upload fail: %x, err:%d\r\n", fw_addr + i, err);
                 break;
             }
         }
@@ -652,7 +652,7 @@ static int rwnx_plat_bin_fw_upload_android(struct rwnx_hw *rwnx_hw, u32 fw_addr,
     if (!err && (i < size)) {// <1KB data
         err = rwnx_send_dbg_mem_block_write_req(rwnx_hw, fw_addr + i, size - i, dst + i / 4);
         if (err) {
-            printk("bin upload fail: %x, err:%d\r\n", fw_addr + i, err);
+            AICWFDBG(LOGERROR, "bin upload fail: %x, err:%d\r\n", fw_addr + i, err);
         }
     }
 
@@ -1489,57 +1489,57 @@ void get_userconfig_txpwr_lvl_in_fdrv(txpwr_lvl_conf_t *txpwr_lvl)
     txpwr_lvl->ofdm256qam_5g    = userconfig_info.txpwr_lvl.ofdm256qam_5g;
     txpwr_lvl->ofdm1024qam_5g   = userconfig_info.txpwr_lvl.ofdm1024qam_5g;
 
-    AICWFDBG(LOGINFO, "%s:enable:%d\r\n",          __func__, txpwr_lvl->enable);
-    AICWFDBG(LOGINFO, "%s:dsss:%d\r\n",            __func__, txpwr_lvl->dsss);
-    AICWFDBG(LOGINFO, "%s:ofdmlowrate_2g4:%d\r\n", __func__, txpwr_lvl->ofdmlowrate_2g4);
-    AICWFDBG(LOGINFO, "%s:ofdm64qam_2g4:%d\r\n",   __func__, txpwr_lvl->ofdm64qam_2g4);
-    AICWFDBG(LOGINFO, "%s:ofdm256qam_2g4:%d\r\n",  __func__, txpwr_lvl->ofdm256qam_2g4);
-    AICWFDBG(LOGINFO, "%s:ofdm1024qam_2g4:%d\r\n", __func__, txpwr_lvl->ofdm1024qam_2g4);
-    AICWFDBG(LOGINFO, "%s:ofdmlowrate_5g:%d\r\n",  __func__, txpwr_lvl->ofdmlowrate_5g);
-    AICWFDBG(LOGINFO, "%s:ofdm64qam_5g:%d\r\n",    __func__, txpwr_lvl->ofdm64qam_5g);
-    AICWFDBG(LOGINFO, "%s:ofdm256qam_5g:%d\r\n",   __func__, txpwr_lvl->ofdm256qam_5g);
-    AICWFDBG(LOGINFO, "%s:ofdm1024qam_5g:%d\r\n",  __func__, txpwr_lvl->ofdm1024qam_5g);
+    AICWFDBG(LOGTRACE, "%s:enable:%d\r\n",          __func__, txpwr_lvl->enable);
+    AICWFDBG(LOGTRACE, "%s:dsss:%d\r\n",            __func__, txpwr_lvl->dsss);
+    AICWFDBG(LOGTRACE, "%s:ofdmlowrate_2g4:%d\r\n", __func__, txpwr_lvl->ofdmlowrate_2g4);
+    AICWFDBG(LOGTRACE, "%s:ofdm64qam_2g4:%d\r\n",   __func__, txpwr_lvl->ofdm64qam_2g4);
+    AICWFDBG(LOGTRACE, "%s:ofdm256qam_2g4:%d\r\n",  __func__, txpwr_lvl->ofdm256qam_2g4);
+    AICWFDBG(LOGTRACE, "%s:ofdm1024qam_2g4:%d\r\n", __func__, txpwr_lvl->ofdm1024qam_2g4);
+    AICWFDBG(LOGTRACE, "%s:ofdmlowrate_5g:%d\r\n",  __func__, txpwr_lvl->ofdmlowrate_5g);
+    AICWFDBG(LOGTRACE, "%s:ofdm64qam_5g:%d\r\n",    __func__, txpwr_lvl->ofdm64qam_5g);
+    AICWFDBG(LOGTRACE, "%s:ofdm256qam_5g:%d\r\n",   __func__, txpwr_lvl->ofdm256qam_5g);
+    AICWFDBG(LOGTRACE, "%s:ofdm1024qam_5g:%d\r\n",  __func__, txpwr_lvl->ofdm1024qam_5g);
 }
 
 void get_userconfig_txpwr_lvl_v2_in_fdrv(txpwr_lvl_conf_v2_t *txpwr_lvl_v2)
 {
     *txpwr_lvl_v2 = userconfig_info.txpwr_lvl_v2;
 
-    AICWFDBG(LOGINFO, "%s:enable:%d\r\n",               __func__, txpwr_lvl_v2->enable);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_1m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[0]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_2m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[1]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_5m5_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[2]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_11m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[3]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_6m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[4]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_9m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[5]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_12m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[6]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_18m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[7]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_24m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[8]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_36m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[9]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_48m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[10]);
-    AICWFDBG(LOGINFO, "%s:lvl_11b_11ag_54m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[11]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs0_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[0]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs1_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[1]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs2_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[2]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs3_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[3]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs4_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[4]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs5_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[5]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs6_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[6]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs7_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[7]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs8_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[8]);
-    AICWFDBG(LOGINFO, "%s:lvl_11n_11ac_mcs9_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[9]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs0_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[0]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs1_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[1]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs2_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[2]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs3_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[3]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs4_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[4]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs5_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[5]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs6_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[6]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs7_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[7]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs8_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[8]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs9_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[9]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs10_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[10]);
-    AICWFDBG(LOGINFO, "%s:lvl_11ax_mcs11_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[11]);
+    AICWFDBG(LOGTRACE, "%s:enable:%d\r\n",               __func__, txpwr_lvl_v2->enable);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_1m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[0]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_2m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[1]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_5m5_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[2]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_11m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[3]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_6m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[4]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_9m_2g4:%d\r\n",  __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[5]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_12m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[6]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_18m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[7]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_24m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[8]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_36m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[9]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_48m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[10]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11b_11ag_54m_2g4:%d\r\n", __func__, txpwr_lvl_v2->pwrlvl_11b_11ag_2g4[11]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs0_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[0]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs1_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[1]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs2_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[2]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs3_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[3]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs4_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[4]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs5_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[5]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs6_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[6]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs7_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[7]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs8_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[8]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11n_11ac_mcs9_2g4:%d\r\n",__func__, txpwr_lvl_v2->pwrlvl_11n_11ac_2g4[9]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs0_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[0]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs1_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[1]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs2_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[2]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs3_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[3]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs4_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[4]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs5_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[5]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs6_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[6]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs7_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[7]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs8_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[8]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs9_2g4:%d\r\n",    __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[9]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs10_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[10]);
+    AICWFDBG(LOGTRACE, "%s:lvl_11ax_mcs11_2g4:%d\r\n",   __func__, txpwr_lvl_v2->pwrlvl_11ax_2g4[11]);
 }
 
 void get_userconfig_txpwr_ofst_in_fdrv(txpwr_ofst_conf_t *txpwr_ofst)
@@ -1553,14 +1553,14 @@ void get_userconfig_txpwr_ofst_in_fdrv(txpwr_ofst_conf_t *txpwr_ofst)
     txpwr_ofst->chan_122_140 = userconfig_info.txpwr_ofst.chan_122_140;
     txpwr_ofst->chan_142_165 = userconfig_info.txpwr_ofst.chan_142_165;
 
-    AICWFDBG(LOGINFO, "%s:enable      :%d\r\n", __func__, txpwr_ofst->enable);
-    AICWFDBG(LOGINFO, "%s:chan_1_4    :%d\r\n", __func__, txpwr_ofst->chan_1_4);
-    AICWFDBG(LOGINFO, "%s:chan_5_9    :%d\r\n", __func__, txpwr_ofst->chan_5_9);
-    AICWFDBG(LOGINFO, "%s:chan_10_13  :%d\r\n", __func__, txpwr_ofst->chan_10_13);
-    AICWFDBG(LOGINFO, "%s:chan_36_64  :%d\r\n", __func__, txpwr_ofst->chan_36_64);
-    AICWFDBG(LOGINFO, "%s:chan_100_120:%d\r\n", __func__, txpwr_ofst->chan_100_120);
-    AICWFDBG(LOGINFO, "%s:chan_122_140:%d\r\n", __func__, txpwr_ofst->chan_122_140);
-    AICWFDBG(LOGINFO, "%s:chan_142_165:%d\r\n", __func__, txpwr_ofst->chan_142_165);
+    AICWFDBG(LOGTRACE, "%s:enable      :%d\r\n", __func__, txpwr_ofst->enable);
+    AICWFDBG(LOGTRACE, "%s:chan_1_4    :%d\r\n", __func__, txpwr_ofst->chan_1_4);
+    AICWFDBG(LOGTRACE, "%s:chan_5_9    :%d\r\n", __func__, txpwr_ofst->chan_5_9);
+    AICWFDBG(LOGTRACE, "%s:chan_10_13  :%d\r\n", __func__, txpwr_ofst->chan_10_13);
+    AICWFDBG(LOGTRACE, "%s:chan_36_64  :%d\r\n", __func__, txpwr_ofst->chan_36_64);
+    AICWFDBG(LOGTRACE, "%s:chan_100_120:%d\r\n", __func__, txpwr_ofst->chan_100_120);
+    AICWFDBG(LOGTRACE, "%s:chan_122_140:%d\r\n", __func__, txpwr_ofst->chan_122_140);
+    AICWFDBG(LOGTRACE, "%s:chan_142_165:%d\r\n", __func__, txpwr_ofst->chan_142_165);
 }
 
 void get_userconfig_txpwr_loss(txpwr_loss_conf_t *txpwr_loss)
@@ -1568,23 +1568,23 @@ void get_userconfig_txpwr_loss(txpwr_loss_conf_t *txpwr_loss)
     txpwr_loss->loss_enable      = userconfig_info.txpwr_loss.loss_enable;
     txpwr_loss->loss_value       = userconfig_info.txpwr_loss.loss_value;
 
-    AICWFDBG(LOGINFO, "%s:loss_enable:%d\r\n",     __func__, txpwr_loss->loss_enable);
-    AICWFDBG(LOGINFO, "%s:loss_value:%d\r\n",      __func__, txpwr_loss->loss_value);
+    AICWFDBG(LOGTRACE, "%s:loss_enable:%d\r\n",     __func__, txpwr_loss->loss_enable);
+    AICWFDBG(LOGTRACE, "%s:loss_value:%d\r\n",      __func__, txpwr_loss->loss_value);
 }
 
 void get_userconfig_xtal_cap(xtal_cap_conf_t *xtal_cap)
 {
     *xtal_cap = userconfig_info.xtal_cap;
 
-    AICWFDBG(LOGINFO, "%s:enable       :%d\r\n", __func__, xtal_cap->enable);
-    AICWFDBG(LOGINFO, "%s:xtal_cap     :%d\r\n", __func__, xtal_cap->xtal_cap);
-    AICWFDBG(LOGINFO, "%s:xtal_cap_fine:%d\r\n", __func__, xtal_cap->xtal_cap_fine);
+    AICWFDBG(LOGTRACE, "%s:enable       :%d\r\n", __func__, xtal_cap->enable);
+    AICWFDBG(LOGTRACE, "%s:xtal_cap     :%d\r\n", __func__, xtal_cap->xtal_cap);
+    AICWFDBG(LOGTRACE, "%s:xtal_cap_fine:%d\r\n", __func__, xtal_cap->xtal_cap_fine);
 }
 
 void rwnx_plat_nvram_set_value(char *command, char *value)
 {
     //TODO send command
-    AICWFDBG(LOGINFO, "%s:command=%s value=%s\n", __func__, command, value);
+    AICWFDBG(LOGTRACE, "%s:command=%s value=%s\n", __func__, command, value);
     if (!strcmp(command, "enable")) {
         userconfig_info.txpwr_lvl.enable = rwnx_atoi(value);
         userconfig_info.txpwr_lvl_v2.enable = rwnx_atoi(value);
@@ -1721,7 +1721,7 @@ void rwnx_plat_userconfig_parsing(char *buffer, int size)
         if (buffer[i] == 0x0a || buffer[i] == 0x0d) {
             if (command[0] != 0 && value[0] != 0) {
                 if (parse_state == PRINT) {
-                    AICWFDBG(LOGINFO, "%s:%s\r\n", __func__, value);
+                    AICWFDBG(LOGTRACE, "%s:%s\r\n", __func__, value);
                 } else if (parse_state == GET_VALUE) {
                     rwnx_plat_nvram_set_value(command, value);
                 }
@@ -1946,9 +1946,9 @@ int rwnx_platform_init(struct rwnx_plat *rwnx_plat, void **platform_data)
     g_rwnx_plat = rwnx_plat;
 
 #if defined CONFIG_RWNX_FULLMAC
-    AICWFDBG(LOGINFO, "%s rwnx_cfg80211_init enter \r\n", __func__);
+    AICWFDBG(LOGTRACE, "%s rwnx_cfg80211_init enter \r\n", __func__);
     ret = rwnx_cfg80211_init(rwnx_plat, platform_data);
-    AICWFDBG(LOGINFO, "%s rwnx_cfg80211_init exit \r\n", __func__);
+    AICWFDBG(LOGTRACE, "%s rwnx_cfg80211_init exit \r\n", __func__);
 #if defined(AICWF_USB_SUPPORT) && defined(CONFIG_VENDOR_GPIO)
     // initialize gpiob2, gpiob3, gpiob5 to output mode and set output to 0
     rwnx_send_dbg_gpio_init_req(rwnx_plat->usbdev->rwnx_hw, 2, 1, 0);//gpiob 2 = 0
@@ -1958,7 +1958,7 @@ int rwnx_platform_init(struct rwnx_plat *rwnx_plat, void **platform_data)
     // read gpiob2
     //struct dbg_gpio_read_cfm gpio_rd_cfm;
     //rwnx_send_dbg_gpio_read_req(rwnx_plat->usbdev->rwnx_hw, 2, &gpio_rd_cfm);
-    //AICWFDBG(LOGINFO, "gpio_rd_cfm idx:%d val:%d\n", gpio_rd_cfm.gpio_idx, gpio_rd_cfm.gpio_val);
+    //AICWFDBG(LOGTRACE, "gpio_rd_cfm idx:%d val:%d\n", gpio_rd_cfm.gpio_idx, gpio_rd_cfm.gpio_val);
 
     // set gpiob2 output to 1
     //rwnx_send_dbg_gpio_write_req(rwnx_plat->usbdev->rwnx_hw, 2, 1);
diff --git a/aic8800_fdrv/rwnx_rx.c b/aic8800_fdrv/rwnx_rx.c
index cb17846..a0e7d08 100644
--- a/aic8800_fdrv/rwnx_rx.c
+++ b/aic8800_fdrv/rwnx_rx.c
@@ -687,18 +687,18 @@ static void rwnx_rx_mgmt(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif,
 	u32 len;
 
     if (ieee80211_is_assoc_req(mgmt->frame_control) && rwnx_vif->wdev.iftype == NL80211_IFTYPE_AP) {
-        printk("ASSOC_REQ: sta_idx %d MAC %pM\n", rwnx_vif->ap.aic_index, mgmt->sa);
+        AICWFDBG(LOGTRACE, "ASSOC_REQ: sta_idx %d MAC %pM\n", rwnx_vif->ap.aic_index, mgmt->sa);
         sta->sta_idx = rwnx_vif->ap.aic_index;
         len = skb->len - (mgmt->u.assoc_req.variable - skb->data);
 
         #ifdef CONFIG_HE_FOR_OLD_KERNEL
         ie = cfg80211_find_ext_ie(WLAN_EID_EXT_HE_CAPABILITY, mgmt->u.assoc_req.variable, len);
         if (ie && ie[1] >= sizeof(*he) + 1) {
-            printk("assoc_req: find he\n");
+            AICWFDBG(LOGTRACE, "assoc_req: find he\n");
             sta->he = true;
         }
         else {
-            printk("assoc_req: no find he\n");
+            AICWFDBG(LOGTRACE, "assoc_req: no find he\n");
             sta->he = false;
         }
         #endif
@@ -707,10 +707,10 @@ static void rwnx_rx_mgmt(struct rwnx_hw *rwnx_hw, struct rwnx_vif *rwnx_vif,
         struct ieee80211_vht_cap *vht;
         ie = cfg80211_find_ie(WLAN_EID_VHT_CAPABILITY, mgmt->u.assoc_req.variable, len);
         if (ie && ie[1] >= sizeof(*vht)) {
-            printk("assoc_req: find vht\n");
+            AICWFDBG(LOGTRACE, "assoc_req: find vht\n");
             sta->vht = true;
         } else {
-            printk("assoc_req: no find vht\n");
+            AICWFDBG(LOGTRACE, "assoc_req: no find vht\n");
             sta->vht = false;
         }
         #endif
@@ -1317,7 +1317,7 @@ struct reord_ctrl_info *reord_init_sta(struct aicwf_rx_priv* rx_priv, const u8 *
 #endif
 
     if (bus_if->state == BUS_DOWN_ST || rx_priv == NULL) {
-        printk("bad stat!\n");
+        AICWFDBG(LOGERROR, "bad stat!\n");
         return NULL;
     }
 
@@ -1365,7 +1365,7 @@ int reord_flush_tid(struct aicwf_rx_priv *rx_priv, struct sk_buff *skb, u8 tid)
     else if((rwnx_vif->wdev.iftype == NL80211_IFTYPE_AP) || (rwnx_vif->wdev.iftype == NL80211_IFTYPE_P2P_GO))
         mac = eh->h_source;
     else {
-        printk("error mode:%d!\n", rwnx_vif->wdev.iftype);
+        AICWFDBG(LOGERROR, "error mode:%d!\n", rwnx_vif->wdev.iftype);
         dev_kfree_skb(skb);
         return -1;
     }
@@ -1398,7 +1398,7 @@ int reord_flush_tid(struct aicwf_rx_priv *rx_priv, struct sk_buff *skb, u8 tid)
         list_del_init(&(prframe->reord_pending_list));
     }
 
-	AICWFDBG(LOGINFO, "flush:tid=%d", tid);
+	AICWFDBG(LOGDEBUG, "flush:tid=%d", tid);
     preorder_ctrl->enable = false;
     spin_unlock_irqrestore(&preorder_ctrl->reord_list_lock, flags);
     if (timer_pending(&preorder_ctrl->reord_timer))
@@ -1420,7 +1420,7 @@ void reord_deinit_sta(struct aicwf_rx_priv* rx_priv, struct reord_ctrl_info *reo
         return;
     }
 
-	AICWFDBG(LOGINFO, "%s\n", __func__);
+	AICWFDBG(LOGTRACE, "%s\n", __func__);
 
     for (i=0; i < 8; i++) {
         struct recv_msdu *req, *next;
@@ -1442,7 +1442,7 @@ void reord_deinit_sta(struct aicwf_rx_priv* rx_priv, struct reord_ctrl_info *reo
             reord_rxframe_free(&rx_priv->freeq_lock, &rx_priv->rxframes_freequeue, &req->rxframe_list);
         }
 
-		AICWFDBG(LOGINFO, "reord dinit in_irq():%d in_atomic:%d in_softirq:%d\r\n", (int)in_irq()
+		AICWFDBG(LOGTRACE, "reord dinit in_irq():%d in_atomic:%d in_softirq:%d\r\n", (int)in_irq()
 			,(int)in_atomic(), (int)in_softirq());
         spin_unlock_bh(&preorder_ctrl->reord_list_lock);
     }
@@ -1770,10 +1770,10 @@ int reord_process_unit(struct aicwf_rx_priv *rx_priv, struct sk_buff *skb, u16 s
     if (reord_need_check(preorder_ctrl, pframe->seq_num)) {
 		if(pframe->rx_data[42] == 0x80){//this is rtp package
 			if(pframe->seq_num == preorder_ctrl->ind_sn){
-				printk("%s pframe->seq_num1:%d \r\n", __func__, pframe->seq_num);
+				AICWFDBG(LOGTRACE, "%s pframe->seq_num1:%d \r\n", __func__, pframe->seq_num);
 	        	reord_single_frame_ind(rx_priv, pframe);//not need to reorder
 			}else{
-				printk("%s free pframe->seq_num:%d \r\n", __func__, pframe->seq_num);
+				AICWFDBG(LOGTRACE, "%s free pframe->seq_num:%d \r\n", __func__, pframe->seq_num);
 			    if (pframe->pkt){
 			        dev_kfree_skb(pframe->pkt);
 			        pframe->pkt = NULL;
@@ -1884,7 +1884,7 @@ void remove_sec_hdr_mgmt_frame(struct hw_rxhdr *hw_rxhdr,struct sk_buff *skb)
 
     if(!hw_rxhdr->hwvect.ga_frame){
         if(((skb->data[0] & 0x0C) == 0) && (skb->data[1] & 0x40) == 0x40){ //protect management frame
-            printk("frame type %x\n",skb->data[0]);
+            AICWFDBG(LOGTRACE, "frame type %x\n",skb->data[0]);
             if(hw_rxhdr->hwvect.decr_status == RWNX_RX_HD_DECR_CCMP128){
                 memcpy(mgmt_header,skb->data,hdr_len);
                 skb_pull(skb,8);
@@ -1892,7 +1892,7 @@ void remove_sec_hdr_mgmt_frame(struct hw_rxhdr *hw_rxhdr,struct sk_buff *skb)
                 hw_rxhdr->hwvect.len -= 8;
             }
             else {
-                printk("unsupport decr_status:%d\n",hw_rxhdr->hwvect.decr_status);
+                AICWFDBG(LOGERROR, "unsupport decr_status:%d\n",hw_rxhdr->hwvect.decr_status);
             }
         }
     }
diff --git a/aic8800_fdrv/rwnx_testmode.c b/aic8800_fdrv/rwnx_testmode.c
index 01d7fe0..afb9d62 100644
--- a/aic8800_fdrv/rwnx_testmode.c
+++ b/aic8800_fdrv/rwnx_testmode.c
@@ -43,7 +43,7 @@ int rwnx_testmode_reg(struct ieee80211_hw *hw, struct nlattr **tb)
 
     /* First check if register address is there */
     if (!tb[RWNX_TM_ATTR_REG_OFFSET]) {
-        printk("Error finding register offset\n");
+        AICWFDBG(LOGERROR, "Error finding register offset\n");
         return -ENOMSG;
     }
 
@@ -61,7 +61,7 @@ int rwnx_testmode_reg(struct ieee80211_hw *hw, struct nlattr **tb)
             /* Allocate the answer message */
             skb = cfg80211_testmode_alloc_reply_skb(hw->wiphy, 20);
             if (!skb) {
-                printk("Error allocating memory\n");
+                AICWFDBG(LOGERROR, "Error allocating memory\n");
                 return -ENOMEM;
             }
 
@@ -72,14 +72,14 @@ int rwnx_testmode_reg(struct ieee80211_hw *hw, struct nlattr **tb)
             /* Send the answer to upper layer */
             status = cfg80211_testmode_reply(skb);
             if (status < 0)
-                printk("Error sending msg : %d\n", status);
+                AICWFDBG(LOGERROR, "Error sending msg : %d\n", status);
         }
         break;
 
     case RWNX_TM_CMD_APP2DEV_REG_WRITE:
         {
             if (!tb[RWNX_TM_ATTR_REG_VALUE32]) {
-                printk("Error finding value to write\n");
+                AICWFDBG(LOGERROR, "Error finding value to write\n");
                 return -ENOMSG;
             } else {
                 val32 = nla_get_u32(tb[RWNX_TM_ATTR_REG_VALUE32]);
@@ -91,7 +91,7 @@ int rwnx_testmode_reg(struct ieee80211_hw *hw, struct nlattr **tb)
         break;
 
     default:
-        printk("Unknown testmode register command ID\n");
+        AICWFDBG(LOGERROR, "Unknown testmode register command ID\n");
         return -ENOSYS;
     }
 
@@ -116,7 +116,7 @@ int rwnx_testmode_dbg_filter(struct ieee80211_hw *hw, struct nlattr **tb)
 
     /* First check if the filter is there */
     if (!tb[RWNX_TM_ATTR_REG_FILTER]) {
-        printk("Error finding filter value\n");
+        AICWFDBG(LOGERROR, "Error finding filter value\n");
         return -ENOMSG;
     }
 
@@ -140,7 +140,7 @@ int rwnx_testmode_dbg_filter(struct ieee80211_hw *hw, struct nlattr **tb)
         break;
 
     default:
-        printk("Unknown testmode register command ID\n");
+        AICWFDBG(LOGERROR, "Unknown testmode register command ID\n");
         return -ENOSYS;
     }
 
@@ -168,7 +168,7 @@ int rwnx_testmode_reg_dbg(struct ieee80211_hw *hw, struct nlattr **tb)
 
     /* First check if register address is there */
     if (!tb[RWNX_TM_ATTR_REG_OFFSET]) {
-        printk("Error finding register offset\n");
+        AICWFDBG(LOGERROR, "Error finding register offset\n");
         return -ENOMSG;
     }
 
@@ -184,7 +184,7 @@ int rwnx_testmode_reg_dbg(struct ieee80211_hw *hw, struct nlattr **tb)
             /* Allocate the answer message */
             skb = cfg80211_testmode_alloc_reply_skb(hw->wiphy, 20);
             if (!skb) {
-                printk("Error allocating memory\n");
+                AICWFDBG(LOGERROR, "Error allocating memory\n");
                 return -ENOMEM;
             }
 
@@ -194,14 +194,14 @@ int rwnx_testmode_reg_dbg(struct ieee80211_hw *hw, struct nlattr **tb)
             /* Send the answer to upper layer */
             status = cfg80211_testmode_reply(skb);
             if (status < 0)
-                printk("Error sending msg : %d\n", status);
+                AICWFDBG(LOGERROR, "Error sending msg : %d\n", status);
         }
         break;
 
     case RWNX_TM_CMD_APP2DEV_REG_WRITE_DBG:
         {
             if (!tb[RWNX_TM_ATTR_REG_VALUE32]) {
-                printk("Error finding value to write\n");
+                AICWFDBG(LOGERROR, "Error finding value to write\n");
                 return -ENOMSG;
             } else {
                 reg_value = nla_get_u32(tb[RWNX_TM_ATTR_REG_VALUE32]);
@@ -214,7 +214,7 @@ int rwnx_testmode_reg_dbg(struct ieee80211_hw *hw, struct nlattr **tb)
         break;
 
     default:
-        printk("Unknown testmode register command ID\n");
+        AICWFDBG(LOGERROR, "Unknown testmode register command ID\n");
         return -ENOSYS;
     }
 
diff --git a/aic8800_fdrv/rwnx_tx.c b/aic8800_fdrv/rwnx_tx.c
index 172421c..36b66d9 100644
--- a/aic8800_fdrv/rwnx_tx.c
+++ b/aic8800_fdrv/rwnx_tx.c
@@ -640,7 +640,7 @@ void rwnx_tx_push(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr, int flags)
         sw_txhdr->need_cfm = 1;
         sw_txhdr->desc.host.status_desc_addr = ((1<<31) | rwnx_hw->sdio_env.txdesc_free_idx[0]);
         aicwf_sdio_host_txdesc_push(&(rwnx_hw->sdio_env), 0, (long)skb);
-		AICWFDBG(LOGINFO, "need cfm ethertype:%8x,user_idx=%d, skb=%p sta_idx:%d\n", 
+		AICWFDBG(LOGTRACE, "need cfm ethertype:%8x,user_idx=%d, skb=%p sta_idx:%d\n", 
 			sw_txhdr->desc.host.ethertype, 
 			rwnx_hw->sdio_env.txdesc_free_idx[0], 
 			skb,
@@ -669,7 +669,7 @@ void rwnx_tx_push(struct rwnx_hw *rwnx_hw, struct rwnx_txhdr *txhdr, int flags)
         sw_txhdr->need_cfm = 1;
         sw_txhdr->desc.host.status_desc_addr = ((1<<31) | rwnx_hw->usb_env.txdesc_free_idx[0]);
         aicwf_usb_host_txdesc_push(&(rwnx_hw->usb_env), 0, (long)(skb));
-        AICWFDBG(LOGINFO, "need cfm ethertype:%8x,user_idx=%d, skb=%p sta_idx:%d\n", 
+        AICWFDBG(LOGTRACE, "need cfm ethertype:%8x,user_idx=%d, skb=%p sta_idx:%d\n", 
 			sw_txhdr->desc.host.ethertype, 
 			rwnx_hw->usb_env.txdesc_free_idx[0], 
 			skb,
@@ -1056,7 +1056,7 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 		rcu_read_unlock();
 #endif /* (LINUX_VERSION_CODE <= KERNEL_VERSION(2, 6, 35)) */
 #ifdef BR_SUPPORT_DEBUG
-		printk("SA=%pM, br_mac=%pM, type=0x%x, da[0]=%x, scdb=%pM, vif_type=%d\n", skb->data + MACADDRLEN,  vif->br_mac, *((unsigned short *)(skb->data + MACADDRLEN * 2)),
+		AICWFDBG(LOGTRACE, "SA=%pM, br_mac=%pM, type=0x%x, da[0]=%x, scdb=%pM, vif_type=%d\n", skb->data + MACADDRLEN,  vif->br_mac, *((unsigned short *)(skb->data + MACADDRLEN * 2)),
 			skb->data[0], vif->scdb_mac,RWNX_VIF_TYPE(vif));
 #endif
         spin_lock_bh(&vif->br_ext_lock);
@@ -1130,7 +1130,7 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 					newskb = skb_copy(skb, in_interrupt() ? GFP_ATOMIC : GFP_KERNEL);
 					if (newskb == NULL) {
 						/* priv->ext_stats.tx_drops++; */
-						printk("TX DROP: skb_copy fail!\n");
+						AICWFDBG(LOGERROR, "TX DROP: skb_copy fail!\n");
 						/* goto stop_proc; */
 						return -1;
 					}
@@ -1146,7 +1146,7 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 				}
 
 				if (skb_is_nonlinear(skb))
-					printk("%s(): skb_is_nonlinear!!\n", __FUNCTION__);
+					AICWFDBG(LOGERROR, "%s(): skb_is_nonlinear!!\n", __FUNCTION__);
 
 
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 18))
@@ -1155,7 +1155,7 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 				res = skb_linearize(skb);
 #endif /* (LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 18)) */
 				if (res < 0) {
-					printk("TX DROP: skb_linearize fail!\n");
+					AICWFDBG(LOGERROR, "TX DROP: skb_linearize fail!\n");
 					/* goto free_and_stop; */
 					return -1;
 				}
@@ -1164,7 +1164,7 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 				if (res < 0) {
 					if (res == -2) {
 						/* priv->ext_stats.tx_drops++; */
-						printk("TX DROP: nat25_db_handle fail!\n");
+						AICWFDBG(LOGERROR, "TX DROP: nat25_db_handle fail!\n");
 						/* goto free_and_stop; */
 						return -1;
 
@@ -1207,13 +1207,13 @@ int aic_br_client_tx(struct rwnx_vif *vif, struct sk_buff **pskb)
 		/* check if SA is equal to our MAC */
 		if (memcmp(skb->data + MACADDRLEN, vif->ndev->dev_addr, MACADDRLEN)) {
 			/* priv->ext_stats.tx_drops++; */
-			printk("TX DROP: untransformed frame SA:%02X%02X%02X%02X%02X%02X!\n",
+			AICWFDBG(LOGERROR, "TX DROP: untransformed frame SA:%02X%02X%02X%02X%02X%02X!\n",
 				skb->data[6], skb->data[7], skb->data[8], skb->data[9], skb->data[10], skb->data[11]);
 			/* goto free_and_stop; */
 			return -1;
 		}
 	}
-	printk("%s:exit\n",__func__);
+	AICWFDBG(LOGTRACE, "%s:exit\n",__func__);
 	return 0;
 }
 #endif /* CONFIG_BR_SUPPORT */
@@ -1673,7 +1673,7 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
     rtap_len = ieee80211_get_radiotap_len(rtap_buf);
     frame_len = skb->len;
 
-    printk("rwnx_start_monitor_if_xmit, skb_len=%d, rtap_len=%d\n", skb->len, rtap_len);
+    AICWFDBG(LOGTRACE, "rwnx_start_monitor_if_xmit, skb_len=%d, rtap_len=%d\n", skb->len, rtap_len);
 
     if((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8801) || 
         ((g_rwnx_plat->usbdev->chipid == PRODUCT_ID_AIC8800DC ||
@@ -1705,7 +1705,7 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
             case IEEE80211_RADIOTAP_RATE:
                 // This is basic 802.11b/g rate; use MCS/VHT for higher rates
                 rate = *iterator.this_arg;
-                printk("rate=0x%x\n", rate);
+                AICWFDBG(LOGTRACE, "rate=0x%x\n", rate);
                 for (idx = 0; idx < HW_RATE_MAX; idx++) {
                     if ((rate * 5) == tx_legrates_lut_rate[idx]) {
                         break;
@@ -1714,26 +1714,26 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                 if (idx < HW_RATE_MAX) {
                     rate_idx = idx;
                 } else {
-                    printk("invalid radiotap rate: %d\n", rate);
+                    AICWFDBG(LOGERROR, "invalid radiotap rate: %d\n", rate);
                 }
                 break;
 
             case IEEE80211_RADIOTAP_TX_FLAGS: {
                 u16_l txflags = get_unaligned_le16(iterator.this_arg);
-                printk("txflags=0x%x\n", txflags);
+                AICWFDBG(LOGTRACE, "txflags=0x%x\n", txflags);
                 if ((txflags & IEEE80211_RADIOTAP_F_TX_NOACK) == 0) {
-                    printk("  TX_NOACK\n");
+                    AICWFDBG(LOGTRACE, "  TX_NOACK\n");
                 }
                 if (txflags & 0x0010) { // Use preconfigured seq num
                     // NOTE: this is currently ignored due to qos_en=_FALSE and HW seq num override
-                    printk("  GetSequence\n");
+                    AICWFDBG(LOGTRACE, "  GetSequence\n");
                 }
             }
             break;
 
             case IEEE80211_RADIOTAP_MCS: {
                 u8_l mcs_have = iterator.this_arg[0];
-                printk("mcs_have=0x%x\n", mcs_have);
+                AICWFDBG(LOGTRACE, "mcs_have=0x%x\n", mcs_have);
                 rate_fmt = FORMATMOD_HT_MF;
                 if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_BW) {
                     u8_l bw = (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_BW_MASK);
@@ -1747,7 +1747,7 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                         bw = IEEE80211_RADIOTAP_MCS_BW_20;
                         ch_offset = 2; // CHNL_OFFSET_UPPER;
                     }
-                    printk("  bw=%d, ch_offset=%d\n", bw, ch_offset);
+                    AICWFDBG(LOGTRACE, "  bw=%d, ch_offset=%d\n", bw, ch_offset);
                 }
                 if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_MCS) {
                     u8_l fixed_rate = iterator.this_arg[2] & 0x7f;
@@ -1755,17 +1755,17 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                         fixed_rate = 0;
                     }
                     rate_idx = fixed_rate;
-                    printk("  fixed_rate=0x%x\n", fixed_rate);
+                    AICWFDBG(LOGTRACE, "  fixed_rate=0x%x\n", fixed_rate);
                 }
                 if ((mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_GI) && (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_SGI)) {
-                    printk("  sgi\n");
+                    AICWFDBG(LOGTRACE, "  sgi\n");
                 }
                 if ((mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_FEC) && (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_FEC_LDPC)) {
-                    printk("  ldpc\n");
+                    AICWFDBG(LOGTRACE, "  ldpc\n");
                 }
                 if (mcs_have & IEEE80211_RADIOTAP_MCS_HAVE_STBC) {
                     u8 stbc = (iterator.this_arg[1] & IEEE80211_RADIOTAP_MCS_STBC_MASK) >> IEEE80211_RADIOTAP_MCS_STBC_SHIFT;
-                    printk("  stbc=0x%x\n", stbc);
+                    AICWFDBG(LOGTRACE, "  stbc=0x%x\n", stbc);
                 }
             }
             break;
@@ -1775,17 +1775,17 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                 u8 known = iterator.this_arg[0];
                 u8 flags = iterator.this_arg[2];
                 rate_fmt = FORMATMOD_VHT;
-                printk("known=0x%x, flags=0x%x\n", known, flags);
+                AICWFDBG(LOGTRACE, "known=0x%x, flags=0x%x\n", known, flags);
                 // NOTE: this code currently only supports 1SS for radiotap defined rates
                 if ((known & IEEE80211_RADIOTAP_VHT_KNOWN_STBC) && (flags & IEEE80211_RADIOTAP_VHT_FLAG_STBC)) {
-                    printk("  stbc\n");
+                    AICWFDBG(LOGTRACE, "  stbc\n");
                 }
                 if ((known & IEEE80211_RADIOTAP_VHT_KNOWN_GI) && (flags & IEEE80211_RADIOTAP_VHT_FLAG_SGI)) {
-                    printk("  sgi\n");
+                    AICWFDBG(LOGTRACE, "  sgi\n");
                 }
                 if (known & IEEE80211_RADIOTAP_VHT_KNOWN_BANDWIDTH) {
                     u8_l bw = iterator.this_arg[3] & 0x1F;
-                    printk("  bw=0x%x\n",bw);
+                    AICWFDBG(LOGTRACE, "  bw=0x%x\n",bw);
                     // NOTE: there are various L and U, but we just use straight 20/40/80
                     // since it's not clear how to set CHNL_OFFSET_LOWER/_UPPER with different
                     // sideband sizes/configurations.  TODO.
@@ -1793,26 +1793,26 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                     txsig_bw = PHY_CHNL_BW_40;
                     if (bw == 0) {
                         txsig_bw = PHY_CHNL_BW_20;
-                        printk("  20M\n");
+                        AICWFDBG(LOGTRACE, "  20M\n");
                     } else if (bw >=1 && bw <= 3) {
-                        printk("  40M\n");
+                        AICWFDBG(LOGTRACE, "  40M\n");
                     } else if (bw >=4 && bw <= 10) {
-                        printk("  80M\n");
+                        AICWFDBG(LOGTRACE, "  80M\n");
                     } else if (bw >= 11 && bw <= 25) {
-                        printk("  160M\n");
+                        AICWFDBG(LOGTRACE, "  160M\n");
                     }
                 }
                 // User 0
                 nss = iterator.this_arg[4] & 0x0F; // Number of spatial streams
-                printk("  nss=0x%x\n", nss);
+                AICWFDBG(LOGTRACE, "  nss=0x%x\n", nss);
                 if (nss > 0) {
                     if (nss > 4) nss = 4;
                     mcs = (iterator.this_arg[4]>>4) & 0x0F; // MCS rate index
                     if (mcs > 8) mcs = 9;
                     rate_idx = mcs;
-                    printk("    mcs=0x%x\n", mcs);
+                    AICWFDBG(LOGTRACE, "    mcs=0x%x\n", mcs);
                     if (iterator.this_arg[8] & IEEE80211_RADIOTAP_CODING_LDPC_USER0) {
-                        printk("    ldpc\n");
+                        AICWFDBG(LOGTRACE, "    ldpc\n");
                     }
                 }
             }
@@ -1842,13 +1842,13 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
                 }
                 if (data2 & IEEE80211_RADIOTAP_HE_DATA2_GI_KNOWN) {
                     u8 gi = (data5 & IEEE80211_RADIOTAP_HE_DATA5_GI) >> 4;
-                    printk("  gi: %d\n", gi);
+                    AICWFDBG(LOGTRACE, "  gi: %d\n", gi);
                 }
             }
             break;
 
             default:
-                printk("unparsed arg: 0x%x\n",iterator.this_arg_index);
+                AICWFDBG(LOGTRACE, "unparsed arg: 0x%x\n",iterator.this_arg_index);
                 break;
         }
     }
@@ -1883,14 +1883,14 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
             txq = rwnx_txq_vif_get(vif, NX_UNK_TXQ_TYPE);
     }
     if (txq->idx == TXQ_INACTIVE) {
-        printk("TXQ_INACTIVE\n");
+        AICWFDBG(LOGERROR, "TXQ_INACTIVE\n");
         goto free_tag;
     }
     // prepare to xmit
     headroom = sizeof(struct rwnx_txhdr);
     skb_mgmt = dev_alloc_skb(headroom + frame_len);
     if (!skb_mgmt) {
-        printk("skb_mgmt alloc fail\n");
+        AICWFDBG(LOGERROR, "skb_mgmt alloc fail\n");
         goto free_tag;
     }
     skb_reserve(skb_mgmt, headroom);
@@ -1912,7 +1912,7 @@ netdev_tx_t rwnx_start_monitor_if_xmit(struct sk_buff *skb, struct net_device *d
     sw_txhdr = kmem_cache_alloc(rwnx_hw->sw_txhdr_cache, GFP_ATOMIC);
     if (unlikely(sw_txhdr == NULL)) {
         dev_kfree_skb(skb_mgmt);
-        printk("sw_txhdr alloc fail\n");
+        AICWFDBG(LOGERROR, "sw_txhdr alloc fail\n");
         goto free_tag;
     }
     txhdr->sw_hdr = sw_txhdr;
diff --git a/aic8800_fdrv/rwnx_txq.c b/aic8800_fdrv/rwnx_txq.c
index 400cb1a..1b44262 100644
--- a/aic8800_fdrv/rwnx_txq.c
+++ b/aic8800_fdrv/rwnx_txq.c
@@ -1255,7 +1255,7 @@ void rwnx_hwq_process(struct rwnx_hw *rwnx_hw, struct rwnx_hwq *hwq)
         /* sanity check for debug */
         BUG_ON(!(txq->status & RWNX_TXQ_IN_HWQ_LIST));
 		if(txq->idx == TXQ_INACTIVE){
-			printk("%s txq->idx == TXQ_INACTIVE \r\n", __func__);
+			AICWFDBG(LOGERROR, "%s txq->idx == TXQ_INACTIVE \r\n", __func__);
 			continue;
 		}
         BUG_ON(txq->idx == TXQ_INACTIVE);
diff --git a/aic8800_fdrv/sdio_host.c b/aic8800_fdrv/sdio_host.c
index 59225b3..59204fd 100644
--- a/aic8800_fdrv/sdio_host.c
+++ b/aic8800_fdrv/sdio_host.c
@@ -100,14 +100,14 @@ void aicwf_sdio_host_tx_cfm_handler(struct sdio_host_env_tag *env, u32 *data)
         {
             // No more confirmations, so put back the used index at its initial value
             env->txdesc_used_idx[queue_idx] = used_idx;
-            printk("ERROR:No more confirmations\r\n");
+            AICWFDBG(LOGERROR, "ERROR:No more confirmations\r\n");
             //break;
         }
         // set the cfm status
         skb = (struct sk_buff *)(uint32_t)host_id;
         txhdr = (struct rwnx_txhdr *)skb->data;
         txhdr->hw_hdr.cfm.status = (union rwnx_hw_txstatus)data[0];
-        printk("sdio_host_tx_cfm_handler:used_idx=%d, 0x%p, status=%x\r\n",used_idx, env->pthis, txhdr->hw_hdr.cfm.status.value);
+        AICWFDBG(LOGTRACE, "sdio_host_tx_cfm_handler:used_idx=%d, 0x%p, status=%x\r\n",used_idx, env->pthis, txhdr->hw_hdr.cfm.status.value);
         //if (env->cb.send_data_cfm(env->pthis, host_id) != 0)
         if (rwnx_txdatacfm(env->pthis, (void *)host_id) != 0)
         {
@@ -115,7 +115,7 @@ void aicwf_sdio_host_tx_cfm_handler(struct sdio_host_env_tag *env, u32 *data)
             env->txdesc_used_idx[queue_idx] = used_idx;
             env->tx_host_id[queue_idx][used_idx % SDIO_TXDESC_CNT] = host_id;
             // and exit the loop
-            printk("ERROR:rwnx_txdatacfm,\r\n");
+            AICWFDBG(LOGERROR, "ERROR:rwnx_txdatacfm,\r\n");
           //  break;
         }
 
diff --git a/aic8800_fdrv/usb_host.c b/aic8800_fdrv/usb_host.c
index fca8dc4..b483f88 100644
--- a/aic8800_fdrv/usb_host.c
+++ b/aic8800_fdrv/usb_host.c
@@ -107,7 +107,7 @@ void aicwf_usb_host_tx_cfm_handler(struct usb_host_env_tag *env, u32 *data)
             return;
             //break;
         }
-		AICWFDBG(LOGINFO, "usb_host_tx_cfm_handler:used_idx=%d, hostid=%p, 0x%p, status=%x\r\n",used_idx, (void *)host_id, env->pthis, data[0]);
+		AICWFDBG(LOGTRACE, "usb_host_tx_cfm_handler:used_idx=%d, hostid=%p, 0x%p, status=%x\r\n",used_idx, (void *)host_id, env->pthis, data[0]);
 
         // set the cfm status
         skb = (struct sk_buff *)host_id;
